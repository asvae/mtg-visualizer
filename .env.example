# Copy to .env (gitignored) and set to 1 to show the review panel (🧾 icon in
# the header) — the card-tagging workflow tool, not meant for a deployed/public
# copy of the app. Leave unset (or omit the file) on a server deployment.
VITE_ENABLE_REVIEW=1
