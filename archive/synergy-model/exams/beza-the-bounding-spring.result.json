{
  "name": "Beza, the Bounding Spring",
  "manaCost": "{2}{W}{W}",
  "typeLine": "Legendary Creature — Elemental Elk",
  "description": "When this creature enters, create a Treasure token if an opponent controls more lands than you. You gain 4 life if an opponent has more life than you. Create two 1/1 blue Fish creature tokens if an opponent controls more creatures than you. Draw a card if an opponent has more cards in hand than you.",
  "assumptions": [
    "Per the user's explicit design decision: ConditionCheckSVar$/ConditionSVarCompare$'s exact operator/threshold isn't parsed precisely -- the goal is to show WHAT the card measures (a paired your-count vs. opponent's-count of some named metric) and mark the gated effect as conditional, not to reproduce the comparison with full rigor."
  ],
  "couldNotDerive": ["The precise Count$/PlayerCountOpponents$ sub-expression behind each metric -- the metric name is read heuristically off Forge's own X<Metric>/Y<Metric> SVar-naming convention, not resolved from the actual counted expression."],
  "verdict": "match",
  "notes": "Implemented per the user's own design call: two `sensor` nodes (owner:me / owner:opp) per conditional clause, named via SCHEMA's own `:=` binding convention (qty:=your_<metric> / qty:=opp_<metric>), plus a loose `cond:compare=opp_X<op>your_X` fact on the gated effect. Only fires on Beza's own exact shape (ConditionCheckSVar$ X<Metric> compared against ConditionSVarCompare$ <op>Y<Metric>) -- tightened after finding other real cards (Harvestrite Host's EQ2, Cache Grab's GT0) compare against a plain NUMBER instead of a Y-prefixed variable, where fabricating an opponent-side sensor would have been a wrong fact, not an approximation. Verified: all 4 of Beza's clauses now show the correct sensor pair + compare flag. Resolves the known_gap_classes ConditionCheckSVar$/ConditionSVarCompare$ entry for this exact two-player-comparison shape (the other 10 cards in that list may use different shapes -- not all re-verified this round). In blb-success.json.",
  "comparedAt": "2026-09-04"
}
