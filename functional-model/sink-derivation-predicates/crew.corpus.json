{
  "total": 3,
  "passing": 3,
  "cases": [
    {
      "case": "crewCost + a real activationCost",
      "mirrors": "The Lunar Whale (fin/60)",
      "expectedVerdict": "produces-tap",
      "note": "A mock Vehicle with both crewCost and a real activationCost set. Verified in crew.test.ts via a real activateAbility call genuinely tapping the crewing creature (not a scripted scenario/trace — a single direct engine call)."
    },
    {
      "case": "crewCost + activationCost, PLUS a separate named ability on the same permanent",
      "mirrors": "Cargo Ship (ENGINE_GAPS.md gap #11 shape)",
      "expectedVerdict": "produces-tap",
      "note": "A mock Vehicle with the same crewCost+activationCost shape as above, plus a second, independently-named ability (e.g. a mana ability). Verified in crew.test.ts via real canActivateAbility calls proving the crew path (no abilityName) and the named-ability path (abilityName set) are both independently legal and don't collide."
    },
    {
      "case": "crewCost declared with NO activationCost at all",
      "mirrors": "The Regalia (fin/58) — a real, currently-live engine gap",
      "expectedVerdict": "no-tap",
      "note": "A mock Vehicle with crewCost but no activationCost. Verified in crew.test.ts via a real canActivateAbility rejection ('has no such activated ability') — not a hypothetical, this is the real engine.ts gate any card in this exact shape hits today."
    }
  ]
}
