# Arbiter of Woe — authoring notes

Migrated from `staticAbilities` to `missingSchemaFunctionality`
(2026-09-18, FDN schema-tightness redesign) — see that field's own
`clause`/`demand` entry in `definition.ts` for the real gap detail.
