[
  {
    "type": "definition",
    "textLocation": "oracle_text",
    "spans": [
      {
        "from": 0,
        "to": 6,
        "text": "Raid —"
      },
      {
        "from": 7,
        "to": 104,
        "text": "When this creature enters, if you attacked this turn, this creature deals 2 damage to any target."
      }
    ],
    "definitionKeys": [
      {
        "kind": "trigger",
        "name": "onEnterRaid"
      }
    ],
    "reasoning": "\"Raid —\" is this ability's own template label, fully expressed by this same trigger's own `condition` field, not a separate clause of its own. This card's own `onEnterRaid` trigger (`on:'enter'`) carries `condition: {kind:'attackedThisTurn'}` (2026-09-18 schema-completeness pass, added during this same coverage-justification authoring pass — was previously a stale omission) — the exact real Raid gate — and runs `dealDamageAnyTarget(amount:2)`, the exact real damage. Declaratively real but not yet engine-enforced (Ward pattern — see `engine-support-registry.ts`'s own `board-state-condition-not-enforced` entry)."
  }
]
