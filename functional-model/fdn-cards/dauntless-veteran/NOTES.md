# Dauntless Veteran — authoring notes

No authoring narration worth keeping — the one existing comment (the
real card text directly above the `pumpAll` trigger) already met the new
"card text over a genuinely complex effect" convention as-is and was left
in place.
