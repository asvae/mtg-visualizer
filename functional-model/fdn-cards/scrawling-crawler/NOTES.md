# Scrawling Crawler — authoring notes

## Authoring background

Bare name-only trigger — no `Trigger.on` value exists for "an opponent
draws a card." `loseLife.owner:'opponents'` is the same documented
`EffectOwner` group-only limitation `drawCard.owner`'s own doc comment
already covers (no single-arbitrary-player targeting exists), not a
novel gap for this card.
