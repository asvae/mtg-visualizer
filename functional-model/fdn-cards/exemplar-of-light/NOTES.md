# Exemplar of Light — authoring notes

## Authoring background

Trigger "whenever you gain life" doesn't have an `on` value yet
(ENGINE_GAPS.md). Modeled as a named trigger for manual scenario
invocation.

Trigger "whenever you put one or more +1/+1 counters on this creature"
doesn't have an `on` value yet (ENGINE_GAPS.md). Modeled as a named
trigger for manual scenario invocation. The `activationLimit` applies:
triggers only once each turn.
