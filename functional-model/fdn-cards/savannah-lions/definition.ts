import type { CardDefinition } from '../../card';

// Vanilla creature — no oracle text (real Forge `Oracle:` line is empty).
export const savannahLions: CardDefinition = {
  name: 'Savannah Lions',
  manaCost: '{W}',
  typeLine: 'Creature — Cat',
  pt: [2, 1],
};
