# Claws Out — authoring notes

No authoring narration worth keeping beyond what the code/justification.json
already say for themselves — the removed inline comments were either
self-evident restatements of literal card text (the `costReduction`/
`pumpAll` fields) or a boilerplate "coverage-justification moved to
justification.json" note with no unique content.
