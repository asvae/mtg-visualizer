{
  "enrichment": "ai",
  "review": "human",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-16",
  "notes": "2026-09-16 (definition lane, task 2 of an engine-core-escalated cleanup pass): the `knownGaps`/exemption bookkeeping below was stale — engine-core's own 2026-09-15 ENGINE_GAPS.md closure (`Card.isAttacking()`/`GameState.attackers`, `pumpAll`'s own `predicate:'attacking-creatures'`, `recognizers/pumpAllAttacking-effect-structural.ts`) already migrated `definition.ts`'s \"Attacking creatures get +2/+0\" off the honest `custom` no-op onto a real, executable `kind:'pumpAll'` effect, but nothing had updated THIS file to say so, and `scenarios.ts` still only demonstrated the old no-op framing. `scenarios.ts` rewritten this pass: `normalCastDuringCombat()` now declares a real attacker (`pilotDeclareAttackers`) alongside a real non-attacking creature you control, casts Auron's Inspiration during combat, and reads real `effectivePT` before/after on BOTH creatures — the attacker's power genuinely recalculates +2 (2/2 -> 4/2), the non-attacker's stays unchanged, proving the real \"attacking,\" not \"you control,\" scope live (not just described). `flashbackCast()` kept, unchanged in shape. `review` reset to 'ai' (real behavioral coverage genuinely changed, even though the underlying `definition.ts` edit itself predates this pass). `knownGaps` below updated to drop the now-closed item. Flagged, not fixed (out of this lane's scope — core-engine/recognizer-tooling file): `scripts/verify-synergy.mjs`'s own `isAuronsInspirationBroadcastPumpFact` exemption (~line 1195) still says \"no possible trace evidence... without first building that missing cross-cutting engine surface\" — that surface now exists and this pass's own new scenario proves it with real trace evidence, so the exemption looks stale and worth the engine-core lane retiring (verify-synergy.mjs still passes with it left in place; it's just a now-unnecessarily-generous tolerance, not a false negative). `apply-recognizers.mjs`/`verify-synergy.mjs` re-run this pass: synergy.json unchanged (definition.ts's own facts were untouched by this pass), verify-synergy.mjs: 0 hard failures. Full `npx vitest run functional-model`: 798 passed, 5 skipped (unchanged).\n\n2026-09-11 (later): added a real SOURCE `{event:'pump'}` fact for \"Attacking creatures get +2/+0\" — deliberately bare (no target/controller/targeted at all: the real effect is symmetric across BOTH players' attackers, and no \"attacking\" bucket/predicate vocabulary exists anywhere in this model to name that constraint honestly, so per the targeted field's own convention, omitting it is the honest choice, not an incomplete one). `event:'pump'` just promoted off `verify-synergy.mjs`'s `PARKED_ACTION_FNS` to real vocabulary (user's own explicit ask: \"otherwise fin8 pretty much does nothing\"). The underlying ENGINE gap (no live attacker-state reaches card.ts's Effect-resolution surface) was, AT THE TIME, unchanged and still real — no possible trace evidence existed for this fact then — so a new, narrowly-scoped `isAuronsInspirationBroadcastPumpFact` exemption was added to `verify-synergy.mjs` (real fact, real documented gap, tolerated exactly the way `verify-synergy.mjs`'s own soft-note/hard-failure discipline already allows for other documented-but-unverifiable cases; NOTE: this gap was later closed 2026-09-15, see the 2026-09-16 note above — this paragraph is preserved as history, not current status). Oracle-anchored (\"get +2/+0\"). `find-synergies.mjs` diff (isolated to just this fact): zero matches gained/lost. Migrated to the unified Fact/annotations model (2026-09-11) AND fixed this card's long-standing hard `verify-synergy.mjs` failure for real, at the root: the old `synergy.json` declared a `zone:'Exile'` produce fact for Flashback with zero possible trace evidence, because `engine.ts`'s `canCastSpell`/`castSpell` had no alternate-cost/cast-from-graveyard legality path at all (ENGINE_GAPS.md gap #7) — no scenario could ever demonstrate it, faked or otherwise. Real engine fix, not a data workaround: `canCastSpell`/`castSpell` (engine.ts) take an optional `alt?: AlternateCost` — when given, `alt.cost` REPLACES `card.manaCost` for affordability/payment (timing still checked against the card's own type per CR 702.32b), and `StackObject` (stack.ts) gained `thenExile?: boolean` so `resolveTop` sends a resolved alt-cost spell to Exile instead of the Graveyard. `engine-trace.ts`'s `pilotCast`/`pilotResolveTop` thread the same `alt` through so a scenario can pilot a REAL Flashback cast. Facts reshaped: dropped the old bare `zone`-shaped facts/`id`/`sourceText`/`highlight`; `self-graveyard` -> `to:'Graveyard'`; the old single `flashback-exile` (`zone:'Exile'`) fact split into two independent, now-real facts matching the ACT-vs-CONSEQUENCE standing rule — `{event:'cast', from:'Graveyard', target:'self'}` (the alternate-cost cast itself, parallel to the existing `{event:'cast', from:'Hand', target:'self'}` self-cast) and `{to:'Exile', controller:'you', subject:'self'}` (the guaranteed post-resolution consequence, parallel to `self-graveyard`'s own `to:'Graveyard'`). `annotations` computed for all 4 facts via compute-annotations.mjs (self-cast/self-graveyard anchored to the printed type line 'Instant'; the two flashback facts anchored to the real Flashback oracle-text line, split across 'cast this card from your graveyard for its flashback cost' and 'Then exile it'). Added to scripts/annotation-coverage.mjs's ANNOTATED_CARD_SLUGS. Real find-synergies.mjs diff: net +5 interaction lines pool-wide from the two new real facts (flashback-cast's own `event:'cast',from:'Graveyard'` and flashback-exile's own `to:'Exile'`) — see the delivering agent's own final report for the exact matched card list; 0 lines lost (the old, never-verifiable `zone:'Exile'` fact never actually matched anything real either, since nothing in the pool wants an unconstrained Exile-presence sink).",
  "textCoverageAudited": true,
  "knownGaps": [],
  "oracleTextSnapshot": "Attacking creatures get +2/+0 until end of turn.\nFlashback {2}{W}{W} (You may cast this card from your graveyard for its flashback cost. Then exile it.)",
  "reviewedAt": "2026-09-16"
}
