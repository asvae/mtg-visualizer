{
  "enrichment": "ai",
  "review": "human",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-11",
  "notes": "2026-09-11 (later): added a real SOURCE `{event:'pump'}` fact for \"Attacking creatures get +2/+0\" — deliberately bare (no target/controller/targeted at all: the real effect is symmetric across BOTH players' attackers, and no \"attacking\" bucket/predicate vocabulary exists anywhere in this model to name that constraint honestly, so per the targeted field's own convention, omitting it is the honest choice, not an incomplete one). `event:'pump'` just promoted off `verify-synergy.mjs`'s `PARKED_ACTION_FNS` to real vocabulary (user's own explicit ask: \"otherwise fin8 pretty much does nothing\"). The underlying ENGINE gap (no live attacker-state reaches card.ts's Effect-resolution surface) is UNCHANGED and still real — no possible trace evidence exists for this fact today — so a new, narrowly-scoped `isAuronsInspirationBroadcastPumpFact` exemption was added to `verify-synergy.mjs` (real fact, real documented gap, tolerated exactly the way `verify-synergy.mjs`'s own soft-note/hard-failure discipline already allows for other documented-but-unverifiable cases). Oracle-anchored (\"get +2/+0\"). `find-synergies.mjs` diff (isolated to just this fact): zero matches gained/lost. `knownGaps` below updated to reflect: the FACT gap is now closed, the ENGINE gap (no live attacker-state surface) remains open and is unaffected by this change. Migrated to the unified Fact/annotations model (2026-09-11) AND fixed this card's long-standing hard `verify-synergy.mjs` failure for real, at the root: the old `synergy.json` declared a `zone:'Exile'` produce fact for Flashback with zero possible trace evidence, because `engine.ts`'s `canCastSpell`/`castSpell` had no alternate-cost/cast-from-graveyard legality path at all (ENGINE_GAPS.md gap #7) — no scenario could ever demonstrate it, faked or otherwise. Real engine fix, not a data workaround: `canCastSpell`/`castSpell` (engine.ts) take an optional `alt?: AlternateCost` — when given, `alt.cost` REPLACES `card.manaCost` for affordability/payment (timing still checked against the card's own type per CR 702.32b), and `StackObject` (stack.ts) gained `thenExile?: boolean` so `resolveTop` sends a resolved alt-cost spell to Exile instead of the Graveyard. `engine-trace.ts`'s `pilotCast`/`pilotResolveTop` thread the same `alt` through so a scenario can pilot a REAL Flashback cast. `scenarios.ts` now has two engine-piloted scenarios: a normal cast (Hand -> Stack -> resolve -> Graveyard) and a real Flashback cast (seeded directly into the Graveyard, cast via `alternateCosts[0]`, paying {2}{W}{W}, resolving to Exile). Facts reshaped: dropped the old bare `zone`-shaped facts/`id`/`sourceText`/`highlight`; `self-graveyard` -> `to:'Graveyard'`; the old single `flashback-exile` (`zone:'Exile'`) fact split into two independent, now-real facts matching the ACT-vs-CONSEQUENCE standing rule — `{event:'cast', from:'Graveyard', target:'self'}` (the alternate-cost cast itself, parallel to the existing `{event:'cast', from:'Hand', target:'self'}` self-cast) and `{to:'Exile', controller:'you', subject:'self'}` (the guaranteed post-resolution consequence, parallel to `self-graveyard`'s own `to:'Graveyard'`). `annotations` computed for all 4 facts via compute-annotations.mjs (self-cast/self-graveyard anchored to the printed type line 'Instant'; the two flashback facts anchored to the real Flashback oracle-text line, split across 'cast this card from your graveyard for its flashback cost' and 'Then exile it'). Card's OTHER real ability ('Attacking creatures get +2/+0 until end of turn') stays an honest, documented no-op and gets no Fact at all — checked, and this is a genuinely deeper, cross-cutting engine gap (no attacker state reaches card.ts's Effect-resolution layer at all, for anyone's creatures — needs new Card/Player interface surface plus a new pumpAll predicate spanning both players, not a one-card fix), NOT the same gap as the Exile-zone one this pass fixed; left clearly flagged in definition.ts and ENGINE_GAPS.md rather than fabricated or silently dropped. Added to scripts/annotation-coverage.mjs's ANNOTATED_CARD_SLUGS. Real find-synergies.mjs diff: net +5 interaction lines pool-wide from the two new real facts (flashback-cast's own `event:'cast',from:'Graveyard'` and flashback-exile's own `to:'Exile'`) — see the delivering agent's own final report for the exact matched card list; 0 lines lost (the old, never-verifiable `zone:'Exile'` fact never actually matched anything real either, since nothing in the pool wants an unconstrained Exile-presence sink).",
  "textCoverageAudited": true,
  "knownGaps": [
    "\"Attacking creatures get +2/+0 until end of turn\" now HAS a real Fact (`event:'pump'`, 2026-09-11) but still has NO engine implementation — real, cross-cutting gap (no attacker state reaches card.ts's Effect-resolution layer for any player's creatures; would need new Card/Player interface surface + a pumpAll predicate spanning both ctx.you and ctx.opponents), out of scope for a single-card migration. The fact is tolerated as documented-but-currently-unverifiable via verify-synergy.mjs's own narrowly-scoped isAuronsInspirationBroadcastPumpFact exemption."
  ],
  "oracleTextSnapshot": "Attacking creatures get +2/+0 until end of turn.\nFlashback {2}{W}{W} (You may cast this card from your graveyard for its flashback cost. Then exile it.)",
  "reviewedAt": "2026-09-11"
}
