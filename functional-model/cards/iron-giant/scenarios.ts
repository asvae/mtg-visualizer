import type { Scenario } from '../../harness';

export const scenarios: Scenario[] = [{ result: 'creature enters, no other effect (Vigilance/Reach/Trample are recognized-but-inert structured facts — see card.ts\'s own Keyword doc comment)' }];
