{
  "enrichment": "ai",
  "review": "human",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-12",
  "notes": "2026-09-12, later: consolidated the 2 scenarios (plain onEnter trigger; separate activate w/ unrelated filler creature) into ONE, per user's own real insight: \"you can use hero to crew the armor\" — the ETB's own 1/1 Hero token already satisfies Crew 1's real power requirement, no separate filler creature needed. Kept the top-level `trigger:'onEnter'` (creates the Hero token) — this card's own `activationCost` (a descriptive Crew-lifecycle label) would otherwise make `harness.ts`'s `lifecycleBefore` auto-fire an unwanted extra top-level `activate` the instant neither `trigger` nor `ability` is set, same real constraint paladin-s-arms/dragoon-s-lance hit — then added `sequence:[{activate:true}]` to run Crew 1 for real right after. Confirmed in the regenerated trace.json: trigger->createToken->activate->animate, single-fire, no double activation. `isCrewCostCreatureWant`'s own exemption (structural, no trace needed regardless of scenario shape) is unaffected. verify-synergy.mjs (scoped: 0 hard failures; full pool 315: 0 hard failures). vitest 238/238. `review` reset to 'ai' (authored scenarios.ts changed).\n\nMigrated to unified Fact model (2026-09-12). 5 source + 1 sink fact, all annotated (ANNOTATED_CARD_SLUGS). New real vocabulary added this pass: `event:'crew'` (the Crew N cost ACT) and `Fact.type` (grantType's granted-type detail) + promoted `animate` off verify-synergy.mjs's PARKED_ACTION_FNS to real evidence (a self-targeted `animate` effect's logged `types` array backs a `grantType` fact for real). Added `isCrewCostCreatureWant` exemption (verify-synergy.mjs) for the sink fact wanting a Battlefield creature to crew — tautologically real, no achievable trace evidence given this harness's simplified activationCost lifecycle (no crewedBy simulation). find-synergies.mjs diff: 12 -> 152 interaction lines (the 12 lost were the old illegitimate bare-presence SOURCE fact, correctly removed per the no-presence-shaped-source rule; 152 gained are legitimate, mostly via the new creature-on-battlefield sink and the two real entersBattlefield facts). Zero grantType/crew matches yet pool-wide (new vocabulary, no consumer sink exists today) — same 'vocabulary now real, matched later' shape as the pump promotion.",
  "textCoverageAudited": true,
  "knownGaps": [
    "Crew (702.121b/c) has real engine execution (canActivateAbility/activateAbility with crewCost+crewedBy, ENGINE_DESIGN.md) but this card's own scenario harness lifecycle doesn't simulate a real crewedBy creature list/tap trace — the sink fact wanting a Battlefield creature to crew has zero achievable trace evidence for that reason (isCrewCostCreatureWant exemption in verify-synergy.mjs), not a modeling gap in the fact itself.",
    "animate/LayerSet tracks no duration (layers.ts's documented scope, pre-existing, not new to this pass) — a crewed Vehicle becomes a creature PERMANENTLY in this model rather than 'until end of turn'."
  ],
  "oracleTextSnapshot": "When this Vehicle enters, create a 1/1 colorless Hero creature token.\nCrew 1 (Tap any number of creatures you control with total power 1 or more: This Vehicle becomes an artifact creature until end of turn.)",
  "reviewedAt": "2026-09-12"
}
