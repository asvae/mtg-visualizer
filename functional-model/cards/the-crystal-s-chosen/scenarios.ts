import type { Scenario } from '../../harness';

export const scenarios: Scenario[] = [{ result: 'creates four 1/1 Hero tokens, then puts a +1/+1 counter on each creature you control (including the new tokens)', you: { creaturesCount: 2 } }];
