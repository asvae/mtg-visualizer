{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "notes",
  "lastVerified": "2026-09-12",
  "notes": "2026-09-12: migrated to the unified Fact/annotations model (SYNERGY_DESIGN.md's synergy.json v2 unified schema — same rework as summon-bahamut/adelbert-steiner/fate-of-the-sun-cryst). Real oracle text (Scryfall-confirmed, data/fin/fin_scryfall.json #51): \"When Edgar enters, draw a card for each artifact you control.\\nTwo-Headed Coin — The first time you flip one or more coins each turn, those coins come up heads and you win those flips.\" Legendary Creature — Human Artificer Noble, {4}{U}{U}, pt [4,5]. Dropped the old v1 `id`/`sourceText`/`highlight` fields, and — per the Adelbert Steiner precedent (this card's own oracle text has zero death/sacrifice/removal-related content, same as Steiner's) — REMOVED the old v1 self-graveyard/self-dies facts entirely: they were unfounded generic \"every creature can die\" boilerplate with no real textual basis, not something this card's own text actually supports. Real facts kept/added: `self-cast` ({event:'cast', from:'Hand', target:'self'}, typeLine-anchored 'Creature'), `self-enters` ({event:'entersBattlefield', to:'Battlefield', subject:'self', target:'self'}, typeLine-anchored 'Creature' — kept `subject:'self'` since the OLD v1 `self-battlefield`-style fact already carried it, same 'don't drop a subject the old fact already had' lesson summon-bahamut's/adelbert-steiner's own merges already established), and the real ETB `drawCard` ({event:'drawCard', controller:'you'}, oracle-anchored 'draw a card for each artifact you control'). The real per-artifact SCALING itself is unchanged, already-correct engine machinery (`definition.ts`'s own `amount: (ctx) => ctx.you.getCardsIn('Battlefield').filter(c => c.isArtifact()).length`, the `Computed<number>` escape hatch's canonical live-count-read use) — no new Fact vocabulary exists or was invented for 'this SOURCE effect's own magnitude scales with a live board-state count' (same open, deliberately-parked gap Adelbert Steiner's own CDA P/T scaling already documents in SYNERGY_DESIGN.md — checked, this is the exact same category, not a new one); `Fact.value` (deprioritized per standing 2026-09-11 decision) is a flat weight, not a scaling formula. SINK half: real `{to:'Battlefield', controller:'you', types:{has:['Artifact']}}` (Edgar *wanting* artifacts on the battlefield), oracle-anchored 'artifact you control' — same 'wants-X' pattern Adelbert Steiner's own wants-equipment sink already establishes. `Two-Headed Coin` (the coin-flip-win static): checked for existing real vocabulary first — `event:'coinFlip'` is real, live pool vocabulary (The Gold Saucer, fin, added 2026-09-09) but only for the FLIP ITSELF happening as a guaranteed, non-probabilistic occurrence; Edgar's own ability is a genuine CR-614-style REPLACEMENT effect on a random flip's OUTCOME, a structurally different and deeper claim no execution or Fact vocabulary exists for anywhere in this engine (confirmed: no `random`/`coin`/`Replacement` mechanism anywhere in card.ts/interfaces.ts/engine.ts/layers.ts). Left as real, structured `staticAbilities` text only, no Fact authored for it, same treatment fate-of-the-sun-cryst's cost-reduction static and the still-open Equipment P/T-bonus/type-grant clauses (gap #14) get. Documented as a new, dedicated ENGINE_GAPS.md gap #15 (not folded into an existing one — it's two gaps deep: no coin-flip resolution mechanism to replace, and no general replacement-effect engine to hook a replacement into). scenarios.ts migrated to the engine-trace pilot format (`runEngineScenarios`) to get REAL cast/entersBattlefield/drawCard trace evidence — the old plain `harness.ts` `Scenario[]` shape (`trigger:'onEnter'`) skips the cast/enters lifecycle entirely (see `lifecycleBefore`'s own `scenario.trigger` branch), so it could never back `self-cast`/`self-enters` with real evidence. Added `on:'enter'` to the card's own `onEnter` trigger (definition.ts) so `pilotResolveTop` auto-fires it for real (same convention Weapons Vendor/Cloud, Midgar Mercenary already established), instead of requiring a scenario to name it explicitly. ONE real scenario per the standing 'default 1, no edge-case coverage' rule (dropped the old v1 second `artifactsCount:0` scenario and the `keywordScenarios(...)` spread, which would otherwise have added an unrequested legend-rule edge case since Edgar is Legendary): casts Edgar for {4}{U}{U} with two real, differently-shaped FIN blue artifacts already on the battlefield (Astrologian's Planisphere, an Equipment; Ether, a plain Artifact — not reusing Dragoon's Lance/generic filler, real variety per the standing scenario-content rule) — resolving him auto-fires the real onEnter trigger, reading a real artifact count of 2 and drawing 2 real cards. `verify-synergy.mjs` (scoped): 0 hard failures, 6 expected soft notes (tapForMana ×6, same shape every other engine-piloted mana-paying scenario produces). `npx vitest run functional-model`: 238/238. Real `find-synergies.mjs` diff (isolated via an old/new file swap against the current, concurrently-changing pool — not a stale git-HEAD diff, since several sibling sessions have uncommitted migrations in flight): -24 lines lost (21 `graveyard presence` + 3 `dying`, all fully explained by removing the two unfounded death/graveyard facts above), the remaining 141 `battlefield presence` lines cleanly RELABELED (not lost/regained) to `enters the battlefield` (pure shape-family rename, same zone-shaped match set, same count both before and after — confirmed via a before/after per-label count, not just the raw line-count delta). Sink side (21 real artifact-producer -> Edgar edges) unchanged. `compute-annotations.mjs`/`compute-weights.mjs --slug=edgar-king-of-figaro` both run (4 facts annotated; all `-1` sentinels resolved to real `value:1`). Card added to `ANNOTATED_CARD_SLUGS` (scripts/annotation-coverage.mjs).",
  "textCoverageAudited": false,
  "knownGaps": [
    "Two-Headed Coin (\"the first time you flip one or more coins each turn, those coins come up heads and you win those flips\") has no Fact/engine vocabulary — documented in ENGINE_GAPS.md gap #15 (new), not modeled here; a real CR-614-style replacement effect on a random outcome, two gaps deep (no coin-flip resolution mechanism to replace, no general replacement-effect engine)",
    "the ETB draw's own real per-artifact SCALING has no Fact vocabulary beyond a flat weight (same open, deliberately-parked gap Adelbert Steiner's CDA P/T scaling documents in SYNERGY_DESIGN.md) — the engine-side Computed<number> execution is already correct and real, this is a Fact-authoring-vocabulary gap only"
  ]
}
