{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "notes",
  "lastVerified": "2026-09-16",
  "notes": "2026-09-16 (recognizer-lane escalation batch, engine agent): both real recognizer-lane escalations this card's own prior notes named below are now CLOSED. (1) New `recognizers/winCoinFlip-structural.ts` (direct sibling of `lifegainDoubleKeyword-structural.ts`, single-card real bar) reads `keywords.includes('TwoHeadedCoin')` and requires the FULL literal clause verbatim; `apply-recognizers.mjs edgar-king-of-figaro` retags the existing hand-authored `winCoinFlip` fact with real provenance, widening its own annotation to the whole sentence (closing the text-coverage gap the old tail-only annotation left open). (2) `drawCardProgram-effect-structural.ts` gained a new `Aggregate{op:'count'}`-amount branch (mirrors `ptFormula-scalingPump-structural.ts`'s own paired source+sink convention) — matches this card's own `program: drawCard(you.permanentsInPlay().filter('cardType','artifact').count())` shape, retagging the existing `drawCard` source fact AND emitting the paired `{to:'Battlefield', controller:'you', types:{has:['Artifact']}}` sink with real provenance (previously unprovenanced). `apply-recognizers.mjs edgar-king-of-figaro`: 3 existing facts retagged (drawCardProgram-effect-structural ×2, winCoinFlip-structural ×1), 0 new facts appended (both already existed, hand-authored). `verify-synergy.mjs` (scoped and full-pool): 0 hard failures. `npx tsc --noEmit`/`npx vitest run functional-model`: clean. `review` stays \"ai\" (already \"ai\"). Full before/after `data/fin/fin_card_status.json` comparison: unchanged for this card (was already fully covered by real facts; only provenance/annotation changed, not the fact set itself). Prior 2026-09-16 (AI-fact-elimination sweep, card-results lane, per scripts/AI_FACT_ELIMINATION_PROCESS.md): re-examined all 3 remaining unprovenanced facts fact-by-fact (drawCard source, winCoinFlip source, sink `{to:'Battlefield',types:{has:['Artifact']}}`) — none closed by an existing-recognizer rerun (`apply-recognizers.mjs edgar-king-of-figaro`: 0 new facts both before and after the definition.ts change below), but real progress made on 2 of 3: (1) **ENGINE GAP #15 IS NOW CLOSED, this card's own knownGaps entry below was STALE** — `card.ts`'s `TwoHeadedCoin` keyword + `state.flipCoin`'s own real CR-614-style replacement hook (both real, confirmed by direct read) now exist; the 2026-09-12 knownGaps note claiming 'no Fact/engine vocabulary' for Two-Headed Coin is out of date (some later pass already added the hand-authored `winCoinFlip` source fact + built the engine mechanism, but never added a recognizer to formalize it). **Recognizer-lane escalation (winCoinFlip)**: no `functional-model/recognizers/*.ts` file reads `keywords.includes('TwoHeadedCoin')` today (grepped the whole directory — confirmed absent). `lifegainDoubleKeyword-structural.ts` is the exact right precedent to mirror (same family: single real card, `CardDefinition.keywords` field read directly, one fixed literal English clause required verbatim) — real clause: \"Two-Headed Coin — The first time you flip one or more coins each turn, those coins come up heads and you win those flips.\" (oracle line 1); the existing hand-authored fact only annotates the short \"you win those flips\" tail (offset 101-120), leaving `verify-text-coverage.mjs` reporting the rest of the sentence (0-101) as a real, uncovered gap — a new recognizer should decide for itself how much of the clause to anchor (probably the same 'you win those flips' tail, or the whole sentence — either closes the coverage gap, whichever the recognizer author prefers) but MUST gate on the `TwoHeadedCoin` keyword being present, not just the literal text, same as its precedent. Only 1 real pool card (this one) needs it today, same acceptable single-card bar `lifegainDoubleKeyword-structural.ts` itself already sets. (2) **drawCard source + its paired sink (artifact-count scaling)**: migrated `definition.ts`'s own ETB effect off the old `kind:'drawCard', amount: (ctx) => ctx.you.getCardsIn('Battlefield').filter(c => c.isArtifact()).length` (an opaque `Computed<number>` closure, permanently unrecognizable) onto the combinator DSL — `kind:'program'`, `program: drawCard(you.permanentsInPlay().filter('cardType','artifact').count())` — same real live board-state read, now genuine structured data instead of a closure (`combinator.ts`'s own `Query`/`Filter`/`Aggregate`/`DrawCard` vocabulary, all pre-existing, no new primitive needed). Confirmed byte-for-byte same real behavior via `run-scenarios.mjs` (still resolves a real artifact count of 2 and draws 2 real cards, same trace shape). This migration does NOT by itself flip the fact's provenance — `drawCardProgram-effect-structural.ts` (built 2026-09-16 for Venat/Hydaelyn) only has a confirmed template for `amount === 1` behind a categorical subtype guard; it declines outright on any other `amount` shape, including this card's own new `Aggregate`-based count (confirmed by direct code read: `if (occ.amount !== 1) return declined`). **Recognizer-lane escalation (drawCard + sink)**: extend `drawCardProgram-effect-structural.ts` (or a sibling file) with a branch reading an `Aggregate{op:'count', input:Filter{predicate:{field:'cardType'}}}`-shaped `amount`, building a 'draw a card for each <type> you control' clause template, and emitting the PAIRED sink fact (`{to:'Battlefield', controller:'you', types:{has:[<Type>]}}`) the same way `ptFormula-scalingPump-structural.ts` already pairs a scaling SOURCE with a 'wants X present' SINK off one shared annotation span — this card is the sole real motivator today (checked: no other pool card's `program`-shaped `DrawCard` node carries a non-1 `Aggregate` amount), but the shape generalizes the same way `ptFormula-scalingPump-structural.ts`'s multi-branch design already does. Did NOT attempt an engine-core-level generalization (e.g. a shared `amountFormula`-style field mirroring `ptFormula` but for `drawCard`) — the combinator-DSL migration above already solves the SAME underlying problem (getting a live board-state count out of an opaque closure) without needing new `card.ts` vocabulary, so no further engine-core escalation is warranted for this specific gap; `summon-shiva`/`deadly-embrace` have the same closure-based `drawCard.amount` shape and would benefit from the identical migration + recognizer branch, not raised as a NEW escalation here (out of my assigned slugs this pass). `verify-synergy.mjs` (scoped): 0 hard failures, same 8 pre-existing soft notes (2 `enters` w/ no declared produce for the 2 setup artifacts + Edgar's own self-enter, 6 `tapForMana`) both before and after the migration. `npx tsc --noEmit`: clean. `review` stays \"ai\" (was already \"ai\", no reset needed). Prior 2026-09-12: migrated to the unified Fact/annotations model (SYNERGY_DESIGN.md's synergy.json v2 unified schema — same rework as summon-bahamut/adelbert-steiner/fate-of-the-sun-cryst). Real oracle text (Scryfall-confirmed, data/fin/fin_scryfall.json #51): \"When Edgar enters, draw a card for each artifact you control.\\nTwo-Headed Coin — The first time you flip one or more coins each turn, those coins come up heads and you win those flips.\" Legendary Creature — Human Artificer Noble, {4}{U}{U}, pt [4,5]. Dropped the old v1 `id`/`sourceText`/`highlight` fields, and — per the Adelbert Steiner precedent (this card's own oracle text has zero death/sacrifice/removal-related content, same as Steiner's) — REMOVED the old v1 self-graveyard/self-dies facts entirely: they were unfounded generic \"every creature can die\" boilerplate with no real textual basis, not something this card's own text actually supports. Real facts kept/added: `self-cast` ({event:'cast', from:'Hand', target:'self'}, typeLine-anchored 'Creature'), `self-enters` ({event:'entersBattlefield', to:'Battlefield', subject:'self', target:'self'}, typeLine-anchored 'Creature' — kept `subject:'self'` since the OLD v1 `self-battlefield`-style fact already carried it, same 'don't drop a subject the old fact already had' lesson summon-bahamut's/adelbert-steiner's own merges already established), and the real ETB `drawCard` ({event:'drawCard', controller:'you'}, oracle-anchored 'draw a card for each artifact you control'). The real per-artifact SCALING itself is unchanged, already-correct engine machinery (`definition.ts`'s own `amount: (ctx) => ctx.you.getCardsIn('Battlefield').filter(c => c.isArtifact()).length`, the `Computed<number>` escape hatch's canonical live-count-read use) — no new Fact vocabulary exists or was invented for 'this SOURCE effect's own magnitude scales with a live board-state count' (same open, deliberately-parked gap Adelbert Steiner's own CDA P/T scaling already documents in SYNERGY_DESIGN.md — checked, this is the exact same category, not a new one); `Fact.value` (deprioritized per standing 2026-09-11 decision) is a flat weight, not a scaling formula. SINK half: real `{to:'Battlefield', controller:'you', types:{has:['Artifact']}}` (Edgar *wanting* artifacts on the battlefield), oracle-anchored 'artifact you control' — same 'wants-X' pattern Adelbert Steiner's own wants-equipment sink already establishes. `Two-Headed Coin` (the coin-flip-win static): checked for existing real vocabulary first — `event:'coinFlip'` is real, live pool vocabulary (The Gold Saucer, fin, added 2026-09-09) but only for the FLIP ITSELF happening as a guaranteed, non-probabilistic occurrence; Edgar's own ability is a genuine CR-614-style REPLACEMENT effect on a random flip's OUTCOME, a structurally different and deeper claim no execution or Fact vocabulary exists for anywhere in this engine (confirmed: no `random`/`coin`/`Replacement` mechanism anywhere in card.ts/interfaces.ts/engine.ts/layers.ts). Left as real, structured `staticAbilities` text only, no Fact authored for it, same treatment fate-of-the-sun-cryst's cost-reduction static and the still-open Equipment P/T-bonus/type-grant clauses (gap #14) get. Documented as a new, dedicated ENGINE_GAPS.md gap #15 (not folded into an existing one — it's two gaps deep: no coin-flip resolution mechanism to replace, and no general replacement-effect engine to hook a replacement into). scenarios.ts migrated to the engine-trace pilot format (`runEngineScenarios`) to get REAL cast/entersBattlefield/drawCard trace evidence — the old plain `harness.ts` `Scenario[]` shape (`trigger:'onEnter'`) skips the cast/enters lifecycle entirely (see `lifecycleBefore`'s own `scenario.trigger` branch), so it could never back `self-cast`/`self-enters` with real evidence. Added `on:'enter'` to the card's own `onEnter` trigger (definition.ts) so `pilotResolveTop` auto-fires it for real (same convention Weapons Vendor/Cloud, Midgar Mercenary already established), instead of requiring a scenario to name it explicitly. ONE real scenario per the standing 'default 1, no edge-case coverage' rule (dropped the old v1 second `artifactsCount:0` scenario and the `keywordScenarios(...)` spread, which would otherwise have added an unrequested legend-rule edge case since Edgar is Legendary): casts Edgar for {4}{U}{U} with two real, differently-shaped FIN blue artifacts already on the battlefield (Astrologian's Planisphere, an Equipment; Ether, a plain Artifact — not reusing Dragoon's Lance/generic filler, real variety per the standing scenario-content rule) — resolving him auto-fires the real onEnter trigger, reading a real artifact count of 2 and drawing 2 real cards. `verify-synergy.mjs` (scoped): 0 hard failures, 6 expected soft notes (tapForMana ×6, same shape every other engine-piloted mana-paying scenario produces). `npx vitest run functional-model`: 238/238. Real `find-synergies.mjs` diff (isolated via an old/new file swap against the current, concurrently-changing pool — not a stale git-HEAD diff, since several sibling sessions have uncommitted migrations in flight): -24 lines lost (21 `graveyard presence` + 3 `dying`, all fully explained by removing the two unfounded death/graveyard facts above), the remaining 141 `battlefield presence` lines cleanly RELABELED (not lost/regained) to `enters the battlefield` (pure shape-family rename, same zone-shaped match set, same count both before and after — confirmed via a before/after per-label count, not just the raw line-count delta). Sink side (21 real artifact-producer -> Edgar edges) unchanged. `compute-annotations.mjs`/`compute-weights.mjs --slug=edgar-king-of-figaro` both run (4 facts annotated; all `-1` sentinels resolved to real `value:1`). Card added to `ANNOTATED_CARD_SLUGS` (scripts/annotation-coverage.mjs).",
  "textCoverageAudited": false,
  "knownGaps": [
    "CLOSED 2026-09-16 — both prior entries here (the missing winCoinFlip recognizer, and drawCardProgram-effect-structural's missing Aggregate-amount branch) are now real, closed recognizers — see this file's own 2026-09-16 notes entry above.",
    "summon-shiva/deadly-embrace have the same closure-based drawCard.amount CLOSURE shape (not yet migrated onto the combinator DSL) and would benefit from the identical migration + recognizer coverage once migrated — not this card's own remaining gap, flagged here only for cross-reference."
  ]
}
