{
  "enrichment": "ai",
  "review": "human",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-16",
  "notes": "User spot-checked FIN #2: the mana-doubling static (\"whenever you tap a land for {C}, add an additional {C}\") has no fact because the engine has no trigger hook for \"land tapped for mana\" at all — real ability, left as descriptive text in definition.ts (pre-existing, documented gap, not a synergy.json miss). Needs a new harness trigger type to model, out of scope for fact-authoring. 2026-09-11: migrated to the unified Fact/annotations model (same rework as summon-bahamut) — both facts' `sourceText`/`highlight`/`id` moved to new sibling `annotations-authoring.json`, real `annotations` baked in via compute-annotations.mjs (card added to `ANNOTATED_CARD_SLUGS`); source `putCounter` fact gained `targeted: true` (real \"target land\", genuine choice among candidates, no restriction on whose land); sink fact's `zone: 'Battlefield'` folded to `to: 'Battlefield'` per the unified shape. No facts removed or re-anchored — neither fact was presence-shaped, both already had real oracle-text backing. find-synergies.mjs diff confirmed byte-identical interaction set before/after (20 lines, same cards, same labels) — pure shape migration, zero matching-behavior change. 2026-09-11 (later): the mana-doubling gap above is now FIXED for real, not just documented — definition.ts gained a real, manually-fired `onTapLandForC` trigger (`kind:'addMana'`, same shape Elvish Archdruid's own mana ability uses); scenarios.ts pilots a real Adventurer's Inn tap for {C} (a real FIN card, throwaway scenario-local ability CardDefinition, that card's own definition.ts untouched) then fires the trigger; synergy.json gained 2 real addMana facts (SOURCE/SINK) plus a self-cast baseline fact (typeLine-anchored, no alternate-cost wrinkle on this card). See SYNERGY_DESIGN.md's own \"Real gap: Ultima, Origin of Oblivion's mana-doubling ability\" section for the full writeup, including one known/accepted imprecision (a self-match caused by `types` being inert on event-shaped facts, not fixed here — filed under future matcher-unification work). 2026-09-16: real user-reported bug fix — `putCounterTarget-effect-structural.ts` used to anchor BOTH the `putCounter` SOURCE fact and its paired \"wants a land present\" SINK fact to the SAME whole-clause span `[25,60)` (\"put a blight counter on target land\"). Split into disjoint spans: SOURCE `[25,48)` (\"put a blight counter on\"), SINK `[49,60)` (\"target land\") — confirmed via direct string slice against this card's own real Scryfall oracle text. `review` reset from `human` back to `ai` per project convention (authored content — the annotation span — changed). 2026-09-16 (later, same day): user follow-up correction — the disjoint SOURCE span above was itself wrong; user wants the SOURCE to cover the FULL clause \"put a blight counter on target land\" (`[25,60)`), since the target is part of the action itself, not a separate condition/multiplier — a real span overlap with the SINK (`[49,60)`, unchanged) is expected and fine. `putCounterTarget-effect-structural.ts`'s source/sink split revised accordingly (source now anchors to the whole regex match, not just its first capture group); `review` was already `ai` from the prior reset, no further reset needed.",
  "textCoverageAudited": false,
  "annotatedNonFactSpans": [
    {
      "target": "oracle",
      "line": 1,
      "start": 62,
      "end": 176,
      "kind": "definition-path",
      "note": "\"For as long as that land has a blight counter on it, it loses all land types and abilities and has '{T}: Add {C}.'\" — real, mechanically-enforced machinery with no Fact of its own. Maps to card.ts's CounterConditionalGrant / state.ts's hasCounterConditionalLandTypeLoss+hasCounterConditionalAbilityLoss (consumed by effectiveSubtypes/effectiveKeywords/engine.ts's canActivateAbility) and mana.ts's own duck-typed check for the granted {T}: Add {C}. See ENGINE_GAPS.md's \"Counter-conditional continuous effects\" entry and this file's own knownGaps entry below for the full writeup."
    }
  ],
  "knownGaps": [
    "2026-09-16 (engine-core, coordinator-flagged sync): this field was empty despite the exact gap already being fully documented elsewhere on this card (this file's own `notes` above, and ENGINE_GAPS.md's own \"Counter-conditional continuous effects\" writeup for Ultima) — a card-status classifier reading uncovered text as a coverage gap without this signal was misclassifying this card as a result. Backfilled, same content, not a new finding: \"For as long as that land has a blight counter on it, it loses all land types and abilities and has '{T}: Add {C}.'\" is real, mechanically-enforced (state.ts's `hasCounterConditionalLandTypeLoss`/`hasCounterConditionalAbilityLoss`, consumed by `effectiveSubtypes`/`effectiveKeywords`/`engine.ts`'s `canActivateAbility`; `mana.ts`'s own duck-typed check for the granted `{T}: Add {C}`) but deliberately carries NO Fact of its own — `installCounterConditionalGrant`'s own trace line is real engine bookkeeping (which object a continuous effect got installed onto), not a produce/consume-shaped board relation any Fact vocabulary models, same treatment `queueExtraPhase` already gets in `scripts/verify-synergy.mjs`'s `IGNORED_FNS`. Genuinely fact-less by design, not an authoring miss."
  ],
  "interactionsReview": "draft",
  "scenariosReview": "draft",
  "oracleTextSnapshot": "Flying\nWhenever Ultima attacks, put a blight counter on target land. For as long as that land has a blight counter on it, it loses all land types and abilities and has \"{T}: Add {C}.\"\nWhenever you tap a land for {C}, add an additional {C}.",
  "reviewedAt": "2026-09-16"
}
