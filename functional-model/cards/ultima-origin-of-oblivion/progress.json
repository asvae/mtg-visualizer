{
  "enrichment": "ai",
  "review": "human",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-11",
  "notes": "User spot-checked FIN #2: the mana-doubling static (\"whenever you tap a land for {C}, add an additional {C}\") has no fact because the engine has no trigger hook for \"land tapped for mana\" at all — real ability, left as descriptive text in definition.ts (pre-existing, documented gap, not a synergy.json miss). Needs a new harness trigger type to model, out of scope for fact-authoring. 2026-09-11: migrated to the unified Fact/annotations model (same rework as summon-bahamut) — both facts' `sourceText`/`highlight`/`id` moved to new sibling `annotations-authoring.json`, real `annotations` baked in via compute-annotations.mjs (card added to `ANNOTATED_CARD_SLUGS`); source `putCounter` fact gained `targeted: true` (real \"target land\", genuine choice among candidates, no restriction on whose land); sink fact's `zone: 'Battlefield'` folded to `to: 'Battlefield'` per the unified shape. No facts removed or re-anchored — neither fact was presence-shaped, both already had real oracle-text backing. find-synergies.mjs diff confirmed byte-identical interaction set before/after (20 lines, same cards, same labels) — pure shape migration, zero matching-behavior change. 2026-09-11 (later): the mana-doubling gap above is now FIXED for real, not just documented — definition.ts gained a real, manually-fired `onTapLandForC` trigger (`kind:'addMana'`, same shape Elvish Archdruid's own mana ability uses); scenarios.ts pilots a real Adventurer's Inn tap for {C} (a real FIN card, throwaway scenario-local ability CardDefinition, that card's own definition.ts untouched) then fires the trigger; synergy.json gained 2 real addMana facts (SOURCE/SINK) plus a self-cast baseline fact (typeLine-anchored, no alternate-cost wrinkle on this card). See SYNERGY_DESIGN.md's own \"Real gap: Ultima, Origin of Oblivion's mana-doubling ability\" section for the full writeup, including one known/accepted imprecision (a self-match caused by `types` being inert on event-shaped facts, not fixed here — filed under future matcher-unification work).",
  "textCoverageAudited": false,
  "knownGaps": [],
  "interactionsReview": "draft",
  "scenariosReview": "draft",
  "oracleTextSnapshot": "Flying\nWhenever Ultima attacks, put a blight counter on target land. For as long as that land has a blight counter on it, it loses all land types and abilities and has \"{T}: Add {C}.\"\nWhenever you tap a land for {C}, add an additional {C}.",
  "reviewedAt": "2026-09-11"
}
