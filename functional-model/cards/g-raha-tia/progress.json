{
  "enrichment": "ai",
  "review": "human",
  "verifySynergy": "notes",
  "lastVerified": "2026-09-12",
  "notes": "Migrated to the unified Fact model (SYNERGY_DESIGN.md). Real oracle: 'Reach\\nThe Allagan Eye — Whenever one or more other creatures and/or artifacts you control die, draw a card. This ability triggers only once each turn.' Reach is a bare printed keyword with no grant/broadcast involved — no Fact per this session's standing rule. No self-dies producer fact (this card has no death trigger of its own; its trigger cares about OTHER permanents dying, not itself — never invented from the type line, matching gaelicat's own precedent of omitting a baseline self-dies fact once annotations became required). Sink is EVENT-shaped ({event:'dies', controller:'you', target:{types:{hasAny:['Creature','Artifact']}}, oncePerTurn:true}) — mirrors al-bhed-salvagers/jenova-ancient-calamity/judge-magister-gabranth's own precedent exactly; checked factsInteract directly and confirmed a zone-shaped ({to:'Graveyard'}) alternative would fail verify-synergy.mjs's trace check (no aggregate Graveyard read in this card's own trace) while the event-shaped form passes cleanly via TRIGGER_EVENT_MAP.onOtherPermanentsDie === 'dies'. Cross-card find-synergies.mjs matching for this sink is currently 0 (real, documented pool-wide gap: no bare event-shaped 'dies' producer fact anywhere in the pool sets `subject`, which the Constraints-target branch of factsInteract requires to resolve a candidate's real types — same zero-match state as its 3 sibling cards above, not unique to this card, not fixed here). Source drawCard fact + sink dies-want both carry oncePerTurn:true (documentary only, same as elrond-moon-reader's own precedent) for the real 'This ability triggers only once each turn' cap — no per-turn trigger-count enforcement exists anywhere in this engine (see ENGINE_GAPS.md), so a scenario just exercises one firing. scenarios.ts rewritten from a bare {trigger:'onOtherPermanentsDie'} flat scenario (which produced zero cast/enters trace evidence, since harness.ts's own lifecycleBefore/After skip the lifecycle entirely for a trigger-shaped scenario) to a real engine-piloted playthrough (cast -> enters -> a real SEPARATE creature you control, Town Greeter, dies in real lethal blocked combat against a real Coeurl -> onOtherPermanentsDie fires manually -> real drawCard), giving real trace backing for self-cast/self-enters/drawCard/dies-want alike.",
  "textCoverageAudited": true,
  "knownGaps": [
    "No per-turn trigger-count tracking anywhere in this engine (turn.ts tracks phases/steps, not per-ability firing counts) — 'This ability triggers only once each turn' is real, honest documentary data (Fact.oncePerTurn:true on both the sink and the source), not an enforced cap. Same class of gap as elrond-moon-reader's own identical clause.",
    "Sink's event-shaped {event:'dies', target:{types:{hasAny:[...]}}} currently matches 0 real producers pool-wide — factsInteract's Constraints-target branch on an event-shaped want requires the PRODUCER to declare a `subject`, and no bare event-only 'dies' producer fact anywhere in the pool sets one (checked directly, not assumed) — shared, pre-existing gap with al-bhed-salvagers/jenova-ancient-calamity/judge-magister-gabranth, not something this card's own migration should fix in isolation; a future full matcher-unification pass (already tracked in SYNERGY_DESIGN.md) is the right place."
  ],
  "oracleTextSnapshot": "Reach\nThe Allagan Eye — Whenever one or more other creatures and/or artifacts you control die, draw a card. This ability triggers only once each turn.",
  "reviewedAt": "2026-09-12"
}
