{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-16",
  "notes": "2026-09-16 (fin/20-47 text-coverage pass): closed all 3 real oracle-text gaps `verify-text-coverage.mjs` flagged (58% covered). Widened `moveConditionalDestinationByCastFrom-effect-structural.ts`'s own two facts (the only real user of this recognizer) from bare \"put it/that card <phrase>\" tails to the WHOLE clause each — Library->Hand fact now spans \"Search your library for a Vehicle card, reveal it, and put it into your hand\" (the search-clause span was already required to match but previously discarded, never backing a Fact); Library->Battlefield fact now spans \"...cast from a graveyard, put that card onto the battlefield instead\" (added an optional trailing \" instead\" to the match). Also widened `flashback-alternateCost-structural.ts`'s own cast fact to start at the printed \"Flashback <cost>\" heading (this card is the one real user of that recognizer's 14 whose heading, {4}{W}{W}{W}, is long enough to clear the 20-char gap threshold — checked the other 13 directly, none flagged). `apply-recognizers.mjs` scoped to the 14 real flashback-cost cards + this one (moveConditionalDestinationByCastFrom has only this 1 real user) retagged/re-widened facts with provenance, 0 new facts, 0 hard failures pool-wide after. `verify-text-coverage.mjs --threshold=1.0`: 100% covered, 0 gaps — no `annotatedNonFactSpans` entry needed (the remaining \"Then shuffle.\" is 13 real chars, under the 20-char noise threshold on its own). Resets `review` to 'ai' per standing rule (synergy.json's own real annotation spans changed as this card's own direct target, not merely collateral).\n\n**Real duplicate-fact bug, caught by user report mid-pass and fixed here**: this card's synergy.json still carried 3 STALE `types:{has:['Artifact']}` facts (2 source + 1 sink) left over from BEFORE the 2026-09-15 pass taught `moveConditionalDestinationByCastFrom-effect-structural.ts` to prefer `effect.subtype`'s literal word ('Vehicle') over `validType`'s generic one ('Artifact') — that fix changed what the recognizer PRODUCES going forward, but `apply-recognizers.mjs`'s own additive-only design never retracts a stale prior-version fact once a recognizer's real output narrows, so the old Artifact-typed trio sat alongside the new Vehicle-typed ones indefinitely (this card's own file hadn't been regenerated since, until this pass). No real printed \"artifact card\" phrase exists anywhere in the oracle text to back them (Vehicle ⊂ Artifact is Magic's own type hierarchy, not a second independent restriction — same convention `validType`'s own `card.ts` doc comment already establishes: it narrows WITHIN whatever `subtype` allows, never adds a second clause). Confirmed real, visible impact via `find-synergies.mjs` before/after: every one of this card's own edges (both `tutor`-named and the generic 'moves to battlefield (from library)' ones) was being emitted TWICE, once per overlapping fact; after deleting the 3 stale Artifact facts, each real edge renders exactly once, and the underlying edge SET is byte-identical (`diff` of the full before/after `From Father to Son` line set: 0 diff) — a pure de-duplication, no real synergy lost or gained. Whole-pool check for the same pattern (same `from`/`to`/`face`, overlapping annotation span, differing `types`) before fixing narrowly: only this card matches; `cloud-midgar-mercenary` (the other real card with both `validType` and `subtype` set on the same `move` effect) was already clean, its own synergy.json regenerated fully during the immediately-prior sibling pass. `apply-recognizers.mjs` re-run afterward confirms idempotence (0 facts re-added). `verify-text-coverage.mjs --threshold=1.0` still 100%/0 gaps (the deleted facts weren't backing any real coverage — their spans were a subset of the Vehicle ones' already-wider spans). `verify-synergy.mjs` scoped: 0 hard failures (only pre-existing soft `tapForMana`/`shuffleLibrary` unrecognized-action notes).\n\n2026-09-12: consolidated the 2 separate engine-piloted scenarios (a plain hand-cast, a separate standalone Flashback-cast) into ONE continuous playthrough, per user's own live call ('use only one scenario: first cast from hand - fetch one artifact, then cast from graveyard - place on the battlefield'). Same real `cardReal` instance is cast twice in sequence against the same shared GameState — hand-cast ({1}{W}) -> real search finds Magitek Armor -> hand -> real move to Graveyard -> Flashback-cast ({4}{W}{W}{W}) from that same Graveyard -> real search finds a SECOND real, distinct FIN Vehicle (Cargo Ship — the first one already left the library for real, so reusing it for the second search would be dishonest) -> battlefield -> real exile. Enough real starting lands provided for BOTH costs combined ({5}{W}{W}{W}{W}) so no turn passage/untap step was needed between the two casts (real, honest choice, not a requirement — no MTG rule needs a turn to pass here). Confirmed in the regenerated trace.json: `cast`(hand)->`moveTo`(hand)->`move`(Graveyard)->`cast`(graveyard, Flashback cost)->`moveTo`(Battlefield)->`move`(Exile), in that real order, both real target names distinct. Every existing fact still gets real trace evidence in this one scenario (verify-synergy.mjs scoped: 0 hard failures; full pool 314: 0 hard failures). vitest 238/238. tsc --noEmit baseline unchanged (45). Resets `review` to 'ai' per standing rule (authored scenarios.ts content changed).\n\n2026-09-11, later: fixed a real correctness bug — all 3 facts (Library->Hand tutor, Library->Battlefield flashback-tutor, Library-presence sink) used `types:{has:['Artifact']}`, but the real oracle text searches for a Vehicle card specifically (\"Search your library for a Vehicle card\"), a real, distinct type-line subtype (Scryfall: 'Artifact — Vehicle'), not a synonym for generic Artifact. Narrowed all 3 to `types:{has:['Vehicle']}`. find-synergies.mjs before/after (isolated swap): 0 diff — the overbroad Artifact constraint had already produced 0 real matches either direction (checked), so this narrowing didn't shrink any real, previously-counted match; it just makes the constraint factually correct going forward instead of silently overbroad. verify-synergy.mjs/vitest: 0 hard failures / 238/238.\n\nEarlier: Migrated to the unified v2 Fact model (to/from, no id, required annotations). Real dual-mode tutor: Hand-cast -> Library->Hand (named 'tutor'), Flashback-cast (from Graveyard) -> Library->Battlefield instead (no ZONE_MOVEMENT_NAMES precedent yet, renders via the generic 'moves to X (from Y)' fallback). Baseline self-cast(Hand)+self-to-Graveyard and self-cast(Graveyard, Flashback)+self-to-Exile facts, mirroring auron-s-inspiration's own real alternate-cost pattern exactly. scenarios.ts rewritten to a real engine-piloted (runEngineScenarios) pair; fixed two real bugs surfaced along the way: engine-trace.ts's ctxFor hardcoded castFrom:'hand' with no override (added EnginePilotCtxOpts.castFrom), and the original libraryArtifactCount-based library setup put the mocked Vehicle at library[0] where the real turn-1 Draw step (single-player pilot setup, not a real 'two-player game' under shouldSkipDraw) silently drew it away before the search ever ran — fixed by appending a real named Vehicle (Magitek Armor) after plain filler instead, same technique ashe-princess-of-dalmasca's own scenario already uses for a different reason.",
  "textCoverageAudited": true,
  "knownGaps": [],
  "oracleTextSnapshot": "Search your library for a Vehicle card, reveal it, and put it into your hand. If this spell was cast from a graveyard, put that card onto the battlefield instead. Then shuffle.\nFlashback {4}{W}{W}{W} (You may cast this card from your graveyard for its flashback cost. Then exile it.)",
  "reviewedAt": "2026-09-12",
  "scenariosReview": "reviewed"
}
