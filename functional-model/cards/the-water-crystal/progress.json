{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-12",
  "notes": "Migrated to unified Fact/annotations model (sibling of the-wind-crystal, same 3-clause shape). Baseline self-cast(Hand)/self-enters + self-tap cost facts for the {4}{U}{U},{T} activated ability. Cost reduction ('Blue spells you cast cost {1} less to cast') is real via spellCostReductionGrants (ENGINE_GAPS.md gap #7's second example, same mechanism The Wind Crystal's own White-spell discount uses) — event:'costReduction' fact authored, exempted from trace-evidence checking (same class as The Wind Crystal's own exemption: demonstrating it needs a DIFFERENT spell's own cast cost to differ, which this card's own scenario doesn't produce). The base mill amount ('{4}{U}{U},{T}: Each opponent mills cards equal to the number of cards in your hand') is real (now modeled as a genuine `kind:'mill'` Effect through `state.mill`, not the generic `move` this file used before — ENGINE_GAPS.md gap #19, closed 2026-09-12), scaling with real hand size — event:'mill' fact with genuine trace evidence (from:'Library'/to:'Graveyard'/controller:'opp'), plus a sink wanting cards in your own hand (the scaling driver). The mill-PLUS-FOUR replacement ('If an opponent would mill one or more cards, they mill that many cards plus four instead.') is ALSO now real (ENGINE_GAPS.md gap #19, closed) — a new `state.ts` `GameState.mill` chokepoint (real per-card library->graveyard moves, mirroring `Player.mill`, forge-game/.../player/Player.java ~line 1539) checks a new, general `CardDefinition.millModifierGrants`/`MillModifierGrant` structured field (`{amount: number}`, a generic 'add N' shape — not a fixed-multiplier keyword like gap #8b's `LifegainDouble`, since this replacement's own delta is card-specific data, not a fixed 2x) before finalizing the real mill count; `state.ts`'s `activeMillModifier` sums the grant across every OTHER player's battlefield (real `ValidPlayer$ Player.Opponent` scoping). This card's own `definition.ts` now declares `millModifierGrants: [{amount: 4}]` (replacing the old documentary-only `staticAbilities` text) and its activated ability uses the new `kind:'mill'` Effect. `scenarios.ts` was fully migrated to a single real `runEngineScenarios` pilot (the two old flat scenarios never put this permanent through `resolveTop`, so `millModifierGrants` could never apply to them — dropped rather than left as dead code once an engine-piloted trace existed) that casts this card for real, lets it resolve (copying the grant), then activates its own mill ability with 3 real cards in hand — the trace shows the real, mechanically-computed `qty:7, requestedQty:3` (3+4), not a scripted number. A new `event:'millIncrease'` fact is authored (mirroring gap #8b's own `lifegainDouble` sibling fact), backed by genuine trace evidence via `scripts/verify-synergy.mjs`'s new `case 'mill'` producedEvents branch (emits `millIncrease` when the real applied qty exceeds the requested one). find-synergies.mjs diff (before/after, isolated to this card): -17 real matches lost (the OLD pre-migration file's own bare, unconditioned {zone:'Graveyard', controller:'you', subject:'self'} SOURCE fact was a presence claim, not a real zone TRANSITION this card's own ability causes — correctly dropped per the standing 'no presence in sources' rule, same class of correction Bahamut's own migration already established, not a regression to fix), +38 gained (self-enters' real to:'Battlefield' fact, newly authored this pass, backing real 'enters the battlefield' matches it had zero capability for before).",
  "textCoverageAudited": false,
  "knownGaps": []
}
