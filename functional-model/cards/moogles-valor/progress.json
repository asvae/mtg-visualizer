{
  "enrichment": "ai",
  "review": "regression",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-16",
  "notes": "2026-09-17 (retroactive correction, `re-review`-bucket rollout): this card's `review` had already been auto-reset to `'ai'` by an earlier run of `scripts/check-verified-regressions.mjs`'s OLD (pre-2026-09-17) behavior, on a real, confirmed mismatch against its own `verified-snapshot.json` (a real, once-human-confirmed baseline still on disk) — the recorded sink fact's own annotation span narrowed since that snapshot was captured, per that script's own live diff. Corrected here to `'regression'`, the value that same auto-detection now writes going forward, since this card genuinely matches that definition (a real prior human confirm + a real detected drift since) and was only ever left at plain `'ai'` as a stale artifact of the OLD reset behavior, not because it was never reviewed.\n\n2026-09-16 (fin/26-50 re-triage): re-verified fresh. 1 AI fact remains (Moogle token creation, non-literal \"for each creature you control\" amount) — already a confirmed, documented decline in token-creation-structural.ts's own module comment (this card named explicitly as one of 3 real non-literal-amount declines). Genuinely bespoke, no action needed. 0 hard failures.\n\nMigrated to the unified Fact model (SYNERGY_DESIGN.md). New real token registered (TOKENS.w_1_2_moogle_lifelink, tfin/34, verified against data/cards.db) with a real, structurally-tracked keywords:['Lifelink'] — the old 'TokenInfo has no keywords field' gap claim was stale/wrong, checked and closed. Indestructible grant now uses the real grantKeywordAll effect (Ardyn/Circle of Power precedent), mechanically enforced and producing real fn:'grantKeyword' trace evidence (6 real calls in the 3-creature scenario: 3 pre-existing Grizzly Bears + 3 new Moogle tokens, confirming the card's own 'then' sequencing).",
  "textCoverageAudited": false,
  "knownGaps": [],
  "oracleTextSnapshot": "For each creature you control, create a 1/2 white Moogle creature token with lifelink. Then creatures you control gain indestructible until end of turn.",
  "reviewedAt": "2026-09-12"
}
