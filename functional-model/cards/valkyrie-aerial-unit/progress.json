{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-12",
  "notes": "2026-09-12: migrated to the unified Fact/annotations model (fin/84). Real oracle text (Scryfall-confirmed, collector_number 84, mana_cost {5}{U}{U}, Artifact Creature — Construct, 5/4): 'Affinity for artifacts (This spell costs {1} less to cast for each artifact you control.) / Flying / When this creature enters, surveil 2.' 3 SOURCE facts, 0 SINK, all annotated (card added to ANNOTATED_CARD_SLUGS in scripts/annotation-coverage.mjs, baked via compute-annotations.mjs; weights via compute-weights.mjs --slug=valkyrie-aerial-unit).\n\nBaseline self-cast ({event:'cast', from:'Hand', target:'self'}) / self-enters ({event:'entersBattlefield', to:'Battlefield', controller:'you', subject:'self', target:'self'}), both typeLine-anchored on 'Creature' (identical typeLine and identical anchor indices [9,17] as Rook Turret's own baseline facts — same real type line 'Artifact Creature — Construct'). Real ETB surveil: {event:'surveil', controller:'you'}, oracle-anchored on 'surveil 2' (existing event:'surveil' vocabulary, reused verbatim — same shape as dreams-of-laguna's own surveil fact).\n\nBare printed Flying (keywords:['Flying']) gets no fact, per the standing 2026-09-12 rule (mechanically inert in this engine, nothing in the vocabulary constrains on it).\n\nAffinity for artifacts: kept as documentary-only `staticAbilities` text, NOT a structured field, NOT a Fact of either role. Same real mechanism question as Travel the Overworld's own 'Affinity for Towns' (fin/82, same batch) — checked that sibling card's own current state before deciding: still unmigrated, same documentary-only staticAbilities treatment, no Fact. Independently confirmed against ENGINE_GAPS.md gap #7's own current, explicit text: the CAST-side board-state-COUNTED discount shape ('costs {1} less... for each X you control') is explicitly NOT modeled — CostReduction.amount is a plain fixed number (fate-of-the-sun-cryst's target-conditional {2}), not a Computed-style board-counted hook; only the ACTIVATION-side shape (qiqirn-merchant's ActivationCostReduction) got a board-counted variable amount, an intentional, checked asymmetry the gap doc itself calls out ('no real cast-side FIN card needs a variable cast discount today' — Travel the Overworld/Valkyrie are exactly the two real FIN cards that would, and neither is closed). Also checked: even qiqirn-merchant's OWN closed, engine-executed board-counted ACTIVATION discount got no 'wants Towns you control' SINK fact for the theme signal alone — so Valkyrie's still-open CAST-side case getting no SINK fact either is consistent, not an inconsistent asymmetry. No new Fact vocabulary, no engine change; this is a fact-model/vocabulary migration only.\n\nScenario consolidated to 1 (dropped the old top-level `trigger: 'onEnter'` shortcut, which gave zero cast/enters evidence, for `sequence: ['onEnter']` after a real cast->resolve->enters lifecycle, same rook-turret/cloudbound-moogle/dwarven-castle-guard consolidation) plus `keywordScenarios(valkyrieAerialUnit)` (empty today — Flying is inert, card isn't Legendary — kept for the template call anyway, same convention rook-turret/il-mheg-pixie already follow). trace.json regenerated via run-scenarios.mjs --slug=valkyrie-aerial-unit.\n\nReal find-synergies.mjs diff: BEFORE 0 lines (synergy.json was empty pre-migration — zero declared facts of any kind). AFTER 151 lines: 150 new 'enters the battlefield' matches via the new self-enters baseline fact (the pool's ~150 unconstrained Battlefield-presence sinks, same generic gain every other newly-migrated card's own baseline self-enters fact produces), plus 1 new 'surveil' match (Matoya, Archon Elder's own onSurveil-consuming want). Zero lost (there was nothing to lose). verify-synergy.mjs scoped (valkyrie-aerial-unit): 0 hard failures. Full pool (319 checked, 3 skipped v1-shaped): 1 pre-existing hard failure (sahagin), unrelated to this card, mid-edit by a concurrent session per git status. npx vitest run functional-model: 349/349 passed.",
  "textCoverageAudited": false,
  "knownGaps": [
    "Affinity for artifacts ('costs {1} less to cast for each artifact you control') is real, un-executed documentary text — ENGINE_GAPS.md gap #7's CAST-side board-state-counted CostReduction.amount shape is explicitly not modeled (only the ACTIVATION-side shape, qiqirn-merchant's ActivationCostReduction, supports a board-counted variable amount today). Kept as staticAbilities free text, same treatment Travel the Overworld's own 'Affinity for Towns' gets, and no Fact is authored for it (same as qiqirn-merchant's own closed, executed board-counted ACTIVATION discount also getting no theme-signal SINK fact)."
  ],
  "oracleTextSnapshot": "Affinity for artifacts (This spell costs {1} less to cast for each artifact you control.)\nFlying\nWhen this creature enters, surveil 2. (Look at the top two cards of your library, then put any number of them into your graveyard and the rest on top of your library in any order.)",
  "reviewedAt": "2026-09-12"
}
