{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-16",
  "notes": "2026-09-16 (AI-fact-elimination sweep, fin/44, re-examined fact-by-fact): re-ran `apply-recognizers.mjs you-re-not-alone` (0 new facts, confirms still uncovered) and `verify-synergy.mjs`/`verify-text-coverage.mjs` (both clean, no hard failures). All 3 remaining facts (source `pump` targeted; sink 'wants a target creature'; sink 'wants 3+ creatures you control', the magnitude condition) genuinely need a NEW recognizer, not just a rerun \u2014 checked `program-ast-walker.ts` directly: its own `actionOccurrence()` only builds a `ProgramOccurrence` for `'destroy'`/`'equip'` `EachAction`s today; `'pump'`/`'dealDamage'` are explicitly named in that file's own header as \"walked structurally but has no occurrence shape built for it yet.\" This card's own program (`selectUpTo(anyPlayer.creaturesInPlay(), 1, 'target', [branch(compare(you.creaturesInPlay().count(),'>=',3), [applyToBound('target',0,pumpEach(4,4,true))], [applyToBound('target',0,pumpEach(2,2,true))])])`) would need: (a) a `'pump'` case in `actionOccurrence`/a new `PumpOccurrence` type (parallel to `EquipOccurrence`), (b) the walker to also read the `Branch`+`compare` wrapping (today's walker only recurses into `branch.then`/`branch.else`, which it already does structurally \u2014 the missing piece is deriving a `{to:'Battlefield', controller:'you', types:{has:['Creature']}, amount:{min:3}}`-shaped SINK fact from the `compare(you.creaturesInPlay().count(), '>=', 3)` condition itself, which nothing in this walker attempts today), and (c) a new consuming recognizer file (parallel to `equipProgram-effect-structural.ts`) mapping the derived occurrence onto this card's real English template (\"target creature gets +2/+2 until end of turn. If you control three or more creatures, it gets +4/+4 until end of turn instead\"). **Real, confirmed 2nd pool sibling for the base pump/damage-occurrence half** (not a 1-card ask): `slash-of-light` (`dealDamageEach` via the identical `selectUpTo(anyPlayer.creaturesInPlay(), 1, 'target', [applyToBound('target', 0, dealDamageEach(...))])` shape, no `Branch` wrapper \u2014 its own magnitude is an `AddValue` sum instead) is ALSO still fully unprovenanced (3/3 facts) and would close together with this card if the walker gained `'pump'`/`'dealDamage'` occurrence support \u2014 cite both together, don't build per-card. Escalating to the recognizer lane rather than attempting this myself (out of this lane's write-fence \u2014 `program-ast-walker.ts`/`recognizers/*` are recognizer-lane files). No change made to this card's own definition.ts/synergy.json (already correctly modeled and migrated to `program`, per the entry below); `review` stays 'ai' (unchanged, no content change made this pass).\n\n2026-09-16 (definition-lane migration, per-card dispatch): the gap noted immediately below (no combinator 'pump' action) is now CLOSED \u2014 engine-core built `EachAction` 'pump' specifically for this card's own real threshold. MIGRATED off `kind:'pumpTarget'` + raw `Computed<number>` closures onto `kind:'program'`: `selectUpTo(anyPlayer.creaturesInPlay(), 1, 'target', [branch(compare(you.creaturesInPlay().count(), '>=', 3), [applyToBound('target', 0, pumpEach(4,4,true))], [applyToBound('target', 0, pumpEach(2,2,true))])])` \u2014 no new combinator vocabulary needed for this shape (per engine-core's own pre-check), just `Branch`+`compare` dispatching between two `pumpEach` calls. Re-confirmed against Forge's own script (`SVar:X:Count$Compare Y GE3.4.2`, a real threshold dispatch, not a smooth formula) and XMage's `YoureNotAlone.java` (`ConditionalOneShotEffect` between two fixed `BoostTargetEffect`s) before migrating. REAL BUG FOUND AND FIXED as a byproduct: the prior `pumpTarget` effect never set `untilEndOfTurn` at all (defaulting to a PERMANENT-within-scenario pump per that field's own doc comment), despite the real oracle text saying \"until end of turn\" twice and Forge's own `SP$ Pump` defaulting to that same duration \u2014 the new `pumpEach(_, _, true)` calls correctly set `untilEndOfTurn: true` on both branches, a real correctness fix, not just a reauthoring. `run-scenarios.mjs`/`apply-recognizers.mjs`/`verify-synergy.mjs` (scoped): 0 hard failures. Migrating to `program` does NOT itself flip the remaining AI facts to parser-derived (no generic program-AST recognizer exists for this shape yet) \u2014 expected.\n\n2026-09-16 (fin/26-50 re-triage): re-verified fresh, 0 hard failures. 3 AI facts remain \u2014 same magnitude-closure gap class as slash-of-light (`pumpTarget`'s power/toughness fields are raw board-state-threshold closures; no combinator `EachAction` for 'pump' exists to make this declarative). See slash-of-light's own progress.json note for the full reasoning; flagged once to the orchestrator rather than duplicated per-card.\n\nMigrated to the unified Fact/annotations model (fin/44, continuation of the fin/1-40 rollout). Real oracle text confirmed against data/fin/fin_scryfall.json (collector_number 44): \"Target creature gets +2/+2 until end of turn. If you control three or more creatures, it gets +4/+4 until end of turn instead.\" {W} Instant. This is ONE real condition-gated pump effect (a single targeted pump whose magnitude varies with board state), not a branching modal \u2014 same class as every other condition-scaled effect this session (Adelbert Steiner's own CDA pump), so it stays at 1 fact-set and 1 scenario, not 2. definition.ts's own pumpTarget effect (power/toughness Computed<number> reading ctx.you.getCreaturesInPlay().length >= 3) was already correct pre-migration; only synergy.json/scenarios.ts/progress.json changed here. Facts (5 total, all annotated, no id/zone/sourceText/highlight on the served object): SOURCE self-cast ({event:'cast', from:'Hand', target:'self'}, typeLine-anchored) + self-graveyard ({to:'Graveyard', controller:'you', subject:'self'}, typeLine-anchored) mirror fate-of-the-sun-cryst's own Instant self-cast/self-graveyard pair (no presence-only source facts); real event:'pump' SOURCE ({target:{types:{has:['Creature']}}, targeted:true}, oracle-anchored on 'gets +2/+2') is a genuine CR 601.2c targeted choice, same shape Battle Menu's own Ability-mode pump uses. SINK pump-target ({to:'Battlefield', types:{has:['Creature']}}, no controller \u2014 'target creature' has no printed controller restriction, same shape Battle Menu's own sink) wants a creature to target; SINK three-plus-creatures ({to:'Battlefield', controller:'you', types:{has:['Creature']}, amount:{min:3}}, oracle-anchored on 'you control three or more creatures') is the condition itself, same amount.min paired-sink convention gaelicat/magitek-infantry already establish. Scenarios trimmed from 2 to 1 per the 2026-09-12 'default 1, demonstrate real basic function' rule \u2014 kept the 3+ creatures / +4/+4 board state (the more complete real number) rather than the baseline +2/+2 case; real trace evidence shows fn:'pump' power:4 toughness:4 against a real filler target (Grizzly Bears). value: authored -1 on every fact then resolved via compute-weights.mjs --slug=you-re-not-alone (self-cast/self-graveyard/pump-source/pump-target-sink -> 1, the neutral floor since compute-weights.mjs has no event:'pump' magnitude case yet, same as every other pool pump fact today; three-plus-creatures sink -> 5, amount.min:3 bucketed). Did NOT add a Fact.untilEndOfTurn field \u2014 the task brief that started this migration referenced one as 'added earlier today,' but SYNERGY_DESIGN.md (re-read fresh) and synergy.ts both show no such field exists, and the design doc explicitly states event:'pump' is deliberately generic with 'no attempt to encode amount/duration/permanence as sub-fields' (2026-09-11 entry, unsuperseded) \u2014 followed the actually-current, documented convention (bare event:'pump', no duration field) instead, matching every other migrated pump fact (Adelbert Steiner, Ambrosia Whiteheart, Battle Menu, Auron's Inspiration, Gaelicat). Flagged back to the orchestrator; not silently overridden. verify-synergy.mjs (scoped + full pool): 0 hard failures for this card (one pre-existing FAIL on white-auracite, unrelated, hit a live race with a concurrent session mid-edit on white-mage-s-staff/ANNOTATED_CARD_SLUGS \u2014 not this card's issue). vitest run functional-model: 238/238 pass. find-synergies.mjs isolated before/after diff (old v1-ish file vs new, whole-pool run, filtered to this card's own lines): 227 total lines both before and after (0 gained/lost) \u2014 the only change is a real label fix on the 13 real graveyard-presence matches (Cantankerous Keepers, Eden Seat of the Sanctum, Elixir, Emet-Selch Unsundered, Ignis Scientia, Magic Pot, Qutrub Forayer, Rydia's Return, Sorceress's Schemes, Summon: Esper Ramuh, The Emperor of Palamecia, Thranduil Sindarin Liege, Vanille Cheerful l'Cie), which now render 'moves to graveyard' instead of the old, incorrect 'graveyard presence' (the self-graveyard fact moved from a bare zone-presence shape to a real to:'Graveyard' movement per the source-facts-are-movements-not-presence standing rule) \u2014 same bug class SYNERGY_DESIGN.md already documents fixing for Ambrosia Whiteheart/Cloud/Ashe. 2026-09-16 (recognizer-lane program-AST occurrence-support triage): program-ast-walker.ts gained real 'pump'/'dealDamage' EachAction occurrence support (previously only 'destroy'/'equip'), plus a Branch/compare-derived magnitude-guard reader (readGuardCondition) for this card's own real threshold dispatch. New pumpProgram-effect-structural.ts (this card's own ONLY real user) provenances all 3 of this card's pre-existing hand-authored facts: the source pump fact (merged from 2 raw occurrences -- base +2/+2 and guarded +4/+4 -- since this pool's own Fact schema has no magnitude field, both collapse into one real event:'pump' claim with 2 annotation spans), the bare 'wants a creature present' sink, and the 'wants 3+ creatures you control' magnitude sink. Hand-patched untilEndOfTurn:true onto the source fact (apply-recognizers.mjs's own additive-only retag path only updates annotations/provenance on a coreKey match, never other fields -- same class of gap the pumpTarget-effect-structural.ts propagation fix this same session already documents). Also required widening apply-recognizers.mjs's own shared coreKey with 'amount' (see that script's own updated doc comment) -- this card's own bare target-creature sink and 3+-creatures magnitude sink previously reduced to the identical coreKey, which would have made the runner's own same-recognizer-output merge wrongly collapse them into one.",
  "textCoverageAudited": false,
  "knownGaps": []
}
