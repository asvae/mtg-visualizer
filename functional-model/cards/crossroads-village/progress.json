{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-09",
  "notes": "2026-09-09: removed the {zone:'Battlefield',controller:'you',types:{has:['Land']}} sink fact — an artifact of the shared ETB-tap-self trigger's tapTarget{validType:'land',owner:'you'} effect needing a valid pool candidate, not a real 'this card wants more lands' synergy claim (trivially self-satisfied by the card itself). Removed pool-wide for every Town-cycle card whose ONLY source for this fact was this trigger shape; zanarkand-ancient-metropolis-lasting-fayth kept it (its own back face genuinely counts lands you control for a token's +1/+1 counters — an independent real want, not from this trigger). Wrote played/battlefield-presence/enters-tapped facts (vector-imperial-capital's own template) plus a `mana` fact for the real \"choose a color, {T}: Add one mana of the chosen color\" ability — modeled as `colors:{hasAny:['W','U','B','R','G']}`, reusing the existing choice-of-color `colors.hasAny` convention (Vector, Imperial Capital's own two-color `hasAny:['B','R']`) just widened to all five since the choice is genuinely unconstrained, rather than inventing a new Constraints shape (no `any:true`-style field exists on TypeConstraint, and this fits the documented vocabulary already). Extended verify-synergy.mjs's own `staticManaColorsFor` with a narrowly-scoped exemption recognizing this exact unique \"{T}: Add one mana of the chosen color.\" text (checked: no other pool card uses this phrasing) as the same \"known statically\" shape the fixed single/choice-of-two case already gets — judgment call, flagging for review since it's a new engine-side exemption, not just a synergy.json fact.",
  "textCoverageAudited": false,
  "knownGaps": []
}
