{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-16",
  "notes": "2026-09-16 (recognizer-lane escalation batch, engine agent): BOTH remaining unprovenanced facts CLOSED. (1) `drawCard-effect-structural.ts` now consumes `effect.optional` for real — when set, a new third template (\"you may draw <phrase>\", the sole real pool shape) is tried once the imperative template's own negative lookbehind correctly excludes the \"may draw\" text; emits the SAME plain `{event:'drawCard', controller:'you'}` fact shape (this Fact vocabulary has no separate \"optional\" marker, same documentary-only convention `destroy`/`move`/`sacrifice`'s own `optional` fields already use). (2) New `recognizers/entersTriggerTypeFilter-sink-structural.ts` derives the paired sink from a named `on(Other)?<Type>Enters` trigger's own name (the type word IS the trigger name, `excludeSelf` derived from the presence of \"Other\" in it) — closes this card's own sink AND (real, checked, generalized) loporrit-scout's/woodland-weavemaster's identical shape, PLUS 2 more real cards found via the same whole-pool check: golbez-crystal-collector/tidus-blitzball-star (`onArtifactEnters`, previously had NO such sink fact at all — genuinely new, not a retag). `apply-recognizers.mjs rook-turret`: both facts retagged with real provenance, 0 new facts. `verify-synergy.mjs` (scoped + full-pool): 0 hard failures. `review` stays \"ai\". Prior notes: Migrated to the unified Fact model (2026-09-12): baseline self-cast/self-enters SOURCE facts added (previously none at all), a real optional drawCard+discard loot SOURCE pair for the ETB trigger, and the real event-shaped SINK for the trigger condition itself (entersBattlefield, controller:you, types:has:['Artifact']) — same shape/precedent as loporrit-scout's/woodland-weavemaster's own 'another X enters' wants. Added TRIGGER_EVENT_MAP['onArtifactEnters'] = 'entersBattlefield' (verify-synergy.mjs), left unmapped until now on purpose. Consolidated to 1 scenario: dropped the old top-level `trigger: 'onArtifactEnters'` shortcut (which gave zero cast/enters evidence) for `sequence: ['onArtifactEnters']` after a real cast->resolve->enters lifecycle, same dwarven-castle-guard/cloudbound-moogle consolidation. Dropped the pre-migration file's own stray, unexplained {zone:'Graveyard'} source fact — no real basis in this card's oracle text. verify-synergy/vitest clean; see SYNERGY_DESIGN.md's own 2026-09-12 Rook Turret entry for the full writeup, including a real newly-surfaced (not caused by this migration) matcher gap: an event-shaped want's own bare `types` constraint is never actually read by factsInteract's event branch, so this card's own sink (and loporrit-scout's/woodland-weavemaster's identically-shaped ones) currently vacuously match all 13 of the pool's unconstrained entersBattlefield producers (all lands), not just real artifacts.\n\n2026-09-16 (AI-fact-elimination re-triage, card-results lane): re-checked both remaining unprovenanced facts. `apply-recognizers.mjs rook-turret` confirmed 0 new facts (only the pre-existing discard fact skipped as already-covered) — not stale, genuinely still open. (1) The drawCard fact (\"you MAY draw a card\"): `card.ts`'s `kind:'drawCard'` Effect gained a real `optional?: boolean` field on 2026-09-16 specifically to let this exact card be told apart from an unconditional draw, but `drawCard-effect-structural.ts` itself was never updated to consume it — it still only ever runs a negative-lookbehind text check and deliberately declines any \"may draw\" occurrence as `kind:'scope'` (its own module doc comment says so explicitly), never reading `effect.optional` at all. This is a narrow, well-specified widening of an EXISTING recognizer (not a new one) — escalated to the recognizer lane. (2) The sink fact ({event:'entersBattlefield', controller:'you', types:{has:['Artifact']}}): no recognizer anywhere derives a paired sink fact from a named `onXEnters` trigger's own type filter — confirmed this is a real, shared, pool-wide gap, not specific to this card: `loporrit-scout` and `woodland-weavemaster` carry the byte-identical unprovenanced shape (types:{has:['Creature']}/['Elf']) for the same reason. A real new-recognizer candidate (trigger-name -> paired entersBattlefield sink), escalated to the recognizer lane. Neither closable by this lane (no recognizer write access).",
  "textCoverageAudited": false,
  "knownGaps": [
    "The 'you MAY draw... if you do, discard' gating is documentary-only (same convention every other optional effect in this pool uses) — a legal draw/discard always happens once the trigger fires, no player-decision engine anywhere in this model.",
    "This card's own SINK types:{has:['Artifact']} constraint is currently inert for matching — factsInteract's event-to-event branch never reads a bare `types` field (only `target`), so the sink vacuously matches any entersBattlefield producer, including the pool's 13 unconstrained land ETB facts. Shared, pre-existing matcher gap (also affects loporrit-scout/woodland-weavemaster identically), not fixed here — flagged in SYNERGY_DESIGN.md for a real future matcher decision."
  ]
}
