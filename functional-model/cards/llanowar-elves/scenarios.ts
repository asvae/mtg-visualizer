import type { Scenario } from '../../harness';

export const scenarios: Scenario[] = [{ result: 'enters the battlefield, no other effect (mana ability is documentary text — see definition.ts comment)' }];
