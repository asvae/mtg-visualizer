import type { Scenario } from '../../harness';

export const scenarios: Scenario[] = [{ result: 'enters the battlefield, no other effect (ETB type-choice grant is documentary text — see definition.ts comment)' }];
