{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-12",
  "notes": "Migrated to unified Fact model (2026-09-12): self-cast(Hand)/self-enters baseline (both anchored to the typeLine's 'Creature' span, same shape zack-fair/the-lunar-whale already use for a plain 'Legendary Creature' type line, no second stacked type to distinguish cast-vs-enters the way summon-choco-mog's Enchantment Creature does), plus a real targeted exile-then-return blink pair for 'exile target creature you control, then return it to the battlefield': {to:'Exile',from:'Battlefield',controller:'you',target:{types:{has:['Creature']}},targeted:true} and {to:'Battlefield',from:'Exile',event:'entersBattlefield',controller:'you',target:{types:{has:['Creature']}},targeted:true} (same to/from pair shape jill/dion's own self-transform exile+return uses, but target-constrained rather than subject:'self' since this blinks a CHOSEN creature, not necessarily itself — checked, no existing pool precedent for blinking ANOTHER target creature before this card). Sink: {to:'Battlefield',controller:'you',types:{has:['Creature']}} (wants a creature you control to target). The 'additional end step' clause is NOT modeled as a Fact — no Effect kind/vocabulary exists for it at all (not just 'no trace evidence possible', the AuronsInspiration-exemption case) — stays real, honest, undemonstrated text on definition.ts, same treatment Ultimecia's own back-face 'take an extra turn' comment used before extra-turns was closed. Surfaced a genuine NEW engine gap, ENGINE_GAPS.md #17 ('insert one more of this same step before the turn moves on') — checked turn.ts directly: TurnState.extraTurns (gap #3, closed) only queues a whole EXTRA TURN at the turn-wrap point, nothing splices one more occurrence of the CURRENT phase into the CURRENT turn. Also found (grepping the real cardsfolder pool-wide before writing the gap up as narrow) that this is NOT unique to this card: Balthier and Fran and Genji Glove already hit the identical primitive gap for their own 'additional combat phase' clauses (real Forge DB$ AddPhase both times) and had only a per-card comment, no central ENGINE_GAPS.md tracking until now — gap #17 generalizes to cover both shapes. annotations-authoring.json added; real offsets baked via compute-annotations.mjs, verified against data/fin/fin_scryfall.json #86 oracle text (byte-identical: 'At the beginning of your end step, exile target creature you control, then return it to the battlefield under its owner's control. Then if it's the first end step of the turn, there is an additional end step after this step.'). value fields recomputed via compute-weights.mjs --slug=y-shtola-rhul (all -1 placeholders resolved to 1, neutral floor — no numeric magnitude on any of these facts). verify-synergy.mjs scoped: 0 hard failures, 1 soft note (trace has legendRule with no matching declared produce) — the same pool-wide-accepted keywordScenarios() 'second-copy-legendary' probe every other Legendary card using that helper also produces (confirmed live against the-prima-vista/ultros-obnoxious-octopus), not something this migration introduced or needs to satisfy. Full pool: 319 checked, 4 pre-existing hard failures unrelated to this card (al-bhed-salvagers, stuck-in-summoner-s-sanctum, ultros-obnoxious-octopus, valkyrie-aerial-unit — all mid-edit by concurrent sessions per git status). vitest run functional-model: 351/351 pass. find-synergies.mjs diff (isolated via a temporary HEAD-restore of just this card's own synergy.json, not a stale full-tree diff): lost ~130 old unconstrained 'battlefield presence' matches from the removed bare presence-only self-battlefield SOURCE fact (no longer allowed — a SOURCE must be a real movement, not presence) plus 1 self-interaction (second-copy-legendary, since self-enters deliberately has no subject, same accepted tradeoff summon-bahamut's own self-enters/self-cast already established) + gained 22 new lines (11 unconstrained battlefield-presence sinks matched twice, once via self-enters' real to:'Battlefield' and once via the blink-return fact's own to:'Battlefield' — both zone-shaped, both land on the same unconstrained sinks, matching the exact bahamut precedent). The new to:'Exile'/targeted Creature-constrained facts currently have zero pool matches (no sink wants Exile presence or a type-constrained battlefield-blink producer yet) — real, honest, just unmatched today, same as several of this card's own newly-authored facts on other cards. UPDATE (2026-09-12, later same day): ENGINE_GAPS.md gap #17 CLOSED for real. turn.ts gained a genuine `queueExtraPhase`/`isFirstPhaseGroupOccurrenceThisTurn` primitive (a queued re-entry of the CURRENT turn's own EndOfTurn/Combat phase group — structurally distinct from `extraTurns`/gap #3, a whole extra TURN; real Forge citation `AddPhaseEffect.java`/`PhaseHandler.extraPhases`). definition.ts's own `onEndStep` effect now reads `ctx.firstPhaseGroupOccurrenceThisTurn` (set for real by engine.ts's `fireOnPhaseEnterTriggers`, or declared per-scenario the same way `mode`/`xPaid` are) and calls `actions.queueExtraPhase('EndOfTurn')` on the first end step of the turn only — the 'additional end step' clause is REAL now, no longer documentary-only text. scenarios.ts updated: 2 existing scenarios now set `firstPhaseGroupOccurrenceThisTurn: true` (queues), 1 new scenario demonstrates the real negative case (`false` — a later end step this turn does NOT re-queue, matching real Forge's own `ConditionSVarCompare$ LT1` gate). trace.json regenerated (5 scenarios now); verify-synergy.mjs scoped: still 0 hard failures, same pre-existing `legendRule` soft note as before, plus a new `queueExtraPhase` fn added to verify-synergy.mjs's own `IGNORED_FNS` (real turn-structure bookkeeping, never produce-relevant — same bucket as `phase`/`delayUntil`), so the new action produces no spurious soft note. No new synergy Fact added for this — same 'never modeled as a Fact' treatment `queueExtraTurn` (gap #3) already established. Full pool re-run: 320 checked, 0 hard failures. vitest run functional-model: 373/373 pass (7 new: turn.test.ts/engine.test.ts's own real queued-extra-phase describe blocks).",
  "textCoverageAudited": false,
  "knownGaps": []
}
