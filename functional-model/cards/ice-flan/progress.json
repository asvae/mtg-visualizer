{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-16",
  "notes": "2026-09-16 (recognizer-lane escalation batch, engine agent): CLOSED via a new pronoun-carryover branch in `putCounterTarget-effect-structural.ts` — reads the constraint off the PRECEDING `tapTarget` effect's own `validType` (this card's own 'creature-or-artifact') rather than requiring a typeWord to appear in the putCounter clause's own text at all (it never does — the real clause is just \"Put a stun counter on it.\"). Matches a new `\\bput\\b...\\bcounters?\\b...\\bon\\b...\\b(?:it|each of those permanents)\\b` template (the second alternative is Omega, Heartless Evolution's own real plural phrasing). `apply-recognizers.mjs ice-flan`: the existing `putCounter` source fact retagged with real provenance; the paired sink now ALSO retags in place (its own coreKey collides with `tapTarget-effect-structural`'s own pre-existing sink for the identical real want, and the shared runner-level identity-merge correctly unions both annotations into one fact rather than duplicating). `verify-synergy.mjs` (scoped + full-pool): 0 hard failures. `npx tsc --noEmit`/`npx vitest run functional-model`: clean (added 3 new real test cases to `putCounterTarget-effect-structural.test.ts` — Ice Flan, Omega Heartless Evolution, Summon: Shiva). `review` stays \"ai\". Prior 2026-09-16 (AI-fact-elimination sweep, card-results lane): re-examined the 1 remaining unprovenanced fact (source `{event:'putCounter', counterType:'stun', controller:'opp', target:{types:{hasAny:['Artifact','Creature']}}, targeted:true}`) fact-by-fact. `apply-recognizers.mjs ice-flan` rerun: 0 new facts (6 already provenanced, unchanged). NOT closable by an existing recognizer, but NOT genuinely bespoke either — a real, well-precedented recognizer-lane escalation, already partly documented (by name) in `putCounterTarget-effect-structural.ts`'s own module doc comment: this card's own `kind:'putCounterTarget'` effect is declined for TWO structural reasons at once — (1) `validType:'creature-or-artifact'` has no confirmed English typeWord template in this pool today, and (2) it's immediately preceded by a `kind:'tapTarget'` effect in the same container (\"put a stun counter on IT\" — a real pronoun-carryover reference back to the just-tapped object, not a fresh \"target <noun>\" clause of its own) — the recognizer's own `isEligible` explicitly declines both, citing Ice Flan by name for reason (2). **Recognizer-lane escalation**: a pronoun-carryover template (\"put a <counterType> counter on it\", immediately after a `tapTarget` effect in the same container) would sidestep reason (1) entirely too — the anaphoric \"it\" never restates a type word at all, so no typeWord/validType-driven template is even needed for this shape; the emitted fact's own `target.types` would read off the PRECEDING `tapTarget` effect's own `validType` instead (same object, same pool, per this card's own definition.ts comment: \"Nothing ties two separate declarative effects to one shared chosen target... both pools must stay identical\"). Real, CONFIRMED multi-card motivation (checked directly, not just cited from the recognizer's own comment): `summon-shiva`'s own synergy.json carries the textually-identical unprovenanced `putCounter`/stun/tapTarget-preceded fact today; `omega-heartless-evolution`'s own synergy.json carries the same shape too (`hasAny:['Creature','Artifact']`, also unprovenanced) — at least 3 real pool cards would close via one new recognizer branch, not a one-off. (Ultros, Obnoxious Octopus — also named in the recognizer's own doc comment as sharing this shape — have no `functional-model/` dir yet, so not counted as currently-closable, but would benefit once authored.) Did not attempt to hand-restore this fact or touch `synergy.json`/`recognizers/*` myself (write-fenced). `verify-synergy.mjs`/`verify-text-coverage.mjs` (scoped): 0 hard failures, full real-text coverage (100%, no gaps reported). **Also fixed a stale `knownGaps` entry below** (dated 2026-09-12, claiming `definition.ts` still narrows to `validType:'creature'` only) — the real fix already landed 2026-09-15 (`validType:'creature-or-artifact'`, confirmed by direct read of the current file); the entry is corrected, not removed, so the history stays legible. `review` stays \"ai\" (already \"ai\", no reset needed — no content change made to this card).\n\n2026-09-14: Islandcycling {2} is now real, structured, engine-piloted (ENGINE_GAPS.md gap #23, closed 2026-09-14) \u2014 same mechanism as Cloudbound Moogle's Plainscycling (see that card's own progress.json note), scoped to Island. scenarios.ts now runEngineScenarios (2 real engine-piloted scenarios). isIceFlanTutorFact/isIceFlanDiscardSelfWant verify-synergy.mjs exemptions REMOVED (real trace evidence now exists instead). vitest 550/550, verify-synergy.mjs 320 checked/0 hard failures, tsc no new errors.\n\nMigrated to the unified Fact/annotations model (2026-09-12). Baseline `self-cast`/`self-enters` added (typeLine-anchored, 'Creature' \u2014 Elemental Ooze). Real ETB effect split into two facts per its own two sentences: `{event:'tap', controller:'opp', target:{types:{hasAny:['Artifact','Creature']}}, targeted:true}` and `{event:'putCounter', counterType:'stun', controller:'opp', target:{types:{hasAny:['Artifact','Creature']}}, targeted:true}` (broadened from the old single `has:['Creature']`-only fact to the full disjunctive oracle scope \u2014 the engine's own `definition.ts` still narrows to `validType:'creature'` only, a documented, unrelated engine gap: no `tapTarget`/`putCounterTarget` validType covers an artifact-or-creature union). Sink (target precondition) broadened the same way, `to:'Battlefield'` (was legacy `zone`). Islandcycling modeled as two new facts, mirroring Cloudbound Moogle's Plainscycling exactly: SINK `{event:'discard', target:'self'}` (the discard-as-cost act) and SOURCE `{to:'Hand', from:'Library', types:{has:['Island']}}` (the tutor), both zero-trace-evidence (Islandcycling lives only as `staticAbilities` text, never a resolvable Effect) \u2014 covered by two new verify-synergy.mjs exemptions (`isIceFlanDiscardSelfWant`/`isIceFlanTutorFact`, same per-card-scoped shape as their Cloudbound Moogle counterparts). No separate scenario for Islandcycling (matches Cloudbound Moogle precedent \u2014 it's fundamentally unmodelable via any Effect, not a 'consolidate into one scenario' case). Scenarios themselves converted from top-level `trigger:'onEnter'` to no-top-level-trigger + `sequence:['onEnter']` (same dwarven-castle-guard/cloudbound-moogle consolidation, 2026-09-11 precedent) so the baseline self-cast/self-enters facts get real `fn:'cast'`/`fn:'enters'` trace evidence instead of relying on an exemption. Added to scripts/annotation-coverage.mjs's ANNOTATED_CARD_SLUGS. verify-synergy.mjs: 0 hard failures (scoped + full pool \u2014 the only other pool failures present, cargo-ship/cecil-dark-knight/dragoon-s-wyvern/il-mheg-pixie/stiltzkin-moogle-merchant/the-wind-crystal/white-auracite/zack-fair, are pre-existing, from concurrent in-flight migrations by other sessions, unrelated to this card). vitest run functional-model: 238/238 unchanged. find-synergies.mjs before/after (isolated via `git stash` scoped to just this card's own files): before 0 real interaction lines (old schema's narrow opp-controller-scoped facts matched nothing pool-wide, a real pre-existing gap, not a regression); after 142 (140 unconstrained-battlefield-presence 'enters the battlefield' matches, same pool-wide pattern every other newly-migrated baseline self-enters fact produces, + 2 real 'tutor' matches \u2014 Nibelheim Aflame, The Water Crystal \u2014 the same 2 cards Cloudbound Moogle's own Plains tutor already matches, confirming the Island tutor fact works identically). The tap/putCounter(stun)/target-precondition-sink/discard-self facts still produce 0 matches \u2014 real, documented gaps (no other pool card sources a literal `discard` event or wants a `tap`/`putCounter:stun` producer yet), not fabricated.",
  "textCoverageAudited": false,
  "knownGaps": [
    "CLOSED 2026-09-16 \u2014 the pronoun-carryover recognizer gap this entry used to describe is now real and closed (`putCounterTarget-effect-structural.ts`'s own new branch) \u2014 see this file's own 2026-09-16 notes entry above."
  ]
}
