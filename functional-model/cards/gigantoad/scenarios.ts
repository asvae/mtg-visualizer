import type { Scenario } from '../../harness';

export const scenarios: Scenario[] = [{ result: 'enters the battlefield, a vanilla 4/4 body (the land-count buff is real text, not mechanically enforced — see definition.ts comment)' }];
