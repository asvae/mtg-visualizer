{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "notes",
  "lastVerified": "2026-09-11",
  "notes": "2026-09-11 (later still, reversal): user asked to add the 'wants an artifact card in library' SINK back (`{to:'Library', controller:'you', types:{has:['Artifact']}}`) for consistency with from-father-to-son/delivery-moogle's own real tutor-precondition pattern — directly reverses the earlier removal noted below (user changed their mind after seeing the fuller pattern). Same real, honest, 0-incoming-match gap as before (no pool card sources a card into Library) — restored, not reshaped, and re-verified oracle-anchored (\"reveal an artifact card\"). find-synergies.mjs: 0 diff (real, documented zero-match). verify-synergy.mjs/vitest: 0 hard failures / 238/238.\n\n2026-09-11 (later still): added a new SINK `{event:'attacks', target:'self'}` for \"Whenever Ashe attacks\" — brand-new event vocabulary pool-wide (zero precedent, checked). Real trace evidence, no scenario change needed: `pilotDeclareAttackers` already logs a real `{fn:'attack', card}` line; `verify-synergy.mjs` gained `producedEvents`' own `case 'attack'` (scoped to `entry.card===cardName`) plus `TRIGGER_EVENT_MAP.onAttack: 'attacks'` (backed by this card's own real `{fn:'trigger', name:'onAttack'}` line right after the attack). find-synergies.mjs diff: 0 new matches (expected — nothing else in the pool produces `event:'attacks'` yet; this is real vocabulary for a future payoff card, same framing as `event:'pump'`'s own promotion). New soft note on this card (expected, not a regression): `trace has attack {...} with no matching declared produce` — correct, since only the SINK was added, no SOURCE. Oracle-anchored (\"Ashe attacks\"). `verify-synergy.mjs`: 0 hard failures. `vitest run functional-model`: 238/238. 2026-09-11 (later, user's own direct call live on the Facts tab): REMOVED the 'wants-artifact-in-library' SINK fact (`{to:'Library', controller:'you', types:{has:['Artifact']}}`) entirely, not reshaped — user: \"let's remove.\" Confirmed via find-synergies.mjs before/after: 0 real matches lost (this sink had 0 incoming matches anyway, as already noted below — no pool card produces an artifact card into a library). Also: the shared `ZONE_MOVEMENT_NAMES` label for this card's own Library->Hand SOURCE fact renamed from 'found' to 'tutor' (user's own direct call, same live session) — see `synergy.ts`'s own table doc comment; this card's own fact data is unchanged, only the rendered label (shared with Cloud, Midgar Mercenary's own identical-shaped fact). Migrated to the unified Fact/annotations model (2026-09-11, same rollout as summon-bahamut/ultima-origin-of-oblivion/adelbert-steiner/aerith-gainsborough/aerith-rescue-mission). This card was previously one of the 8 pool cards audited and confirmed genuinely empty (`{source:[],sink:[]}`) rather than under-authored — that was correct as far as it went (no fact of any kind existed), but the underlying knownGaps note ('dig effect logs zero read: lines even after full trace regen — harness instrumentation gap, not a synergy-authoring gap') pointed at a REAL, fixable bug rather than a permanent dead end, same rigor ultima-origin-of-oblivion's own mana-doubling fix used. Fixed for real in harness.ts: `dig`'s own `matches` predicate checked `effectiveTypes(c)` directly, bypassing the logged Card interface entirely — the exact same bug class `move`/`sacrifice`'s own `matches` closures were already fixed for (2026-09-05/06), just never hit until a card (this one) declared a real want against a `dig`-filtered search. Now routes through `loggingCard`, so a `validType:'artifact'` dig logs real `read:isArtifact` per candidate. Checked impact on the other 5 pool cards using `kind:'dig'` (esper-origins-summon-esper-maduin, dark-confidant, choco-seeker-of-paradise, commune-with-beavers, memories-returning): none declare `validType:'artifact'` (all use `'any'`/omitted, which short-circuits before the new `loggingCard` wrap), so this fix is a genuine no-op for them — confirmed by actually regenerating all 5 via `run-scenarios.mjs` and diffing byte-for-byte against their pre-fix `trace.json` (identical, not left un-checked/assumed). Only `ashe-princess-of-dalmasca/trace.json` itself needed (and got) a real regen. Four new facts authored from the real, single-line oracle text ('Whenever Ashe attacks, look at the top five cards of your library. You may reveal an artifact card from among them and put it into your hand. Put the rest on the bottom of your library in a random order.'): `self-cast` (`{event:'cast', from:'Hand', target:'self', value:-1}`, typeLine-anchored 'Creature' — checked definition.ts, plain {2}{W}, no alternate-cost/flashback/foretell wrinkle) and a baseline `self-enters` (`{event:'entersBattlefield', to:'Battlefield', controller:'you', subject:'self', target:'self', value:-1}`, typeLine-anchored 'Legendary Creature' — CR 111.7/608.2b, no pre-existing bare presence fact to convert, so added fresh, same pattern Steiner/Aerith Gainsborough both got) are the two universal baseline facts every card in this rollout gets. The real ability itself is a genuine SOURCE zone-change fact, not presence-shaped: `{to:'Hand', from:'Library', controller:'you', types:{has:['Artifact']}, value:-1}` (Library->Hand, an artifact card specifically) plus its mirror SINK `{to:'Library', controller:'you', types:{has:['Artifact']}, value:-1}` ('wants an artifact card in your library for this ability to find') — same shape delivery-moogle's own 'find-artifact-to-hand'/'wants-artifact-in-library' pair already establishes as precedent pool-wide. No `dies`/sacrifice fact invented — this card's real oracle text has zero death/sacrifice/self-destruction mechanism, same discipline that removed adelbert-steiner's unfounded one. Known, pre-existing matcher limitation (documented, not fixed here — same category as ultima-origin-of-oblivion's own addMana-sink note): the SOURCE fact's own `types:{has:['Artifact']}` is presently INERT for narrowing matches — `factsInteract`'s zone-zone branch only ever resolves a producer's static attrs via `subject` (omitted here, correctly, since the found card's identity genuinely varies), never consults a bare `types` field declared directly on the SOURCE fact itself — so today this fact can only satisfy an UNCONSTRAINED 'wants something in hand' want (confirmed via find-synergies.mjs: it does match Nibelheim Aflame/The Water Crystal's own unconstrained hand-presence wants), never a hypothetical 'wants an artifact specifically in hand' want, even though the real ability is genuinely artifact-filtered. `annotations-authoring.json` added (4 entries, positionally aligned); `annotations` computed via `compute-annotations.mjs` against the real oracle text/type line; card added to `scripts/annotation-coverage.mjs`'s `ANNOTATED_CARD_SLUGS`. Real find-synergies.mjs diff: 0 -> 132 real interaction lines (130 real unconstrained battlefield-presence wants now matched via `self-enters`, e.g. Aerith Gainsborough/Battle Menu/Summon: Bahamut/Zodiark Umbral God; 2 real unconstrained hand-presence wants matched via the artifact-to-hand fact, Nibelheim Aflame/The Water Crystal) — pure gain, nothing lost (the card had zero facts and zero matches before). The sink's own 'wants-artifact-in-library' fact has 0 incoming matches (no pool card currently authors a SOURCE fact producing an artifact card into a library) — a pre-existing pool-authoring gap, not something fixable from this card's side, same as adelbert-steiner's own 0-incoming 'wants-equipment' sink. `verify-synergy.mjs`: 0 hard failures for this card (soft notes only: mechanical `tapForMana`/`drawCard`/`attack` actions with no fact vocabulary, same as every other card's own casting/combat scaffolding); full pool re-verified at baseline (1 pre-existing hard failure, auron-s-inspiration, unrelated — a peer session may be fixing that one in parallel). `npx vitest run functional-model`: 236/236. `npm run typecheck`: clean.",
  "textCoverageAudited": false,
  "knownGaps": [
    "2026-09-15 (real, concrete gap found via the new functional-model/scripts/text-coverage.mjs signal — see .claude/contracts/card-schema.md's own 'factsTextCoverage' section): the real oracle text's own \"look at the top five cards of your library\" clause (the dig's own qty:5 lookup itself, as opposed to the qty:1 reveal/take-to-hand half, which IS covered) and \"Put the rest on the bottom of your library in a random order\" clause (the dig's own unchosen-4-cards destination) have NO fact/annotation anywhere on this card. The first is arguably inert for synergy purposes (a same-zone Library reposition, not an external zone change any other card could plausibly produce/consume) and the second is a genuine CR-mechanical consequence with the same property (Library -> Library, no external hook) — neither is a blanket 'stop tracking' verdict, just an honest note that this card's own textCoverageAudited claim (previously true) was stale/wrong and is reset to false pending an explicit decision on whether either clause needs its own fact."
  ],
  "oracleTextSnapshot": "Whenever Ashe attacks, look at the top five cards of your library. You may reveal an artifact card from among them and put it into your hand. Put the rest on the bottom of your library in a random order.",
  "reviewedAt": "2026-09-11"
}
