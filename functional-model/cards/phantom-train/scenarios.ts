import type { Scenario } from '../../harness';

export const scenarios: Scenario[] = [{ result: 'sacrifices another artifact or creature, puts a +1/+1 counter on itself, and becomes an artifact creature', you: { creaturesCount: 1, artifactsCount: 1 } }];
