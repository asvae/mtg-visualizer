import type { Scenario } from '../../harness';

export const scenarios: Scenario[] = [{ result: 'draws a card (the two sacrificed artifacts are paid as part of the activation cost)', you: { libraryCount: 1 } }];
