{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-16",
  "notes": "2026-09-16 (engine-lane primitive build, whole-pool sweep off white-mage-s-staff's own equip-trigger-grant closure): this card's own \"Whenever equipped creature attacks, destroy target creature an opponent controls\" no longer needs a manual `pilotFireTrigger` call to fire at all — added a new, closed `Trigger.on: 'equippedAttacks'` value (card.ts) + widened `engine.ts`'s `fireOnAttackTriggers` to auto-fire an Equipment's own trigger the moment the creature it's attached to (not the Equipment) attacks (real Forge citation: `res/cardsfolder/u/ultima_weapon.txt`'s own `T:Mode$ Attacks | ValidCard$ Card.EquippedBy | ...`). `definition.ts`'s own `onEquippedAttacks` trigger now carries `on: 'equippedAttacks'`. This card's OWN `scenarios.ts` is still the older harness.ts-style manual-fire shape (not migrated to a real engine-piloted trace in this same pass — bonus whole-pool find, not this pass's own named target), but `cloud-midgar-mercenary/scenarios.ts`'s own real engine-piloted combo trace (which reuses this card's `definition.ts` directly, see this file's own prior note) WAS already a real engine-piloted scenario, and its own manual `pilotFireTrigger(..., 'onEquippedAttacks', ...)` call would have DOUBLE-FIRED against the new real auto-dispatch (confirmed via a real before/after trace: 4 total trigger firings instead of the correct 2, the extra 2 finding 0 legal targets and silently no-opping) — fixed by removing that manual call from `cloud-midgar-mercenary/scenarios.ts` (Cloud's own real `triggerDoubling` grant still correctly doubles the now-single real auto-fire, verified: exactly 2 `destroy` events, matching the real intended combo). `verify-synergy.mjs ultima-weapon`/`cloud-midgar-mercenary`: both 0 hard failures.",
  "textCoverageAudited": false,
  "knownGaps": [
    "This card's own scenarios.ts is still the older harness.ts-style (manual `trigger:` name-fire) — not migrated to a real engine-piloted trace that would exercise the new `on: 'equippedAttacks'` auto-dispatch (white-mage-s-staff/sage-s-nouliths got that migration in the same pass; this card was a bonus find, deferred for scope). `cloud-midgar-mercenary`'s own combo scenario, which reuses this card's definition.ts directly, DOES exercise the real auto-dispatch already."
  ]
}
