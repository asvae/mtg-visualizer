{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "notes",
  "lastVerified": "2026-09-16",
  "notes": "2026-09-16 (card-results lane, pool-wide baseline-pair cleanup): removed one stale legacy fact — `{zone:'Graveyard', controller:'you', subject:'self', face:'front'}` — directly from synergy.json (hand-edit, not a script regen). This predates BOTH the 2026-09-11 zone->to fold (used the retired `zone` key instead of `to`) and the 2026-09-11 required-annotations rule (carried no `annotations` field at all, unlike every other fact on this card) — a leftover half of the old self-cast/self-graveyard baseline pair for this card's own front Sorcery face (no matching `{event:'cast', from:'Hand', ...}` half was present to remove alongside it — apparently already dropped or never regenerated). Definition lane's own pool-wide check named this card as 1 of 4 real remaining outliers still carrying this now-dropped convention (ultima, zack-fair, memories-returning — the last of which turned out NOT to match this shape). `apply-recognizers.mjs`'s own synthetic-fact match-time synthesis (`isNormalInstantOrSorcery`) covers the equivalent self-cast/self-graveyard pair for this face automatically now, same as the rest of the pool. Every other fact on this card (lifegain, flashback cast/exile, saga lore/sacrifice/dies, grantKeyword, pump, surveil — all real, provenance-tagged since this note's own predecessor was written) is untouched. Re-ran `verify-synergy.mjs esper-origins-summon-esper-maduin`: still 0 hard failures; the pre-existing soft notes below are UNCHANGED (this removal didn't add or remove any of them — the stale fact had no annotations to lose and wasn't backing any of these checks either way). Not a content/behavior change, so `review` stays `\\\"ai\\\"` unchanged, not reset. NOTE: this card's own `progress.json` is otherwise quite stale (last substantive verification 2026-09-04, predating most of today's recognizer wiring) — this entry only covers the one narrow removal, not a full re-triage; `verifySynergy`/`knownGaps` below still reflect the 2026-09-04 state for everything else.\n\n2026-09-04: trace has moveTo ({\"fn\":\"moveTo\",\"target\":\"Esper Origins\",\"zone\":\"Battlefield\"}) with no matching declared produce | trace has putCounter ({\"fn\":\"putCounter\",\"target\":\"Esper Origins\",\"counterType\":\"finality\",\"amount\":1}) with no matching declared produce",
  "textCoverageAudited": false,
  "knownGaps": [
    "front-face transform exile/putCounter mid-effect left undeclared (2 soft notes)"
  ]
}
