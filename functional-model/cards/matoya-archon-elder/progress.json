{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "notes",
  "lastVerified": "2026-09-16",
  "notes": "2026-09-16 (recognizer-lane escalation batch, engine agent): CLOSED — new `recognizers/scryOrSurveilTrigger-structural.ts` (plain-text, mirrors `lifegain-trigger-structural.ts`) matches \"Whenever you scry or surveil,\" and emits the 2 real sinks (`event:'scry'`/`event:'surveil'`, both `controller:'you'`), each anchored to its own word within the clause. Re-confirmed this card is still the sole real pool motivator (grepped `onScry`/`onSurveil` triggers and `scry`/`surveil` SINK facts pool-wide again before building — unchanged from the prior note's own check). `apply-recognizers.mjs matoya-archon-elder`: both existing hand-authored sink facts retagged with real provenance, 0 new facts. `verify-synergy.mjs` (scoped + full-pool): 0 hard failures (same pre-existing `isMatoyaScryBroadcastWant` scry-side exemption still applies — unaffected by this pass, the scry engine gap itself is untouched). `review` stays \"ai\". Prior 2026-09-16 (AI-fact-elimination sweep, card-results lane): re-examined all 3 remaining unprovenanced facts (2nd `drawCard` source, `scry` sink, `surveil` sink). `apply-recognizers.mjs matoya-archon-elder` rerun: 0 new facts (matches the prior 2026-09-12 note's own design, which deliberately kept two separately-annotated drawCard facts \"so the two facts don't collide on factIdentity\" — a real, since-superseded design decision, see below). **Closed 1 of 3 via direct investigation + a genuinely-stale hand-edit** (not an existing-recognizer rerun, and not a hand-restore — the opposite: recognized a fact as a stale duplicate of an ALREADY-covered real claim and dropped it, per this task's own explicit allowance): directly invoked `recognizeDrawCardEffectStructural` against this card's own real `definition.ts` + oracle text (both `onScry`/`onSurveil` triggers carry an identical bare `{kind:'drawCard'}` effect) and confirmed it produces the SAME fact (`{event:'drawCard', controller:'you'}`, annotated at 'draw a card', offset 30-41) for BOTH triggers — exactly the \"matoya-archon-elder's onScry/onSurveil pair\" case `drawCard-effect-structural.ts`'s own module doc comment already names as its motivating multi-container dedup example, deduped at the runner level (`apply-recognizers.mjs`'s own `mergeRecognizedFactsByIdentity`). The pre-existing SECOND hand-authored `drawCard` fact (annotated at offset 44-74, which is the card's own REMINDER-TEXT parenthetical \"(Draw after you scry or surveil.)\", not real rules text) was a 2026-09-12-era authoring choice to dodge an identity collision under the OLD (pre-recognizer) fact-identity model — `apply-recognizers.mjs`'s own `coreKey` doesn't key on annotations at all, so this workaround is now obsolete: the real card only has ONE \"draw a card\" instruction, already fully covered by the first (correctly-provenanced) fact. Dropped the second fact directly from synergy.json (never hand-restored/inferred — the opposite operation). `verify-synergy.mjs`/`verify-text-coverage.mjs` (scoped) confirmed clean both before and after (0 hard failures, 0 coverage gaps — the reminder-text parenthetical was never required for coverage). **The 2 remaining unprovenanced facts (`scry`/`surveil` sinks) are a real, clean recognizer-lane escalation, not bespoke**: no recognizer in the whole pool reads the trigger PRECONDITION \"Whenever you scry or surveil,\" at all (grepped `functional-model/recognizers/*.ts` for 'scry'/'surveil': zero hits) — `lifegain-trigger-structural.ts` is the exact right precedent to mirror (pure text-based, no CardDefinition field read at all, just the literal clause \"Whenever you gain life,\" → a sink fact; this card's own clause would build the same way, but emit TWO sinks — `event:'scry'` anchored on the word 'scry', `event:'surveil'` anchored on the word 'surveil', both `controller:'you'` — since the real clause names both conditions as independent OR-branches, matching this card's own pre-existing two separately-anchored sink facts exactly). Checked whole pool for other real motivators: this card is the ONLY one with an `onScry`/`onSurveil`-named trigger (grepped every `definition.ts`) and the ONLY one with a `scry`/`surveil` SINK fact anywhere (10 other pool cards have `scry`/`surveil` SOURCE facts, none has a SINK) — single-card today, same accepted bar `lifegainDoubleKeyword-structural.ts`/`ptFormulaSetToCreaturesControlled-structural.ts` already set elsewhere in this catalog. `review` stays \"ai\" (already \"ai\"). Prior 2026-09-12: Migrated to the unified Fact model (SYNERGY_DESIGN.md). Real oracle: 'Whenever you scry or surveil, draw a card. (Draw after you scry or surveil.)' Forge's own real script models this as TWO separate triggered abilities (Mode$ Scry, Mode$ Surveil, both running the same TrigDraw) — kept as definition.ts's own pre-existing onScry/onSurveil triggers, unchanged. Baseline self-cast(Hand)/self-enters facts added (typeLine-anchored, same 'Creature'/'Legendary Creature' substring convention as g-raha-tia/dwarven-castle-guard). Two SINK facts, one per trigger ({event:'scry',controller:'you'} / {event:'surveil',controller:'you'}, both oracle-anchored on the respective word in 'Whenever you scry or surveil') paired with two SOURCE drawCard facts (one anchored on 'draw a card', one on the reminder text's 'Draw after you scry or surveil' — distinct annotations specifically so the two facts don't collide on factIdentity, since both are otherwise role+label-identical). Neither drawCard/sink pair carries oncePerTurn — the real oracle text has no once-per-turn cap, unlike G'raha Tia's Allagan Eye.\n\nReal, checked, asymmetric engine support: `surveil` is fully wired (card.ts's own `kind:'surveil'` Effect, `actions.surveil`/state.ts, already exercised for real by Dreams of Laguna/fin-50 and Il Mheg Pixie) but `scry` has ZERO implementation anywhere in this engine — checked directly: interfaces.ts's own `declare function scry(player, qty): void` is a bare Forge-signature-mirror doc entry, never wired into card.ts's real Actions type or state.ts (no Effect kind, no Actions.scry, no state.scry call anywhere in the pool). This is a real, documented engine gap (not a modeling oversight) — there is no possible in-scenario action that could produce real fn:'scry' trace evidence until that machinery is built. Added a scoped verify-synergy.mjs exemption, `isMatoyaScryBroadcastWant` (w.event==='scry' && card.name==='Matoya, Archon Elder'), same 'real fact, real documented wall, zero achievable evidence' tolerance as isAuronsInspirationBroadcastPumpFact — the scry SINK fact is still modeled (a future scry-producing card should still match it once find-synergies.mjs sees one) but carries no trace evidence and never will until the engine gains a real scry action. The onSurveil sibling sink on this SAME card is NOT exempted and gets real evidence.\n\nscenarios.ts rewritten to a real engine-piloted playthrough (cast -> enters -> a real, generic `actions.surveil(ctx.you, 1)` call — fully-wired real engine action, not fabricated — -> onSurveil fired manually -> real drawCard). Per this session's own '1 scenario, real basic function' rule, this single self-contained scenario is enough; no cross-card synergy (e.g. also casting Dreams of Laguna) required in the scenario itself. verify-synergy.mjs: 0 hard failures, 2 soft notes — 'tapForMana no fact vocabulary' (pre-existing, pool-wide mana-fact gap, not this card's concern) and 'trace has surveil with no matching declared produce' (expected: the in-scenario surveil is a generic/external action, not Matoya's own produce — Matoya is a pure SINK for it, never a SOURCE). find-synergies.mjs: real cross-card matches confirmed — Dreams of Laguna and Il Mheg Pixie (both real surveil producers already in the pool) --[surveil]--> Matoya; Matoya's own self-enters also gained the usual ~130 generic Battlefield-presence sink matches every migrated creature's baseline ETB fact picks up pool-wide. No scry-side matches exist yet (0 real scry producers anywhere in the pool), as expected.",
  "textCoverageAudited": true,
  "knownGaps": [
    "No real `scry` action implemented anywhere in this engine (interfaces.ts's own `declare function scry(...)` is a bare, never-wired Forge-signature mirror — unlike `surveil`, which is fully real). The `{event:'scry'}` SINK fact on this card is genuine, oracle-anchored vocabulary but has zero achievable trace evidence until a real scry action is built; exempted in verify-synergy.mjs via `isMatoyaScryBroadcastWant`. Worth a real ENGINE_GAPS.md entry if/when a second card also needs real scry (not added there yet, scoped to this card's own progress notes per the existing per-card-gap precedent — Auron's Inspiration/Magitek Infantry's own gaps are also only documented in verify-synergy.mjs+progress.json, not ENGINE_GAPS.md).",
    "CLOSED 2026-09-16 — `scryOrSurveilTrigger-structural.ts` now reads this trigger precondition for real — see this file's own 2026-09-16 notes entry above."
  ],
  "oracleTextSnapshot": "Whenever you scry or surveil, draw a card. (Draw after you scry or surveil.)",
  "reviewedAt": "2026-09-12"
}
