{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-12",
  "notes": "2026-09-12 (ENGINE_GAPS.md gap #13, real closure): Masamune's own \"Equipped creature has 'If a creature dying causes a triggered ability of this creature or an emblem you own to trigger, that ability triggers an additional time.'\" is now real, general engine machinery (`card.ts`'s new `triggerDoubling`, a new shared `functional-model/triggers.ts`'s `fireTrigger`, `state.ts`'s new `shouldDoubleTrigger`) — the SAME mechanism Cloud, Midgar Mercenary and Traveling Chocobo also needed, built once and shared, not three one-off fixes. `definition.ts` now declares `triggerDoubling: [{ scope: 'equippedSelf', causedBy: 'dying' }]` (replacing the old documentary-only `staticAbilities` string for this one clause; the first-strike/must-be-blocked static stays text, no representable mechanism for it). `runEngineScenarios()` replaced the old flat `harness.ts` scenario (which only demonstrated the plain Equip activation) with a real full combo: cast + equip Al Bhed Salvagers (`cards/al-bhed-salvagers/definition.ts`, a real FIN card already modeled with its own real `onDies` trigger) with The Masamune, real lethal combat (Hill Gigas blocks and kills it, a genuine one-sided 704.5g death), then its own `onDies` trigger fires manually (same real \"no auto-fire for a dies-triggered ability\" pattern dwarven-castle-guard's own scenario already establishes) with a real `{kind:'dying'}` cause — genuinely fires TWICE (opponent loses 1 life / caster gains 1 life, twice), since Masamune's own `causedBy:'dying'` gate is checked against the real cause, not just presence/equip. The \"...or an emblem you own\" half of the real printed text is genuinely unmodelable — no emblem mechanism exists anywhere in this engine — so it can never actually match; a real, accepted, permanent sub-gap, documented on the `triggerDoubling` field itself, not silently dropped. `run-scenarios.mjs --slug=the-masamune`: regenerated trace.json, 1 scenario, engine-piloted. No synergy.json change this pass — this card's own existing facts (a Graveyard-presence source, a Battlefield-creature-count sink) are unrelated to this clause; whether a new fact for the doubling effect itself is worth authoring, now that real evidence exists, is a synergy-authoring question left open, not decided here. New unit tests: `functional-model/triggers.test.ts`'s own `The Masamune's own gate` describe block (3 cases: doubles on a real dying cause, does NOT double with no/wrong cause, does NOT double a different creature's own dying trigger). `vitest run functional-model`: 340/340.",
  "textCoverageAudited": false,
  "knownGaps": [
    "The \"...or an emblem you own\" half of the real printed doubling text can never actually match — no emblem mechanism exists anywhere in this engine. Real, accepted, permanent sub-gap (see `card.ts`'s own `TriggerDoublingGrant` doc comment), not something a future pass should expect to close without first building emblems generally.",
    "The other real static (\"As long as equipped creature is attacking, it has first strike and must be blocked if able\") has no representable mechanism in this engine (no attacker-declaration-time keyword-grant-plus-forced-block concept) — stays `staticAbilities` text only, unrelated to gap #13's own closure."
  ]
}
