{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-13",
  "notes": "2026-09-13 (real cast-lifecycle fix, the fin/28 (paladin-s-arms) regression the user directly reported: \"should have been cast, onEnter trigger doesn't magically trigger\"): this card's own top-level `trigger:'onEnter'` had the same \"mythical enter\" problem dragoon-s-lance's own sibling regression documents — starts `self` already on the Battlefield, skips `cast`/`enters` outright. Fixed with the same real `Scenario.forceCast` mechanism (harness.ts): `forceCast: true` now forces a genuine `cast`(Hand)->`enters`(Battlefield) lifecycle despite this card's own `activationCost` (Equip {4}), and `'onEnter'` moved from the top-level `trigger` field into the front of `sequence` (`sequence: ['onEnter', {activate: true}]`), preserving the existing chained Equip {4} activation demonstration unchanged otherwise. `review` stays 'ai' (no reset needed, was already ai). Regenerated trace.json (`run-scenarios.mjs --slug=machinist-s-arsenal`) — confirmed real `cast`/`enters` log lines now present ahead of the trigger/equip sequence. verify-synergy.mjs scoped: OK.\n\n2026-09-12 (ENGINE_GAPS.md gap #14's own remaining sub-gap): the 'is an Artificer' TYPE-grant half is now real, executable machinery — `definition.ts` gained `continuousTypeGrants: [{types:['Artificer'], includeSelf:false, equippedBySelf:true}]` (`card.ts`'s new sibling field to `continuousKeywordGrants`, read live by `state.ts`'s new `effectiveSubtypes`/`hasSubtype`). The '+2/+2 for each artifact you control' PUMP half deliberately STAYS text-only/un-modeled — checked and confirmed this is a genuinely different, harder gap than dragoon-s-lance's own flat +1/+0 (which IS now real via the new `continuousPTGrants`): this card's own bonus is a live, board-state-SCALED count (`SVar:X:Count$Valid Artifact.YouCtrl/Times.2`, ../mtg-forge's own machinists_arsenal.txt), and `continuousPTGrants` is deliberately a plain fixed `{power,toughness}` number pair, not a `Computed`-style formula — this card's own pump fact stays exempted (verify-synergy.mjs's shape-scoped `isEquippedPTGrantFact`, replacing the old per-name pump exemption) for a REAL, still-open reason (variable-amount CDA, same class as Gaelicat's/Magitek Infantry's own threshold gaps), not the 'no pipeline at all' reason it used to be. The grantType fact also stays evidence-exempted regardless (this card's own `scenarios.ts` is a plain `harness.ts` Scenario[], same structural wall as every sibling card here) — `isEquippedTypeGrantFact` (shape-scoped, replacing the old per-name grantType exemption) covers it. `review` was already 'ai' (no reset needed). verify-synergy.mjs scoped: OK. Regenerated trace.json (`run-scenarios.mjs --slug=machinist-s-arsenal`) — unchanged shape (harness.ts's own scenario pipeline doesn't copy the new grant fields onto `RealCard` at all, same pre-existing limitation `continuousKeywordGrants` already has).\n\n2026-09-12 (earlier): migrated to the unified Fact/annotations model (fin/23, continuation of the fin/1-20 rollout), following dragoon-s-lance's own just-updated real template (same Job-select ETB, same '+N/+N-and-is-a-Type in addition to its other types' clause shape, same Equip {4} activated ability). Real oracle text (data/fin/fin_scryfall.json, collector_number 23): \"Job select (When this Equipment enters, create a 1/1 colorless Hero creature token, then attach this to it.)\\nEquipped creature gets +2/+2 for each artifact you control and is an Artificer in addition to its other types.\\nMachina — Equip {4} ({4}: Attach to target creature you control. Equip only as a sorcery.)\". Mana cost {4}{W}, type line \"Artifact — Equipment\" — both verified against fin_scryfall.json, unchanged from the pre-existing definition.ts. 2 pre-existing legacy facts (1 source token-creation, 1 sink wants-creature-to-equip, both old id/sourceText/highlight shape, no annotations) -> 5 facts (4 source, 1 sink) after adding baseline self-cast/self-enters and 2 new real facts for the previously-unmodeled static clause.\n\nself-cast: {event:'cast', from:'Hand', target:'self', value:-1}, typeLine-anchored ('Artifact'). self-enters: {event:'entersBattlefield', to:'Battlefield', subject:'self', target:'self', value:-1} — carries BOTH subject and target, same as dragoon-s-lance's own, so this Equipment's own real entering satisfies TYPE-CONSTRAINED Artifact/Equipment-aware sinks. Job-select token-creation fact converted to {event:'entersBattlefield', to:'Battlefield', controller:'you', subject:{token:'c_1_1_hero'}, value:1} — same real shape aerith-rescue-mission/dragoon-s-lance's own migrated token fact. Equip sink converted from {zone:...} to {to:'Battlefield', controller:'you', types:{has:['Creature']}}, value:1 unchanged — find-synergies.mjs confirms its own match set (100 lines, X-->Machinist's Arsenal direction) is byte-identical before/after.\n\nThe static clause ('Equipped creature gets +2/+2 for each artifact you control and is an Artificer in addition to its other types.') is now split into 2 real, honest-but-structurally-inert Facts, reusing dragoon-s-lance's own just-landed vocabulary rather than leaving it text-only: {event:'pump', target:{equippedBySelf:true}, value:-1} and {event:'grantType', type:'Artificer', target:{equippedBySelf:true}, value:-1}. No `power`/`toughness` sub-fields on the pump fact — deliberately, per the per-artifact-count SCALING factor being genuinely uncomputed (no live-recalculated CDA/layer-7c pipeline anywhere in this model for an Equipment's static bonus flowing to whatever it's attached to; checked card.ts's animate dispatch, self-only). This matches adelbert-steiner's own precedent for a scaling per-count pump ({event:'pump', target:'self', value:1}, no power/toughness), NOT dragoon-s-lance's own FLAT +1/+0 pump (which does carry real power:1/toughness:0, since that magnitude is fixed and known) — checked both precedents before choosing, per the task's explicit instruction not to invent a richer shape unprompted. No `continuousKeywordGrants`/Flying entry — this card's oracle text has no flying clause at all (checked fresh against fin_scryfall.json), so that half of dragoon-s-lance's own migration doesn't apply here.\n\nNo 'equip' event fact added (the actual attach action, both via Job select's auto-attach and the Equip {4} activated ability, stays covered by the existing parked fn:'equip' — PARKED_ACTION_FNS — same as every other Equipment in the pool).\n\nverify-synergy.mjs: added 2 new name-scoped exemptions (`card.name === \"Machinist's Arsenal\" && p.event === 'pump'`/`'grantType'`) mirroring dragoon-s-lance's own exact exemption pair — same real documented engine gaps, zero possible trace evidence. Added 'machinist-s-arsenal' to scripts/annotation-coverage.mjs's ANNOTATED_CARD_SLUGS (appended after a concurrent peer session's own 'gaelicat', no conflict). New annotations-authoring.json (6 entries, positionally aligned with synergy.json's 5 source + 1 sink) baked via compute-annotations.mjs — output confirmed byte-identical to a hand-computed version checked first. scenarios.ts left unchanged (already abstract-count-based, `you: {creaturesCount, artifactsCount}`, no named filler cards to check — per this task's own explicit 'not checking scenarios, only facts' instruction).\n\nReal find-synergies.mjs before/after (isolated via a synergy.json swap + grep -F \"Machinist's Arsenal\", not a stale git-HEAD diff): outgoing (Machinist's Arsenal as source) 12 -> 54 lines (net +42) — same 3-part breakdown dragoon-s-lance's own diff established: 12 relabeled (old bare 'battlefield presence' -> 'enters the battlefield', same underlying token-ETB match), 12 duplicate-but-real (the same 12 targets gain a second line via self-enters' own independent Artifact-entering match), 30 genuinely new (self-enters' own real Artifact type now satisfies real TYPE-CONSTRAINED Artifact/Equipment-aware wants pool-wide — Adelbert Steiner, Ahriman, Airship Crash, Beatrix, Coliseum Behemoth, Edgar King of Figaro, Elixir, Elrond Moon-Reader, Fate of the Sun-Cryst, Gaelicat, Gilgamesh Master-at-Arms, Golbez Crystal Collector, Judgment Bolt, Lunatic Pandora, Midgar City of Mako, Namazu Trader, Omega Heartless Evolution, Phantom Train, Raubahn, Relm's Sketching, Reno and Rude, Ring of the Lucii, Sidequest: Hunt the Mark, Slash of Light, Stolen Uniform, Suplex, The Gold Saucer, Ultima, Unexpected Request, Venat, Weapons Vendor). Incoming (X-->Machinist's Arsenal) byte-identical, 100 lines both times. Verified: verify-synergy.mjs scoped (machinist-s-arsenal: OK, 0 hard/soft) and full pool (314 v2 cards checked, 1 pre-existing unrelated hard failure on gaelicat from a concurrent sibling session, not touched here). npx vitest run functional-model: 238/238 passed (unchanged). Note: functional-model/cards/machinist-s-arsenal/trace.json shows an unrelated in-flight diff (id/equipmentId fields on its 2 equip log lines) picked up mid-task from a concurrent sibling session's own pool-wide trace regeneration sweep — not touched or caused by this task, left as-is per the 'file changed on disk, that's usually deliberate' convention.",
  "textCoverageAudited": true,
  "knownGaps": [
    "2026-09-12 (gap #14 partially closed, real distinction confirmed): the 'is an Artificer' type-grant half is now REAL machinery (continuousTypeGrants) — no longer a gap. The '+2/+2 for each artifact you control' pump half STAYS a real, genuinely-open gap, but for a DIFFERENT reason than before: it's not that no pipeline exists (dragoon-s-lance/paladin-s-arms/crystal-fragments/white-mage-s-staff/sage-s-nouliths' own flat +N/+N clauses now have one), it's that THIS card's own bonus is a live, board-state-scaled variable amount, which the new fixed-delta continuousPTGrants field structurally can't represent — same class as Gaelicat's/Magitek Infantry's own threshold-CDA gaps, would need a `Computed`-style amount on continuousPTGrants to close for real. dark-knight-s-greatsword/buster-sword/lion-heart/black-mage-s-rod (fixed +N/+N shape, not this card's variable one) are real, ready candidates for the SAME continuousPTGrants migration dragoon-s-lance etc. just got — not swept in this pass.",
    "The Job select auto-attach ('...then attach this to it') and the Machina Equip {4} activated ability's own attach action are both real, executed (actions.equip), but carry no Fact/event vocabulary — 'equip' stays on verify-synergy.mjs's PARKED_ACTION_FNS list pool-wide (no card in the corpus wants an 'equip' event today); only the token-creation half of Job select and the equip sink's own 'wants a creature to attach to' half are represented as Facts.",
    "2026-09-12 scenario consolidation (user: '1st scen is enough'): scenarios.ts trimmed from 2 down to 1, chaining the real Equip {4} activation onto the Job-select ETB scenario via `sequence:[{activate:true}]` (same corrected 1-scenario shape paladin-s-arms/sage-s-nouliths/white-mage-s-staff already use) rather than leaving a bare single scenario, because a bare drop of the second scenario produced a real verify-synergy.mjs hard failure (the 'wants a creature you control' sink specifically needs a `read:getCreaturesInPlay` trace line, which only the Equip {4} activation's own `chooseTarget(ctx.you.getCreaturesInPlay())` call produces — the Job select trigger's own Hero-token creation is a real creature entering the battlefield but does not satisfy that specific read-based check). The dropped scenario's own `artifactsCount: 1` setup demonstrated nothing an engine trace could show either way — the per-artifact-count SCALING factor on the static pump clause has no live-recalculated CDA/layer-7c pipeline anywhere in this model (see the first knownGaps entry above and definition.ts's own comment), so varying the artifact count was never real evidence for anything; dropped per the user's explicit 'still trim regardless' instruction, not preserved."
  ],
  "oracleTextSnapshot": "Job select (When this Equipment enters, create a 1/1 colorless Hero creature token, then attach this to it.)\nEquipped creature gets +2/+2 for each artifact you control and is an Artificer in addition to its other types.\nMachina — Equip {4} ({4}: Attach to target creature you control. Equip only as a sorcery.)",
  "reviewedAt": "2026-09-11"
}
