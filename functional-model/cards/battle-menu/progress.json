{
  "enrichment": "ai",
  "review": "human",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-11",
  "notes": "2026-09-11 (later): added a real SOURCE `{event:'pump', target:{types:{has:['Creature']}}, targeted:true}` fact for the Ability mode's own \"target creature gets +0/+4\" — `event:'pump'` just promoted off `verify-synergy.mjs`'s `PARKED_ACTION_FNS` to real vocabulary (user's own explicit ask). Real evidence: this card's own `abilityMode` scenario already logs a genuine `fn:'pump'` line (targets the opponent's real `w_1_1_cat` token) — no scenario change needed. Oracle-anchored (\"gets +0/+4\"). `find-synergies.mjs` diff (isolated to just this fact): zero matches gained/lost — no sink in the pool wants `event:'pump'` yet. `knownGaps` below (which flagged exactly this) is now RESOLVED, removed. 2026-09-11: migrated to the unified Fact/annotations model (id/sourceText/highlight moved off synergy.json into a new annotations-authoring.json; compute-annotations.mjs run, all 8 facts carry real annotations). Real oracle text (fin/9, Instant, 'Choose one'): Attack (create Knight token), Ability (pump a target creature), Magic (destroy target creature power>=4), Item (gain 4 life). attack-token reshaped from bare Battlefield presence to a real to:'Battlefield'+event:'entersBattlefield' zone-change fact (token genuinely enters). Added a baseline self-cast (typeLine-anchored 'Instant') per the summon-bahamut/adelbert-steiner/aerith-rescue-mission convention. Re-anchored the pre-existing self-graveyard baseline fact from a wrong 'Sorcery' comment (this card is an Instant) to the real typeLine 'Instant'. Removed the old magic-destroy-graveyard-opp fact (its own comment admitted the controller:'opp' assumption had no real textual basis) and merged it with magic-destroy into ONE dies-consequence fact (from:'Battlefield', to:'Graveyard', target={Creature, power>=4}, targeted:true, no controller -- the real text has no controller restriction, so this also gained real matches the old controller:'opp'-scoped fact had wrongly excluded). Added a separate destroy-act ACT fact (bare event:'destroy', same target filter, targeted:true) alongside the dies consequence, mirroring summon-bahamut's own destroy-act/dies split (CR 701.6: destroying is conditional/preventable via indestructible/regeneration, so the ACT stays a bare tag while the guaranteed dies consequence carries from/to). Known, documented gap (not fixed, out of this migration's per-card scope): the Ability mode's pump effect ('Target creature gets +0/+4 until end of turn') has NO source fact -- event:'pump' is an explicitly PARKED_ACTION_FN in verify-synergy.mjs (no fact vocabulary designed for it pool-wide yet); the want side is already covered by the pre-existing wants-target-creature-pump sink.",
  "textCoverageAudited": true,
  "knownGaps": [],
  "oracleTextSnapshot": "Choose one —\n• Attack — Create a 2/2 white Knight creature token.\n• Ability — Target creature gets +0/+4 until end of turn.\n• Magic — Destroy target creature with power 4 or greater.\n• Item — You gain 4 life.",
  "reviewedAt": "2026-09-11"
}
