{
  "enrichment": "ai",
  "review": "ai",
  "verifySynergy": "pass",
  "lastVerified": "2026-09-16",
  "notes": "2026-09-16 (AI-fact-elimination sweep, fin/48, re-examined per this task's own standing 'try hard before declaring bespoke' instruction): re-ran `apply-recognizers.mjs combat-tutorial` — confirms the same `drawCard-effect-structural` decline as before (its own suppressed-mismatch log: expected imperative \"Draw two cards\" not found, real text is third-person \"Target player draws two cards\"). Checked whether this is really just a recognizer-template gap (fixable by widening the regex to also accept \"draws\") or a deeper engine one: it's the LATTER — even a widened regex could only assert `event:'drawCard'` with no `targeted`/`target` field, because `card.ts`'s own `{kind:'drawCard', amount?}` Effect type has no target-player field at all for any recognizer to read structurally (confirmed by re-reading `card.ts`'s real type directly, not from memory) — the SAME gap `loseLife`/`discard`'s own `EffectOwner` field already solves for THOSE effect kinds, per `definition.ts`'s own comment, but `drawCard` has no equivalent. `drawCard-effect-structural.ts`'s own module doc comment already confirms `combat-tutorial` is the ONLY real pool card hitting this exact 'third-person draws, any player' shape (checked there, not re-derived). Conclusion unchanged: genuinely bespoke pending a new engine-core `drawCard` target-player field, not closable via a recognizer widening alone. Worth flagging: this specific gap isn't yet in `ENGINE_GAPS.md`'s own master list (only documented in this recognizer's doc comment + this card's own comment/progress.json) — noted for whichever future engine-core pass reconciles that, not fixed here (out of this lane, and a documentation-completeness nit rather than a blocking issue). No file changes made.\n\n2026-09-16 (fin/26-50 re-triage): re-verified fresh, 0 hard failures. 1 AI fact remains (\"target player draws two cards\") — already a named, documented decline in drawCard-effect-structural.ts's own module comment (real text is third-person \"draws,\" targeting ANY player, but card.ts's `drawCard` Effect kind always draws for `ctx.you` with no target-player field at all) — a real, doubly-flagged (recognizer file + definition.ts comment) engine-core gap, not new. No action taken here.\n\n2026-09-12: migrated to the unified Fact model (SYNERGY_DESIGN.md). 5 facts total, all annotation-backed (annotations-authoring.json added, compute-annotations.mjs run, slug added to ANNOTATED_CARD_SLUGS): source self-cast ({event:'cast', from:'Hand', target:'self'}, typeLine-anchored, 'Sorcery'), source self-graveyard ({to:'Graveyard', controller:'you', subject:'self'}, typeLine-anchored, no `from` since the real origin is the deliberately-invisible Stack per the standing rule), source draw-target ({event:'drawCard', targeted:true} — deliberately NO `controller`, since real oracle text is 'Target player draws two cards', any player, not just you; controller on a drawCard fact means 'who draws' per synergy.ts's own convention, so omitting it correctly leaves the side unconstrained rather than misstating it as you-only), source counter-target ({event:'putCounter', counterType:'+1/+1', controller:'you', target:{types:{has:['Creature']}}, targeted:true} — 'up to one target creature you control', modeled with plain `targeted:true` and no separate optional/upToOne field, matching the established summon-bahamut 'destroy up to one target nonland permanent' precedent: 0-or-1 is inherent to real targeting, not a distinct schema concept), sink wants-creature-target ({to:'Battlefield', controller:'you', types:{has:['Creature']}} — needs a legal creature to target for the counter half). compute-weights.mjs --slug=combat-tutorial run (all facts have no numeric constraint/amount field of their own, so all computed to the neutral floor, value 1). Collapsed scenarios.ts from 2 to 1 per the 2026-09-12 'scenario count defaults to 1' standing rule — the dropped scenario was exactly the anti-pattern the rule's own magitek-infantry example calls out (a same-single-mode 'no legal creature to target, no-op' edge case, not a real second mode); regenerated trace.json via run-scenarios.mjs --slug=combat-tutorial. verify-synergy.mjs: 0 hard failures scoped and full pool (315 v2 cards; the pool's other concurrent hard-failure counts fluctuating card-to-card during this session are unrelated, other in-flight migrations, not this card). vitest run functional-model: 238/238 unchanged. find-synergies.mjs diff, isolated to 'Combat Tutorial' lines only (full-pool diff has heavy unrelated noise from concurrent sessions): net ZERO interactions gained or lost (123 lines before and after) — the only change is a description-label rename on the 13 existing graveyard-presence matches, 'graveyard presence' -> 'moves to graveyard' (expected fallout of authoring self-graveyard as a real `to:'Graveyard'` SOURCE movement instead of the old legacy bare `zone:'Graveyard'` presence tag, same rendering change every other migrated-this-week card's own self-graveyard fact already got). The draw-target and counter-target facts add real vocabulary (`event:'drawCard'` targeted, `event:'putCounter'` targeted-with-type-constraint) but create no new matches today since no sink in the pool currently wants either shape from this card specifically (counter-target's own `putCounter` still only matches the 2 pre-existing 'counters' lines, Aerith Gainsborough/Zack Fair, unchanged). Known, accepted, documented engine gap (not fixed here, same class as auron-s-inspiration's own pump gap): `drawCard`'s effect kind has no target-player parameter (always draws for `ctx.you`), so real trace evidence for draw-target can only ever show the `player:'you'` case even though the fact is authored to the real 'any player' oracle text (targeted:true, no controller) — see definition.ts's own comment on this specific gap.",
  "textCoverageAudited": true,
  "knownGaps": [
    "drawCard effect kind has no target-player selection primitive (card.ts) — this card's 'Target player draws two cards' always resolves as you drawing in the engine; the draw-target fact is authored to the real oracle text (targeted, no controller restriction) regardless, per the documented honest-fact/engine-gap convention."
  ]
}
