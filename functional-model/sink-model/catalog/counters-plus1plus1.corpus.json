{
  "total": 9,
  "passing": 9,
  "cases": [
    {
      "case": "PRODUCER: plain self-targeted putCounter, counterType '+1/+1'",
      "mirrors": "Exemplar of Light (FDN #11) — real \"put a +1/+1 counter on this creature\" effect",
      "expectedVerdict": "matches",
      "note": "Mocked Creature with effects:[{kind:'putCounter', target:'self', counterType:'+1/+1', amount:1}]. Verified in counters-plus1plus1.test.ts via matchSink."
    },
    {
      "case": "PRODUCER: triggered putCounter",
      "mirrors": "a real \"when this enters, put a +1/+1 counter on it\" permanent",
      "expectedVerdict": "matches",
      "note": "Mocked creature whose onEnter trigger's own effects contain kind:'putCounter', counterType:'+1/+1' — proves trigger effects are walked, not just top-level effects. Verified via matchSink."
    },
    {
      "case": "PRODUCER: broadcast putCounterAll, counterType '+1/+1'",
      "mirrors": "a real \"put a +1/+1 counter on each creature you control\" effect",
      "expectedVerdict": "matches",
      "note": "Mocked Sorcery with effects:[{kind:'putCounterAll', predicate:'creatures-you-control', counterType:'+1/+1', amount:1}]. Verified via matchSink."
    },
    {
      "case": "PRODUCER: differently-typed counter effect (counterType '-1/-1')",
      "mirrors": "any real card that puts a -1/-1 counter instead — a genuinely different mechanic",
      "expectedVerdict": "no-match",
      "note": "Mocked Sorcery with effects:[{kind:'putCounterTarget', validType:'creature', counterType:'-1/-1', amount:1}]. Proves real discrimination on counter type, not 'any putCounter counts'. Verified via matchSink."
    },
    {
      "case": "PRODUCER: vanilla creature, no effects at all",
      "mirrors": "Serra Angel (FDN) — a real, deliberate no-sink case",
      "expectedVerdict": "no-match",
      "note": "Mocked vanilla creature. Verified via matchSink."
    },
    {
      "case": "CONSUMER: named trigger 'onCounterAdded', checked standalone from the producer query",
      "mirrors": "the real Exemplar of Light (FDN #11) shape — \"whenever one or more counters are put on this creature, draw a card\"",
      "expectedVerdict": "matches (consumer mode)",
      "note": "Mocked creature whose only trigger is named 'onCounterAdded'. Verified in counters-plus1plus1.test.ts via matchesConsumerTriggerNames."
    },
    {
      "case": "CONSUMER: differently-named trigger",
      "mirrors": "any real card whose trigger reacts to something else entirely",
      "expectedVerdict": "no-match",
      "note": "Mocked creature with a trigger named 'onAttack'. Verified via matchesConsumerTriggerNames."
    },
    {
      "case": "CONSUMER: no triggers at all",
      "mirrors": "Serra Angel (FDN) again, for the consumer recognition mode specifically",
      "expectedVerdict": "no-match",
      "note": "Mocked vanilla creature. Verified via matchesConsumerTriggerNames."
    },
    {
      "case": "CONSUMER: named trigger on the BACK face of a transforming DFC",
      "mirrors": "a transforming DFC whose back face carries the reactive trigger",
      "expectedVerdict": "matches",
      "note": "Mocked front/back face pair; the 'onCounterAdded' trigger lives only on backFace.triggers. Verified via matchesConsumerTriggerNames."
    }
  ]
}
