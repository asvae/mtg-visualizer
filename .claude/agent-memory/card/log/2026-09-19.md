# 2026-09-19

- Built the "Forge Script" card-page tab (real Forge card script, dev-only,
  live-read from the gitignored local `tmp/mtg-forge/` checkout) —
  `server/utils/forgeScript.ts` (new), `server/api/forge-script/index.get.ts`
  (new), `app/components/CardDetailTabs.vue` + `app/composables/
  useGraphStore.ts` (both edited, additive only). Deliberately re-raised the
  2026-09-11 `forge-model/` GPL-exposure deletion — flagged to the
  orchestrator first (did not implement until confirmed); user confirmed
  directly. See `topics/forge-script-dev-tab.md` for the full design/pieces
  and `topics/forge-model-deletion.md`'s new closing update.
  Verified live via Playwright against the running dev server: real content
  for `Exemplar of Light` (fdn/733) and `Fleeting Flight` (fdn/13), plus a
  genuine (not fabricated) not-found path exercised via a fetch-intercept
  test harness since this dev machine's checkout turned out to already
  cover every real FIN/FDN card name tried. `npm run typecheck` — same
  pre-existing errors as before this change (`CardStatusBucket` indexing,
  `functional-model/card.ts`/`card-status.ts`/`mana.ts`,
  `server/api/tokens/by-key.ts`), none new, none in files this task touched.
  Hit and resolved an unrelated dev-server hiccup (stale Vite transform from
  a concurrent session's mid-save of `functional-model/card.ts`) via a plain
  dev-server restart — not a project bug, noted in the topic file in case a
  future session hits the same symptom.
- Added a "Notes" tab to `CardDetailTabs.vue` (next to Card Definition/Forge
  Script) serving `functional-model/fdn-cards/<slug>/NOTES.md` via
  `FunctionalModelData.notes` (server/api/card/[set]/[number].ts, same slug
  resolution `definition.ts` already uses). New `MarkdownView.vue` component
  (reuses/extends `app/lib/markdown.ts`, which gained minimal GFM pipe-table
  support for exemplar-of-light's real table). Hit the `cardResponse.ts`
  hand-mirror gotcha again (had to add `notes` there too) plus a 3rd
  new-tab-value site, `useGraphStore.ts`'s `FUNCTIONAL_MODEL_TABS` literal
  tuple — see the new topic file. Verified live via Playwright (fdn/733
  shows real content incl. table; fin/1 shows no tab, no errors);
  `npm run typecheck` diffed against a `git stash`-bracketed baseline —
  identical error set, no new errors. Committed (`df0864c`).
