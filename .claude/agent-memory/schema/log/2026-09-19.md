
## Mocked CardDefinition name rename in counters.test.ts (Source/Sink terminology)
Renamed 7 mocked fixture `name` values in
`functional-model/sink-model/catalog/counters.test.ts` to reflect the
Source Candidate/Sink Candidate terminology (established `e927b91`):
- `'Mock Counter Creature'` → `'Mock Counter Source'` (source-only, 2
  occurrences: SOURCE CANDIDATE case + CALLABLE case)
- `'Mock Triggered Counter Creature'` → `'Mock Triggered Counter Source'`
  (source-only)
- `'Mock Counter Anthem Spell'` → `'Mock Counter Anthem Source'`
  (source-only)
- `'Mock Counter Reactor'` → `'Mock Counter Sink'` (sink-only, 2
  occurrences: SINK CANDIDATE case + CALLABLE case)
- `'Mock Back Face'` → `'Mock Back Face Sink'` (sink-only, DFC back-face
  fixture)

Left unchanged (already neutral, deliberately-neither negative/
discrimination fixtures): `'Mock -1/-1 Counter Spell'`, `'Mock Vanilla
Creature'` (×2), `'Mock Unrelated Reactor'`, `'Mock Vanilla Reactor'`,
`'Mock Front Face'` (no signal of its own — the back face carries the
sink signal). Checked `counters-plus1plus1.corpus.json` for drift —
none found, it describes fixture shapes generically, never references
the literal mock `name` field.

Pure rename, zero assertion/behavior changes. `counters.test.ts`: 12/12
pass. Full `functional-model` suite: 1339 passed/5 skipped (120 files).
`npm run typecheck`: exact known 7-diagnostic baseline, unchanged.
Committed `57d474b`.

## counters.test.ts builds its own entry via CountersSink, not the singleton import
User reacted strongly ("What's that shit?") to `counters.test.ts` importing
the pre-built production singleton `countersPlus1Plus1` from
`./counters-plus1plus1`. Changed to `import { CountersSink } from
'./families/counters'` and construct `entry` in-file via `CountersSink({
slug: 'counters-plus1plus1', counterType: '+1/+1', consumerTriggerNames:
['onCounterAdded'] })` — byte-identical to `counters-plus1plus1.ts`'s own
real config (verified by reading it), with a comment noting the two must
stay in sync. `counters-plus1plus1.ts` itself untouched, still exports the
real production singleton for `catalog/index.ts`/`card-interactions.ts`/the
server API route. Zero assertion/behavior changes — same 12/12 pass, full
`functional-model` suite 1339 passed/5 skipped (120 files) unchanged,
`npm run typecheck` same known 7-diagnostic baseline. Committed as its own
commit.

## `CountersSink(definition: CardDefinition)` — public factory signature change
Rewrote `families/counters.ts`: `CountersSink(config: CountersSinkConfig)`
-> `CountersSink(definition: CardDefinition)`, per user's "I don't care
about magical configuration file. Sink(definition). Got it?" instruction.
`CountersSinkConfig` deleted entirely. New private helpers derive
slug/counterType/consumerTriggerNames from the passed `CardDefinition`:
`deriveCounterType` (walks `effects` + `triggers[].effects`, throws if no
real counter-granting effect found), `slugForCounterType` (deterministic
sanitization, reproduces `'counters-plus1plus1'` byte-for-byte so
`sink-catalog-status.ts`'s file-lookup-by-slug convention keeps working
with zero other-file changes), `deriveConsumerTriggerNames` (investigated
"no `Trigger.on` field" as a possible reserved discriminator — confirmed
it is NOT one anywhere in the codebase, dozens of unrelated name-only
triggers exist pool-wide; used a small family-owned name allowlist,
`['onCounterAdded']`, with an `on`-field exclusion safety net instead).
Mid-task coordinator relay: user sharpened the chain to `SinkFamily
(sinkCandidateDefinition) -> (Predicates) -> Candidate Definition` — added
a named `withPredicates(counterType, consumerTriggerNames)` step (returns
`{producer, consumer}`) so the middle step is visible code, not anonymous
closure logic; did NOT rename `definition`/`candidate` params to match the
user's diagram literally (would collide with the pre-existing, differently-
scoped "Source/Sink Candidate" UI vocabulary — flagged, not silently
picked). Second coordinator relay ("throw away complete bullshit") —
confirmed scope stayed Counters-only, used as license to delete
`CountersSinkConfig` outright rather than deprecate it.

`counters-plus1plus1.ts` now imports the real `exemplarOfLight`
`CardDefinition` and calls `CountersSink(exemplarOfLight)`.
`counters.test.ts` builds an inline mock `CardDefinition` shaped like
Exemplar of Light (not importing the real one, per this file's own
"mocks inline" convention) and calls `CountersSink(mockDefinition)`; added
5 new derivation-focused test cases (slug/category/consumerTriggerNames
correctness, a `-1/-1` differently-typed driving definition, throw-on-no-
producer-effect, both `on`-field discrimination edge cases).

Updated `entry.ts`'s `SinkFamily<Config>` doc comment,
`SINK_MODEL_DESIGN.md` (new "2c" section + open-items update),
`.claude/contracts/card-schema.md` (new section 12).

Verified: `npx vitest run functional-model` 120 files/1344 passed/5
skipped (net +6). `npm run typecheck` unchanged 7-diagnostic baseline.
Live dev-server check: `GET /api/sink-catalog`'s `counters` entry and
`GET /api/card/fdn/11`'s `functionalModel.cardInteractions` both
byte-identical to pre-change (19 source candidates/1 sink candidate,
Exemplar of Light self-included, slug/category/consumerTriggerNames all
resolve identically); `/app/engine/sinks/counters` and
`/app/engine/sinks/counters-plus1plus1` both 200. `BattlefieldPresenceSink`
untouched. New topic file: `topics/countersSink-takes-carddefinition-
2026-09-19.md`.

## counters.test.ts mock CardDefinitions trimmed to minimal fields
Shrunk every mock `CardDefinition` literal in `counters.test.ts` down to
just `name` + whatever structural fields (`effects`/`triggers`/`backFace`)
the specific assertion actually reads — dropped decorative `manaCost`/
`typeLine`/`pt`/`keywords`/`activationLimit` copied from the real Exemplar
of Light print (confirmed via `families/counters.ts` + `match-sink.ts`
that neither `manaCost` nor `typeLine` affects `CountersSink`'s
producer/consumer derivation at all — `isNormalPermanent`/
`isNormalInstantOrSorcery` only gate an unrelated baseline
`entersBattlefield`/graveyard occurrence, never consulted by these tests).
Added one small local helper, `mockCard(name, overrides)`, filling the two
required-but-irrelevant `manaCost`/`typeLine` strings with `''` — no
general-purpose factory. Kept every mock's descriptive Source/Sink `name`.
Pure trim, zero assertion changes: `counters.test.ts` 17/17 pass,
`functional-model` suite 120 files/1344 passed/5 skipped (unchanged from
baseline), `npm run typecheck` unchanged pre-existing baseline (5
diagnostics, none touching this file). Committed as its own commit.

## `SinkInstance` callable returns `boolean`; `SinkFamily<Config>` returns `SinkInstance[]`
3rd real design iteration on the callable contract, two live corrections in
one task (relayed mid-task by the coordinator, not asked for directly):

1. `SinkInstance`'s call signature `(candidate, root?) => SinkMatchDetail |
   null` -> `(candidate, root?) => boolean` (producer question only —
   user's exact target: `const b = sinkInstance(candidate)`, rejected two
   compromises as "both complete bullshit"). `SinkMatchDetail` deleted
   entirely. `predicateDerived` moved to a new, separate, optional
   `SinkInstance.isPredicateDerived?(candidate, root?) => boolean` accessor
   — the one real piece of the old combined return `card-interactions.ts`'s
   `selfDirectProducerMatch` still needs and a bare boolean can't carry.
   Both `CountersSink`/`BattlefieldPresenceSink` implement it.
   `card-interactions.ts`'s `matchEntry` and `server/api/sink-catalog/
   index.get.ts`'s `instanceConsumerMatched` no longer route the CONSUMER
   question through the callable at all (whether callable or not) — they
   call `matchesConsumerTriggerNames`/`matchesConsumerTriggerOn`/
   `matchesBattlefieldPresenceConsumer` directly against the entry's own
   plain data fields uniformly (this also fully fixed
   `instanceConsumerMatched`'s pre-existing `matchesBattlefieldPresenceConsumer`
   omission for the non-callable branch — never actually mattered in
   practice since only callable entries carry that field, but now
   structurally correct either way).
2. `SinkFamily<Config>` return type `SinkInstance` -> `SinkInstance[]` (2nd
   live correction: "we need array handling here obviously"). `CountersSink`
   now derives one instance PER DISTINCT `counterType`
   (`deriveCounterType` singular -> `deriveCounterTypes` plural, `Set`-based
   dedup) instead of silently keeping only the first found. Dedup rule
   stated generally per explicit user framing: identical-effect duplicates
   collapse to one instance, any real difference gets its own — for
   Counters, `counterType` alone is the complete identity key since every
   other derived field is a pure function of it + the whole definition.
   `BattlefieldPresenceSink` — conforming edit only (still `SinkQuery`/
   `matchSink`-based internally), now returns `[sink]`. All 4 real instance
   files (`counters-plus1plus1.ts`,
   `battlefield-presence-{cats,creatures,hare-apparent}.ts`) now read
   `...(...)[0]!` — NOT destructuring (`const [x] = ...`), since a live
   `tsc --noUncheckedIndexedAccess` probe confirmed that infers `T |
   undefined`, and the Nuxt-generated tsconfig sets that flag even though
   `functional-model/tsconfig.json` itself doesn't.

Updated `entry.ts` (both type doc comments), `families/counters.ts`
(`producerPredicate` replaces `withPredicates`, `buildCounterInstance`
split out), `families/battlefield-presence.ts` (conforming edit),
`card-interactions.ts`'s `matchEntry`, `server/api/sink-catalog/
index.get.ts`'s `instanceProducerMatched`/`instanceConsumerMatched` (also
fixed a real missing import: `matchesBattlefieldPresenceConsumer` wasn't
imported at all before this pass touched that function).
`counters.test.ts`/`battlefield-presence.test.ts` rewritten for the boolean
contract; added 2 new `counters.test.ts` cases proving the array/dedup rule
(2 distinct counterTypes -> 2 instances; 2 occurrences same counterType -> 1
instance) plus `isPredicateDerived` coverage in both test files.

`SINK_MODEL_DESIGN.md`: new "2d / 3b" section (section 3's own stale
`SinkMatchDetail` prose left untouched per this doc's own "keep history
intact" convention, same treatment section 2's stale `SinkFamily` line
already got). `.claude/contracts/card-schema.md`: new section 13. New topic
file: `topics/sink-instance-boolean-return-2026-09-19.md`.

Verified: `npx vitest run functional-model` 120 files/1347 passed/5 skipped
(net +3 vs 1344/5 baseline, zero shrink). `npm run typecheck` unchanged
7-diagnostic baseline (checked before AND after — identical set both
times), zero new. Live dev server: `GET /api/sink-catalog` — `counters`
19/1, `battlefield-presence` 111/4, both byte-identical to documented
pre-change numbers; `GET /api/card/fdn/11` "+1/+1" row still count:19,
self:true; `GET /api/card/fdn/6` (Claws Out) "Creatures" count:111, "Cats"
count:10; `/app/engine/sinks/counters`,
`/app/engine/sinks/counters-plus1plus1`, `/app/engine/cards/fdn/11` all
200.

Wording-only pass: scrubbed leftover "corpus" jargon from
`sink-model/catalog/*.test.ts` (header comments + `describe()` titles) —
the per-test-file `*.corpus.json` fixture-manifest design is gone (mocks
inlined), word survived stale. Left real `.corpus.json` filename
references and the "Predicate corpus uses mocks" memory-entry citation
alone (still real artifacts, not jargon). Folded in a coordinator-flagged
follow-up in `counters.test.ts`: stripped decorative real-card flavor
("Angel"/"Blight", from Exemplar of Light's own creature type, irrelevant
to a +1/+1-counter-only test) from mock names, and renamed the
`mockExemplarOfLight` variable to `mockDrivingDefinition`. `npx vitest run
functional-model/sink-model` 9 files/122 tests green, no logic changed.
Committed as 83ba859.

## counters.test.ts: real-card-cited fixtures for arbitrary "Mock ..." ones
Per user-supplied fixtures (Fleeting Flight/Exemplar of Light), verified
both against `data/fdn/fdn_scryfall.json`'s real `oracle_text` before use
— confirmed exact wording, and corrected a citation error: the file's
existing header/test comments (pre-existing, not introduced by this task)
say "Exemplar of Light, FDN #11" in 3 places, but the real
`collector_number` in `fdn_scryfall.json` is **733**, not 11 (only one
`Exemplar of Light` entry exists in the pool, set `fdn`). Used the correct
`#733` in this task's own new/edited citations; did NOT touch the 3
pre-existing wrong "#11" mentions (header comment + 2 old test
descriptions) — out of scope for this task, flagged here for whoever
touches this file next. `functional-model/fdn-cards/exemplar-of-light/
definition.ts`'s own two triggers confirmed byte-shape-identical to what
this file already mocks.

Added one new SOURCE CANDIDATE case (Fleeting Flight, FDN #13, a real
`putCounterTarget` on `target creature` — non-self). Directly read
`families/counters.ts`'s `producerPredicate` controller-compatibility
check first to confirm the claim before trusting it: `occ.controller ??
(occ.subject === 'self' || occ.target === 'self' ? 'you' : undefined)`,
then `!controller || controller === 'you'` — a targeted, non-self
`putCounterTarget` occurrence has no `controller` and no `self`
subject/target, so it resolves `undefined` -> treated as compatible.
Confirmed true, matches the task's own pre-stated empirical result.
Every SOURCE CANDIDATE case before this one only ever used
`target: 'self'`, so this is genuinely new covered behavior, not a
duplicate assertion.

Replaced the two `'Mock Counter Sink'`-named positive-match fixtures
(the SINK CANDIDATE "matches whose own named trigger is..." case, and the
CALLABLE "sink-candidate-only card" case) with the real Exemplar-of-
Light-consumer-half shape (`{name:'onCounterAdded', activationLimit:1,
effects:[{kind:'drawCard', amount:1}]}` instead of a bare
`effects: []`) — a genuine drop-in equivalent, `matchesConsumerTriggerNames`
only reads the trigger `name`, so behavior is unchanged. Left the
DFC-back-face positive-match case (`'Mock Back Face Sink'`) and every
negative case (`-1/-1` mismatch, differently-named trigger, no triggers,
vanilla creature) untouched — the DFC case tests a genuinely different
structural signal (face-plurality routing) not specifically named by the
task, and negative cases can't be real-card-sourced by definition.

Verified: `npx vitest run functional-model/sink-model/catalog/
counters.test.ts` 20/20 pass (net +1 case). Full `npx vitest run
functional-model` 120 files/1348 passed/5 skipped (net +1, zero
regressions). Committed as its own commit.

## New `Trigger.on:'counterAdded'` + `counterAddedMatch.counterType` (real schema addition)
Closed the real gap `families/counters.ts`'s own header comment had been
flagging since 2026-09-18 (task dispatched by orchestrator, scoped to
`card.ts`/`exemplar-of-light`/`CountersSink`'s own derivation only — NOT
the live `counters.test.ts` rewrite a concurrent session owns). Real Forge
citation: `TriggerCounterAdded`/`TriggerCounterAddedOnce`
(`res/cardsfolder/e/exemplar_of_light.txt`: `Mode$ CounterAddedOnce |
CounterType$ P1P1 | ValidSource$ You | ValidCard$ Card.Self |
ActivationLimit$ 1`). Verified the file's own "tracked — ENGINE_GAPS.md"
citation was wrong (zero hits, grepped) — corrected in that comment.

Landed: `on: 'counterAdded'` (self-only scope, mirrors `'dies'`) +
`counterAddedMatch?: { counterType?: string }` (omitted = any type, real
Forge wildcard semantics). Mid-task coordinator relay narrowed scope
first (confirmed my design already matched — only `counterType` needed
representing, not controller/quantity/target), then raised a real design
fork: should a bare `on:'counterAdded'` implicitly default to `+1/+1`
(mirroring `enter`/`otherPermanentEnters`)? Investigated and rejected:
Forge's own un-parameterized case is a genuine wildcard, not an implicit
+1/+1 assumption, and the pool has real non-+1/+1 counter types in active
use (checked distribution: 21/26 FDN, 25/37 FIN producer effects are
+1/+1, but `stun`/`revival`/`loyalty`/`incubation`/`SOUL`/`LORE`/`CHARGE`/
`finality`/`blight`/`Indestructible` are all real too) — kept the
parameterized shape, documented the full reasoning on both fields.

`exemplar-of-light/definition.ts` updated (name kept, still load-bearing).
Re-gated: `pipeline-status.json` purple → blue. `justification.json`
re-verified OK, no rewrite needed. `CountersSink` (`families/counters.ts`)
widened additively: `deriveCounterTypes` now also reads
`counterAddedMatch.counterType` off a consumer trigger (unions with the
existing producer-effect walk, only throws when both are empty); new
`deriveConsumerTriggerOn` mirrors `etb.ts`'s `consumerTriggerOn`
precedent, already read generically by `card-interactions.ts` with zero
further plumbing. New `engine-support-registry.ts` entry
(`counter-added-trigger-not-enforced`, ordinary Ward pattern, no
`gapRef`) — `engine-support-registry.test.ts`'s exact-id-list assertion
updated to match.

Could NOT perform the required `engine` consult directly — this subagent
invocation had no `SendMessage`/`ListAgents` tool in its toolset (only
Read/Edit/Write/Bash/SubagentHandback). Classified by strong precedent
instead (identical shape to 3 prior schema-only Ward-pattern additions,
none of which got a new `ENGINE_GAPS.md` entry either) and flagged this
explicitly to the orchestrator in the handback report to relay/confirm.

Did not touch `counters.test.ts`/`counters.test.ts.old` (concurrent
session's own live WIP) — confirmed backward compat instead by copying
the git-historical pre-rewrite 289-line test content to a throwaway
filename, running it against the widened `families/counters.ts` (20/20
pass), deleting the throwaway file. Committed via `git commit --
<explicit paths>` to avoid sweeping the pre-existing staged `.old` file or
the unstaged live WIP file into this commit. Verified:
`npx vitest run functional-model` 119/120 files green (1328 passed, 5
skipped) — the one failure is the concurrent session's own known-
incomplete WIP mock (still uses the old no-`on` shape, fails identically
before/after this change, not a regression). `npm run typecheck`:
identical pre-existing 7-diagnostic baseline (line numbers shifted from
added comments only). New topic file:
`topics/counter-added-trigger-2026-09-19.md`. Updated
`.claude/contracts/card-schema.md` (new dated section, appended).
Committed `8214a0c`.
