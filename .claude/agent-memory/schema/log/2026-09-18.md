- Hub created via `engine`/`schema` specialist split (orchestrator-driven, Part 1 of the split+engine-support plan): 7 topic files migrated from `engine/topics/` (fdn-sink-model-open-items, fdn-cheap-tier-vocabulary-mistakes, fdn-static-abilities-gate-rule, fdn-schema-tightness-coverage-manifest, recognizer-wiring-checklist, sink-predicate-corpus-mocking-policy, combinator-dsl-open-followups), content unchanged, plus a fresh `current.md` index.
- Built `functional-model/engine-support-registry.ts` (Part 2 of the split+engine-support plan) — new per-card `on`/`off` crossref, seeded with the confirmed real Ward-not-enforced case, distinct from `pipeline-status.json`'s own gray/purple/blue axis. Wired into `pipeline-status.ts` (`PipelineStatusFile.engineSupport`, set only on blue/purple, never gray) and `validate-card-definition.mjs` (carries the resolved `CardDefinition` through on ok/capacity-gap results); `gate-and-write-status.mjs` needed no changes. Added `engine-support-registry.test.ts` (6 cases). Authored a real, span-verified `justification.json` for `zul-ashur-lich-lord` (previously gray/no-manifest) so both confirmed real Ward users (it + `sire-of-seven-deaths`) could actually reach purple and carry `engineSupport: 'off'` — this is a real, non-fabricated authoring pass, but it shifts the pool-wide gate outcome from the previously-recorded 2 blue/3 purple/95 gray to 2 blue/4 purple/94 gray (see `topics/engine-support-registry.md` for the full tension/reasoning). Full pool re-gated via `gate-and-write-status.mjs --all`; `npm run vitest run functional-model` (122 files, 1305 passed/5 skipped) and `npm run typecheck` both clean of any NEW errors (7 pre-existing diagnostics, all in files this task never touched, confirmed unchanged before/after). Committed as its own commit.
- Schema-completeness pass (separate task): surveyed all 20 FDN cards with real `missingSchemaFunctionality` entries, clustered, closed the clean/multi-card clusters purely additively in `card.ts`/`combinator.ts`: `KeywordCost` (Ward/Kicker non-default cost, sibling field not a `Keyword`-shape change), `'Kicker'`/`'Prowess'`/`'CantBlock'` new `Keyword` members, `BoardStateCondition` (`Trigger.condition`/`ContinuousGrantTargeting.condition` — Threshold/Raid/counter-gate), `Trigger.on:'otherPermanentEnters'` (+`otherPermanentEntersMatch`), `combinator.ts`'s `Query.source:'graveyard'` (genuinely engine-enforced, not Ward-pattern). 5 new `engine-support-registry.ts` entries + matching test coverage. Did NOT touch any `fdn-cards/*/definition.ts` (out of scope) — `divine-resilience`'s masked Kicker `tsc` error fixed for free as a side effect, needed zero edits. Left `crystal-barricade`/`herald-of-eternal-dawn`/`arbiter-of-woe`/`blasphemous-edict`/MayPlay gaps open (single-card or genuinely-large, not clustered). `npx vitest run functional-model` (122/1314 passed) and `npm run typecheck` (same 7 pre-existing diagnostics, confirmed via git-stash A/B) both clean. See `topics/schema-completeness-pass-2026-09-18.md` for full detail. Committed as its own commit(s).
- Follow-up authoring pass (separate task): re-authored the 13 real `fdn-cards/*/definition.ts` the schema-completeness pass's new vocabulary unblocked, each verified against `data/fdn/fdn_scryfall.json`, each with a fresh/updated span-verified `justification.json` (`verify-coverage-justification-cli.mjs` OK per-card as authored, not batched at the end). `sire-of-seven-deaths`/`zul-ashur-lich-lord`: `keywordCosts` for Ward's non-default "Pay N life" cost, old gap entry removed (sire now `blue` with 0 reasons; zul-ashur stays `purple` for its unrelated MayPlay gap only). `sun-blessed-healer`: `keywords:['Kicker']` + `keywordCosts` for "{1}{W}". `divine-resilience`: confirmed untouched per task instruction (still `gray`, no manifest — out of scope). `elementalist-adept`/`drake-hatcher`: `keywords:['Prowess']` (auto-fire half stays the already-tracked `ENGINE_GAPS.md` trigger-doubling gap, not reinvented; drake-hatcher stays `purple` regardless, for its own SEPARATE pre-existing name-only-trigger cap, `onCombatDamageToPlayer`, unrelated to Prowess). `vampire-soulcaller`: `keywords:['CantBlock']`. `crypt-feaster`/`midnight-snack`/`gutless-plunderer`: `Trigger.condition` (`graveyardCountAtLeast`/`attackedThisTurn`). `billowing-shriekmass`: `continuousPTGrants[].condition` (`graveyardCountAtLeast`). `cephalid-inkmage`: modeled via a NEW real fit — self-only `continuousKeywordGrants:[{keywords:['Unblockable'], includeSelf:true, condition:{graveyardCountAtLeast}}]` (`'Unblockable'` is the real "can't BE blocked" fact, already a pre-existing `Keyword`) — flagged a genuine SEPARATE pre-existing `engine.ts` gap discovered while authoring this: `canBlock` reads `attacker.keywords` (raw) for its Unblockable check, never `effectiveKeywords(state, card)`, so even an unconditional `continuousKeywordGrants`-granted Unblockable wouldn't affect blocking legality today — not fixed (engine.ts is engine-owned), just surfaced in the card's own comment for `engine` to see. `skyknight-squire`: BOTH new mechanisms at once — `Trigger.on:'otherPermanentEnters'`/`otherPermanentEntersMatch:{sameController:true}` (closes its own previously-SILENT, never-declared gap) + self-only `continuousKeywordGrants`/`continuousTypeGrants` with `condition:{selfCounterCountAtLeast}` for the flying/Knight grant. `arahbo-the-first-fang`: added a second real trigger (`onOtherCatEnters`, `on:'otherPermanentEnters'`, `otherPermanentEntersMatch:{subtype:'Cat',nonToken:true,sameController:true}`) alongside its pre-existing self-only `onEnter` — a clean additive swap, not a rewrite, closing its own previously-declared gap fully (now `blue`, 0 reasons) — its own justification.json's old "rules"-type gap entry for the "or another nontoken Cat" middle is now a real "definition"-type entry pointing at this new trigger. Pool re-gated via `gate-and-write-status.mjs --all`: 13 blue / 3 purple / 84 gray (baseline was 2/4/94) — net +11 blue, -1 purple (arahbo closed), -10 gray. `npx vitest run functional-model` (122/1314 passed, 5 skipped, unchanged) and `npm run typecheck` (same 7 pre-existing diagnostics, unchanged) both clean. Committed as its own commit.
- Comment-cleanup sweep (separate task, orchestrator-dispatched): moved authoring narration out of all 150 `fdn-cards/*/definition.ts` into new per-card `NOTES.md` files, per new convention (real card-text comments stay only directly above genuinely complex `Effect`s — program/combinator trees, non-no-op `custom` closures, `modal` branches, `condition`-gated continuous grants; everything else — vocabulary-choice rationale, gap-attribution narration, boilerplate "moved to justification.json" trailers, self-evident field-shape comments — relocated or deleted). 36/150 cards had zero comments to relocate (still got a brief placeholder NOTES.md); 114/150 had a definition.ts edit (1 more, dauntless-veteran, got only a NOTES.md — its one existing comment already matched the new convention as-is). Pure relocation, zero logic changes: confirmed via a full `gate-and-write-status.mjs --all` re-run (every card's `pipeline-status.json` content byte-identical modulo `computedAt`, which was reverted rather than committed), `npx vitest run functional-model` (122/1314 passed, unchanged), and `npm run typecheck` (same known 7 pre-existing diagnostics, none under `fdn-cards/`). Surfaced (flagged in-file, NOT fixed — out of scope) several real gaps documented only as prose with no `missingSchemaFunctionality` entry (`valkyrie-s-call`, `curator-of-destinies`, `make-your-move`, `squad-rallier`, `abyssal-harvester`, `courageous-goblin`, `uncharted-voyage`, `joust-through`, `archmage-of-runes`, `fleeting-flight`), one gap (`high-fae-trickster`'s "cast with flash" ability) with NEITHER a gap entry NOR any placeholder Effect at all, and 2 cards (`skyship-buccaneer`, `sphinx-of-forgotten-lore`) using an off-convention `Trigger` shape (`condition`/`description` fields instead of `on`/`name`). See `topics/fdn-notes-md-comment-convention.md`. Committed as a single commit.
- Pool-extension task (separate task, orchestrator-dispatched): extended `fdn-cards/` from 100 to 150 real cards. Determined the real selection convention by cross-referencing `data/cards.db` (gitignored bulk sync — the task brief's own assumption that `data/fdn/fdn_scryfall.json` "should have the full real FDN set" was WRONG, flagged; that file only ever holds the pipeline's own already-synced subset) against `git log --diff-filter=A`'s own 3-commit history: contiguous real collector #s 1-91 (systematic batch) + 9 unrelated demo-batch cards at #134-155 (earlier, hand-picked, NOT part of the sequence). Picked the next 50 as #92-146 (skipping the 3 demo-batch numbers inside that window). All 50 `definition.ts` transcribed from real Forge scripts (`tmp/mtg-forge`, confirmed present for all 50); used the 2026-09-18 schema-completeness pass's new vocabulary (`keywordCosts`, `Trigger.condition`/`BoardStateCondition` for Raid/Threshold, `CantBlock`/`Kicker`/`Prowess`) wherever applicable, zero `staticAbilities` usage. 23/50 declared a real `missingSchemaFunctionality` entry (several genuinely NEW gaps not previously documented anywhere in this pool — see `topics/fdn-pool-100-to-150.md` for the full list, Morbid recurring 5x is the most notable cluster); 27/50 used only existing vocabulary with zero gaps (2 of those via the `program`/combinator DSL: Cathar Commando's artifact-or-enchantment target select+destroy, Zimone's up-to-two-target counter placement mirroring `felidar-savior`). No `justification.json` authored (matches the pool's own 16/100 bulk-authoring ratio); ran `sync-fdn-oracle-text.mjs` to add the 50 new cards' real oracle text to `data/fdn/fdn_scryfall.json` anyway (150/150 resolved) for a future pass's benefit. Re-gated via `gate-and-write-status.mjs --all`: 13 blue / 3 purple / 134 gray / 0 other / 0 missing-file, 150 total (unchanged blue/purple — all 50 land at `gray`). `npx vitest run functional-model` (122/1314 passed/5 skipped, unchanged) and `npm run typecheck` (same 7 pre-existing diagnostics, confirmed via `git stash`/`git stash pop` A/B compare) both clean. Committed as 2 commits (new cards + oracle-text sync; then the pre-existing 100 cards' pure-timestamp `pipeline-status.json` churn from the same `--all` run, verified zero slug overlap via `comm` before splitting).

## Batch B: justification.json for 28 FDN cards (orchestrator-dispatched, parallel batch)
Authored+verified+gated `functional-model/fdn-cards/<slug>/justification.json`
for 28 slugs (abyssal-harvester through dauntless-veteran, see
topics/fdn-batch-b-28-cards-2026-09-18.md for the full list/outcome: 13
blue/15 purple). Declared 7 real missingSchemaFunctionality gaps across 7
cards (4 previously flagged: abyssal-harvester, archmage-of-runes,
courageous-goblin, curator-of-destinies; 3 more found the same way:
aetherize, angel-of-finality, chandra-flameshaper — the last had no real
structure at all before this pass, built real `abilities` for it). Fixed
a real lowercase-'flying' Keyword typo on angel-of-finality. Ran
`vitest run functional-model` (1314 passed, unaffected) and scoped `tsc`
(4 pre-existing diagnostics, 0 new). Committed `43ec470`, scoped to
exactly this batch's 64 files (2 sibling batches had concurrent
uncommitted edits to other slugs in the same tree, left untouched).
- Batch A (parallel to batch B above, disjoint 28-slug list, own dispatch): authored+verified+gated justification.json for day-of-judgment, dazzling-angel, divine-resilience, dragon-trainer, dreadwing-scavenger, eager-trufflesnout, electroduplicate, elfsworn-giant, elvish-regrower, erudite-wizard, essence-scatter, exemplar-of-light, faebloom-trick, felling-blow, fiendish-panda, fiery-annihilation, fleeting-distraction, fleeting-flight, goblin-boarders, goblin-negotiation, gorehorn-raider, grappling-kraken, guarded-heir, hare-apparent, healer-s-hawk, helpful-hunter, high-fae-trickster, high-society-hunter. Outcome: 17 blue / 11 purple. Resolved both task-flagged gaps (fleeting-flight: real CombatDamagePrevention fix, no gap after all; high-fae-trickster: genuine missingSchemaFunctionality for the caster-permission "cast with flash" grant, checked no existing continuous-grant shape fit). Found+fixed 3 more real issues while tiling: goblin-negotiation was a fully empty CardDefinition (implemented via ctx.xPaid/getNetToughness/createToken, no gap after all), divine-resilience was missing its own keywordCosts entry (added) alongside a genuine "any number of targets" gap (declared), fiery-annihilation's Equipment-exile half is representable via Card.getEquippedBy() (implemented) alongside a genuine CR 614.2 death-replacement gap (declared); goblin-boarders/gorehorn-raider both had a stale unwired Raid `Trigger.condition` (added, real existing BoardStateCondition vocabulary, no gap). Ran `vitest run functional-model` (1314 passed, unaffected) and `npm run typecheck` (same 7 pre-existing diagnostics, 0 new). Committed `14252ff`, scoped to exactly this batch's 70 files (2 sibling batches had concurrent uncommitted/untracked edits to other slugs in the same tree, left untouched).
- Batch C (parallel to A/B above, disjoint 28-slug list, own dispatch): authored+verified+gated justification.json for homunculus-horde, hungry-ghoul, icewind-elemental, incinerating-blast, infernal-vessel, infestation-sage, inspiration-from-beyond, joust-through, kaito-cunning-infiltrator, kellan-planar-trailblazer, kiora-the-rising-tide, luminous-rebuke, lunar-insight, make-your-move, mischievous-mystic, mischievous-pup, mossborn-hydra, nine-lives-familiar, preposterous-proportions, prideful-parent, quakestrider-ceratops, quick-draw-katana, raise-the-past, ravenous-amulet, refute, resolute-reinforcements, revenge-of-the-rats, rune-sealed-wall. Outcome: 16 blue / 12 purple. Resolved both task-flagged gaps (joust-through: combat-role target-filter gap declared; make-your-move: disjunctive type+power target-restriction gap declared) plus 6 more found the same way while tiling: homunculus-horde/kaito-cunning-infiltrator (pre-existing no-op `custom` placeholders — copy effect, emblem — now backed by real gap entries), incinerating-blast (optional-discard-then-conditional-draw, was COMPLETELY absent from `effects`, not even a placeholder), infernal-vessel/nine-lives-familiar (self-type/cast-history conditional gates with no `BoardStateCondition` variant), kellan-planar-trailblazer (found with a fully empty `CardDefinition` — both real activated abilities entirely unrepresented — added 2 gap entries), ravenous-amulet ("Activate only as a sorcery" on a plain ability, no timing-restriction field exists, distinguished from the separate Equip-keyword-reminder-text convention which needs no gap). One real, narrow existing-vocabulary FIX (not a gap): kiora-the-rising-tide's own Threshold `onAttack` trigger had no `condition` field at all — its NOTES.md's own "same simplification crypt-feaster uses" claim was stale (crypt-feaster already has `condition:{graveyardCountAtLeast}` from the earlier schema-completeness pass); added the identical field. New precedent: quakestrider-ceratops (blank real oracle_text, vanilla creature) — confirmed the full-coverage tiling check trivially passes with zero oracle_text entries when real text is empty, used a single type_line entry to satisfy the manifest's own non-empty-entries requirement. No off-convention `condition:string`+`description` Trigger shapes found in this batch. Ran `vitest run functional-model` (1314 passed, unaffected) and `npm run typecheck` (same 7 pre-existing diagnostics, 0 new). Committed, scoped to exactly this batch's files (2 sibling batches had concurrent committed/uncommitted edits to other slugs in the same tree, left untouched).
