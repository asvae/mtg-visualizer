- Hub created via `engine`/`schema` specialist split (orchestrator-driven, Part 1 of the split+engine-support plan): 7 topic files migrated from `engine/topics/` (fdn-sink-model-open-items, fdn-cheap-tier-vocabulary-mistakes, fdn-static-abilities-gate-rule, fdn-schema-tightness-coverage-manifest, recognizer-wiring-checklist, sink-predicate-corpus-mocking-policy, combinator-dsl-open-followups), content unchanged, plus a fresh `current.md` index.
- Built `functional-model/engine-support-registry.ts` (Part 2 of the split+engine-support plan) — new per-card `on`/`off` crossref, seeded with the confirmed real Ward-not-enforced case, distinct from `pipeline-status.json`'s own gray/purple/blue axis. Wired into `pipeline-status.ts` (`PipelineStatusFile.engineSupport`, set only on blue/purple, never gray) and `validate-card-definition.mjs` (carries the resolved `CardDefinition` through on ok/capacity-gap results); `gate-and-write-status.mjs` needed no changes. Added `engine-support-registry.test.ts` (6 cases). Authored a real, span-verified `justification.json` for `zul-ashur-lich-lord` (previously gray/no-manifest) so both confirmed real Ward users (it + `sire-of-seven-deaths`) could actually reach purple and carry `engineSupport: 'off'` — this is a real, non-fabricated authoring pass, but it shifts the pool-wide gate outcome from the previously-recorded 2 blue/3 purple/95 gray to 2 blue/4 purple/94 gray (see `topics/engine-support-registry.md` for the full tension/reasoning). Full pool re-gated via `gate-and-write-status.mjs --all`; `npm run vitest run functional-model` (122 files, 1305 passed/5 skipped) and `npm run typecheck` both clean of any NEW errors (7 pre-existing diagnostics, all in files this task never touched, confirmed unchanged before/after). Committed as its own commit.
