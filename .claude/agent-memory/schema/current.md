- [Recognizer wiring checklist](topics/recognizer-wiring-checklist.md) — RECOGNIZER_IDS allowlist + coreKey field-completeness, both repeatedly forgotten.
- [Sink-predicate corpus mocking policy](topics/sink-predicate-corpus-mocking-policy.md) — saga/crew predicate tests use mocked CardDefinitions, not real cards (scenarios/Cards stay real-only).
- [FDN/sink-model open items](topics/fdn-sink-model-open-items.md) — SUPERSEDED entry-count history (see the family-factory topic below for the current real shape); 3/5 sink-derivation predicates real (saga/crew/lifelink); predicate-derived matches AND bare producer/type-membership matches never self-own a category alone (only a genuine consumer signal does; reverse direction unaffected); `Trigger.name` has no canonical spelling per precondition — check for near-miss variants, not just the one already listed.
- [Sink catalog family/instance factory refactor](topics/sink-catalog-family-factory-refactor.md) — `SinkFamily`/`SinkInstance` vocabulary, `entry(candidate)` callable contract, review status now grouped per-family not per-instance (`entry.family`); the 2 flagged API-route gaps are now FIXED; 2026-09-18 follow-up split factory (`catalog/families/*.ts`) from per-instance config files, dropped authored `category` for a `getName`-derived one, renamed Counters' category to `'+1/+1'`.
- [CountersSink migrates off SinkQuery](topics/counters-sinkquery-migration-2026-09-18.md) — producer check now inspects `deriveOccurrences` directly, no `SinkQuery`; `SinkCatalogEntry.query`/`.category` now optional/new; BattlefieldPresenceSink NOT migrated yet (open follow-up); Source/Sink Candidate rename scoped to the sinks review page only.
- [CountersSink takes CardDefinition, not config](topics/countersSink-takes-carddefinition-2026-09-19.md) — SUPERSEDED (callable contract) by the boolean-return topic below; derivation rules (throw-on-missing-producer, deterministic slug, `on`-field NOT a reserved "reacts to X" convention) still current. `withPredicates` renamed `producerPredicate` (consumer half removed).
- [SinkInstance returns boolean; SinkFamily returns array](topics/sink-instance-boolean-return-2026-09-19.md) — 3rd callable-contract iteration: `instance(candidate)` now a plain `boolean` (producer-only, `SinkMatchDetail` deleted), `predicateDerived` moved to `.isPredicateDerived?()`; `SinkFamily<Config>` now returns `SinkInstance[]` (one per distinct `counterType` for Counters, dedup-by-identity-key rule stated generally); BattlefieldPresenceSink conforming-edit-only; `noUncheckedIndexedAccess` gotcha for array-unwrap sites (`arr[0]!`, not destructuring).
- [combinator.ts open follow-ups](topics/combinator-dsl-open-followups.md) — no Bind/Choose/ContextEquals yet; saga recognizer can't see through program-kind custom-alikes.
- [FDN cheap-tier vocabulary mistakes](topics/fdn-cheap-tier-vocabulary-mistakes.md) — recurring Haiku-authoring mistakes (Keyword/Trigger.on/TokenInfo/Actions vocab) + how a missing TokenInfo.name crashes the validator (not a validator bug).
- [FDN gate: staticAbilities/name-only-trigger cap at purple](topics/fdn-static-abilities-gate-rule.md) — SUPERSEDED IN SUBSTANCE by the schema-tightness redesign below (kept as historical record of the investigation it responds to); name-only-trigger rule itself is UNCHANGED. `hare-apparent` still needs a re-gate once a concurrent session's edit to it lands. `divine-resilience`'s `keywords:['Kicker']` masked `tsc` error is now FIXED (`'Kicker'` is a real `Keyword` member as of the 2026-09-18 schema-completeness pass — see that topic file).
- [FDN schema-tightness: missingSchemaFunctionality + coverageJustification](topics/fdn-schema-tightness-coverage-manifest.md) — `staticAbilities` now a hard FDN policy violation (not a capacity gap); coverage-justification is now a real, span-verified per-slug `justification.json` (not an inline field) checked against new durable `data/fdn/fdn_scryfall.json`; manifest-less card lands at `gray`; 5-card POC re-verified pool-wide, 2 blue/3 purple/95 gray outcome — SUPERSEDED, see engine-support-registry.md (now 2 blue/4 purple/94 gray).
- [engine-support-registry.ts](topics/engine-support-registry.md) — new per-card tri-state (`on`/`off`) distinct from pipeline-status color; seeded with Ward; wired into `pipeline-status.json` only for blue/purple; re-gate moved pool to 2 blue/4 purple/94 gray at the time (zul-ashur-lich-lord's own overdue justification.json authored to unblock it) — pool counts SUPERSEDED again, see the 2026-09-18 authoring-pass log entry (now 13 blue/3 purple/84 gray).
- [Schema-completeness pass 2026-09-18](topics/schema-completeness-pass-2026-09-18.md) — closed keyword-cost-payload, BoardStateCondition (Threshold/Raid/counter-gate), otherPermanentEnters watch-trigger, Prowess/Kicker/CantBlock keywords, `Query.source:'graveyard'`; 5 new engine-support-registry entries; divine-resilience's masked Kicker `tsc` error now genuinely fixed (zero edits needed); crystal-barricade/herald-of-eternal-dawn/arbiter-of-woe/blasphemous-edict/MayPlay gaps still open, not attempted. Follow-up authoring pass (same day, see log) actually re-authored the 13 unblocked cards — pool now 13 blue/3 purple/84 gray.
- [canBlock reads raw keywords, not effectiveKeywords](topics/canblock-reads-raw-keywords-not-effective.md) — found authoring cephalid-inkmage's granted Unblockable; a real, pre-existing, unfixed engine.ts gap (engine-owned), not something this pass's own condition field caused.
- [FDN pool extended 100 -> 150](topics/fdn-pool-100-to-150.md) — real selection convention is contiguous collector #s (1-91 done, next 50 = 92-146 skipping 3 demo-batch numbers); 23/50 new cards declare a real gap (several NEW, undocumented ones — Morbid recurs 5x); pool now 13 blue/3 purple/134 gray/150 total.
- [FDN NOTES.md comment convention](topics/fdn-notes-md-comment-convention.md) — definition.ts keeps only real-card-text comments over genuinely complex Effects; everything else moves to a new per-card NOTES.md; verified no-op across all 150 (gate/tests/typecheck unchanged); several real missingSchemaFunctionality-declaration gaps surfaced, not fixed.
- [FDN batch-B justification.json, 28 cards](topics/fdn-batch-b-28-cards-2026-09-18.md) — 13 blue/15 purple this batch; closed the 4 flagged undeclared gaps + 3 more found the same way (aetherize/angel-of-finality/chandra-flameshaper, the last had ZERO functional structure before this pass); pool-wide totals STALE until sibling batches A/C also land.
- [FDN batch-A justification.json, 28 cards](topics/fdn-batch-a-28-cards-2026-09-18.md) — 17 blue/11 purple this batch; resolved both task-flagged gaps (fleeting-flight real fix, high-fae-trickster genuine gap); found+fixed goblin-negotiation (fully empty CardDefinition), 2 stale unwired Raid conditions, divine-resilience's missing keywordCosts; pool-wide totals STALE until sibling batches B/C also land.
- [FDN batch-C justification.json, 28 cards](topics/fdn-batch-c-28-cards-2026-09-18.md) — 16 blue/12 purple this batch; resolved both task-flagged gaps (joust-through, make-your-move) + 6 more found the same way (kellan-planar-trailblazer had a fully empty CardDefinition); fixed kiora-the-rising-tide's stale unwired Threshold condition; new blank-oracle-text-vanilla-creature precedent (quakestrider-ceratops); pool-wide totals STALE until sibling batches A/B are combined with this one.
- [False-positive gap fixes: kykar, ravenous-amulet](topics/false-positive-gap-fixes-2026-09-18.md) — delayUntil/named-ability-cost sorcery-speed check both already existed; ravenous-amulet now blue, kykar still purple (unrelated name-only-trigger rule); tinybones-bauble-burglar is NOT a valid `activatedAbilities`/`description` precedent (not a real field); treetop-snarespinner's NOTES.md has the same stale false-positive claim, unfixed; engine.ts's unsupportedCostComponent has a latent named-ability-cost gap, flagged not fixed.
- [Sink model conceptual design doc](topics/sink-model-conceptual-design-doc.md) — new `sink-model/SINK_MODEL_DESIGN.md`: Self/Sink/Predicates/Candidate chain grounded in real code; card-page instance-granularity vs review family-granularity confirmed correct in code.
- [FDN trigger-dispatch-cluster pass](topics/fdn-trigger-dispatch-cluster-2026-09-18.md) — 10 new Forge-cited `Trigger.on` values + `otherPermanentEntersMatch.isLand` + 2 new `BoardStateCondition` kinds + `SpellCostReductionGrant.cardTypes`, all Ward-pattern (1 bundled + 1 new engine-support-registry entry); 21/25 re-authored cards now blue; kykar-zephyr-awakener has the same `onCastNoncreatureSpell` gap but is OUT of this task's scope, flagged as a freebie.
- [counterAdded trigger + counterAddedMatch](topics/counter-added-trigger-2026-09-19.md) — new `Trigger.on:'counterAdded'` + `counterAddedMatch.counterType` (deliberately NOT a default-plus-exception pair, full reasoning inside); `CountersSink` now derives counterType from a consumer-only definition too; exemplar-of-light flips purple→blue; engine consult flagged to orchestrator (no SendMessage tool in this subagent's toolset). Follow-up same file: `counterAddedMatch.source?: 'you'` closes the real `ValidSource$` gap (72-file pool check, `'you'`-only), exemplar-of-light updated, `CountersSink` NOT touched (no consumer yet). Follow-up 2 same file: `Trigger` renamed `TriggerOld`, new nested `Trigger{cause,effects}`/`TriggerCause` shape added (additive, exemplar-of-light ONLY — no other card migrated), new `TriggerOnValue` alias + `triggerOn`/`triggerCondition`/`triggerCounterAddedMatch`/`triggerActivationLimit`/`triggerTapLandForManaColor` narrowing helpers; 9 `tsc` diagnostics flagged for `engine` in `engine.ts`/`triggers.ts` (mechanical, runtime-unaffected, exact lines+fix in the topic file). Follow-up 3 same file: `Effect.kind:'putCounter'.target` widened to `'self' | PutCounterChosenTarget` (mirrors Forge's real ONE `PutCounter` ability, `Defined$`/`ValidTgts$` fork — `putCounterTarget`/`putCounterAll` unchanged), new `CardDefinition.abilityType?: 'spell'|'activated'` (Forge `SP$`/`AB$` made explicit); Fleeting Flight migrated to both (safe — confirmed `counters.test.ts` never imports the real card, only cites it in a comment). Follow-up 4 same file: exemplar-of-light param-by-param Forge audit — `ValidPlayer$ You` and `TriggerZones$ Battlefield` BOTH no-gap (reusable rules: who-does-it axis -> new `on` value not a param; absent `TriggerZones$` = ALL zones in Forge, battlefield-only is structural here), zero new fields, but found 3 stale "name-only trigger" claims (NOTES.md + 2 justification reasonings) — grep pool-wide for that phrase after ANY future `on`-value addition.
- [Forge-JSON -> CardDefinition compiler experiment](topics/forge-json-compiler-experiment-2026-09-19.md) — 2-card origin story (exemplar-of-light/fleeting-flight); superseded in scope (not substance) by the FDN 1-50 coverage push topic below, itself now superseded by the promotion/file-replacement topic.
- [CountersSink target-shape fix + comment trim](topics/countersSink-target-shape-fix-2026-09-19.md) — fixed self-only-producer false positive (checks `occ.target` now, `satisfiesType` exported from `match-sink.ts`); also trimmed `counters.ts`'s accumulated change-log comments to durable technical "why" only.
- [counters.test.ts: corpus test -> unit test rename + Test2/Test3 cut](topics/counters-test-corpus-to-unit-rename-2026-09-19.md) — SUPERSEDED (path text) by the Sink->Matcher rename below; content still accurate. 4 doc-comment sites renamed, generic/predicate "corpus test" usages left untouched; file stripped to 3 real comparisons, Test2/Test3 removed.
- ["Sink" -> "Matcher" rename, catalog-matcher layer](topics/sink-to-matcher-rename-2026-09-19.md) — `sink-model/`->`matcher-model/`, predicates relocated to top-level `sink-derivation-predicates/` (NOT nested, to avoid breaking the untouched sink-derivation-status.ts hardcoded path); full identifier map in card-schema.md section 14; role labels/Source-Sink-Candidate UI vocab/predicate subsystem deliberately untouched.
- [forge-json-compiler: FDN 1-50 coverage push](topics/forge-json-compiler-fdn-1-50-coverage-2026-09-19.md) — SUPERSEDED (counts + "not wired to fdn-cards" framing) by the promotion/file-replacement topic below; the 2-state gray/blue scheme, `structuralDiff`-as-diagnostic-only, and the still-genuinely-open gaps (Sagas, S:/R: lines, activated costs, planeswalkers, modal Charm, dynamic Count$ formulas) are all still accurate.
- [forge-json-compiler promoted + 23-card FDN file replacement](topics/forge-json-compiler-fdn-1-50-file-replacement-2026-09-19.md) — tool now lives at `functional-model/scripts/forge-json-compiler/` (not `scripts/experiments/`); new `TargetMin$/TargetMax$` -> `selectUpTo`/`applyToBound` rule (Felidar Savior, 23rd blue card); 23 real `fdn-cards/<slug>/definition.ts` replaced with compiler output + 14 `justification.json` trigger-name/effectKind reconciliations; mischievous-mystic export typo + skyship-buccaneer stale placeholder both fixed; zero of the 23 were previously `green`; new `write-fdn-definition.ts`/`author-fdn-definitions.ts` — deliberately never folds a token literal back into a `TOKENS.<key>` reference even on exact structural match. Same-day follow-up (see log): coordinator caught that `pipeline-status.json` still needed a real regate (`gate-and-write-status.mjs`) for a fresh `computedAt`/live-API visibility — file-content-only replacement never triggers that; ran it for exactly these 23, verified live via the running dev server.
- [Pipeline-status freshness: standing policy](topics/pipeline-status-freshness-policy.md) — never report an FDN card/batch ready without verifying `computedAt` is fresh vs current `definition.ts` (re-run `gate-and-write-status.mjs --all` if in doubt); full pool-wide `--all` regate 2026-09-19 confirmed 82 blue/19 purple/49 gray, zero real `status` flips, 15 purple cards had stale `engineGapsContext` (gate/schema-context drift, not card-content drift) — `engineGapsContext` goes stale on `ENGINE_GAPS.md` edits alone.
- [Compiler-sourced cards skip justification.json](topics/compiler-sourced-justification-exemption-2026-09-19.md) — new `CardDefinition.provenance?: 'forge-json-compiler'` field, set only by `compileForgeCard`; gate's Part 1.5 skips the manifest requirement when present; fingerprint/`re-review` mechanism untouched/reverified; Skyship Buccaneer/Vanguard Seraph now blue; pre-existing unrelated `tsc`/test issues flagged, not fixed.
- [forge-json-compiler: 5-card follow-up pass](topics/forge-json-compiler-5-card-followup-2026-09-19.md) — Squad Rallier (new `Dig` case + `dig.powerLE`/`'creature'`) and Twinblade Blessing (`Enchant:Creature`→synthesized attach trigger + `S:Continuous/EnchantedBy/AddKeyword`→`continuousKeywordGrants`) now blue, pool 25 blue/25 gray; new generic `otherCreatureDiesMatch.excludeSubtype` (Forge's real `non<Type>` pattern) got Valkyrie's Call PAST that gap but it still throws deeper (real `Defined$ TriggeredCard`/counter-bearing-move/instance-scoped-continuous-grant gap, needs an `engine` design decision, unattempted); Crystal Barricade/Herald of Eternal Dawn confirmed genuinely still blocked, no precedent anywhere in the pool; `write-fdn-definition.ts` now extracts real TS source via the `typescript` AST (not a lossy runtime `Function.toString()`) to serialize a `custom` effect's `run` closure.
