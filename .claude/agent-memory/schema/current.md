- [Recognizer wiring checklist](topics/recognizer-wiring-checklist.md) — RECOGNIZER_IDS allowlist + coreKey field-completeness, both repeatedly forgotten.
- [Sink-predicate corpus mocking policy](topics/sink-predicate-corpus-mocking-policy.md) — saga/crew predicate tests use mocked CardDefinitions, not real cards (scenarios/Cards stay real-only).
- [FDN/sink-model open items](topics/fdn-sink-model-open-items.md) — sink catalog has 6 entries (lifegain, graveyard-fodder, etb, battlefield-presence-cats/-creatures, counters-plus1plus1), 3/5 sink-derivation predicates real (saga/crew/lifelink); predicate-derived matches AND bare producer/type-membership matches never self-own a category alone (only a genuine consumer signal does; reverse direction unaffected); `Trigger.name` has no canonical spelling per precondition — check for near-miss variants, not just the one already listed.
- [combinator.ts open follow-ups](topics/combinator-dsl-open-followups.md) — no Bind/Choose/ContextEquals yet; saga recognizer can't see through program-kind custom-alikes.
- [FDN cheap-tier vocabulary mistakes](topics/fdn-cheap-tier-vocabulary-mistakes.md) — recurring Haiku-authoring mistakes (Keyword/Trigger.on/TokenInfo/Actions vocab) + how a missing TokenInfo.name crashes the validator (not a validator bug).
- [FDN gate: staticAbilities/name-only-trigger cap at purple](topics/fdn-static-abilities-gate-rule.md) — SUPERSEDED IN SUBSTANCE by the schema-tightness redesign below (kept as historical record of the investigation it responds to); name-only-trigger rule itself is UNCHANGED. `hare-apparent` still needs a re-gate once a concurrent session's edit to it lands. `divine-resilience`'s `keywords:['Kicker']` is a live but currently-masked `tsc` error, flagged not fixed.
- [FDN schema-tightness: missingSchemaFunctionality + coverageJustification](topics/fdn-schema-tightness-coverage-manifest.md) — `staticAbilities` now a hard FDN policy violation (not a capacity gap); coverage-justification is now a real, span-verified per-slug `justification.json` (not an inline field) checked against new durable `data/fdn/fdn_scryfall.json`; manifest-less card lands at `gray`; 5-card POC re-verified pool-wide, 2 blue/3 purple/95 gray outcome — SUPERSEDED, see engine-support-registry.md (now 2 blue/4 purple/94 gray).
- [engine-support-registry.ts](topics/engine-support-registry.md) — new per-card tri-state (`on`/`off`) distinct from pipeline-status color; seeded with Ward; wired into `pipeline-status.json` only for blue/purple; re-gate moved pool to 2 blue/4 purple/94 gray (zul-ashur-lich-lord's own overdue justification.json authored to unblock it).
