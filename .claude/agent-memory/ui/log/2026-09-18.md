## 2026-09-18

- Housekeeping (no domain work): migrated `.claude/agent-memory/ui/` from a
  single 3866-line append-only `notes.md` into the hub structure specified
  in `.claude/agents/SHARED.md` — 13 `topics/<slug>.md` files (durable
  decisions/gotchas/conventions/open-flags only, most verification
  narration dropped), a 13-line `current.md` index, and the full original
  `notes.md` content preserved verbatim as `log/pre-2026-09-18-archive.md`
  for grep purposes. Old `notes.md` deleted.
- `/app/engine/sinks/[[slug]].vue` follow-up: added a "Real FDN pool
  matches" collapsible section (two `<details>`, producer vs. consumer,
  same "N cards" `<summary>` convention `CardDetailTabs.vue` uses) showing
  which real FDN dev-pool cards actually match each catalog entry today.
  Server side: `GET /api/sink-catalog` now also returns
  `realMatches?: { producerMatches: string[]; consumerMatches?: string[] }`
  per entry, computed unconditionally (not gated on review color — a
  reviewer needs to see real matches for gray/purple entries too) via
  `matchSink`/`matchesConsumerTriggerNames`/`matchesConsumerTriggerOn`
  against a new shared `server/utils/fdnDefinitionPool.ts` (extracted the
  FDN-pool-loading convention out of `server/api/card/[set]/[number].ts`'s
  own private copy into a reusable module, rather than duplicating the
  vite-node-spawn/cache machinery a second time — that route's own private
  copy was left untouched to avoid an unrelated edit to a `card`-owned
  file). Skipped real thumbnails (plain card names only) — the enrichment
  convention (`resolveFunctionalModelCardMeta` and its whole fin/db/live-
  Scryfall fallback cascade) lives entirely unexported inside
  `card/[set]/[number].ts`; duplicating it felt like real, untested scope
  creep for a dev-only review tool, so this was a deliberate "your call"
  skip per the task's own allowance. Hit the known stale-dev-server-HMR
  gotcha live while verifying (a peer `engine` agent's same-day
  `consumerTriggerOn` addition to `SinkCatalogEntry` wasn't reflected until
  a full dev-server restart, not just Nitro's own file-change HMR) —
  restarted cleanly, no data loss. Live-verified against
  `computeCardInteractions`'s own output via `/api/card/fdn/12` (Felidar
  Savior) and `/api/card/fdn/135` (Ajani's Pridemate) — exact same card
  lists both directions.

- Added a "Schema" page under the `/app/engine/*` console's "…" overflow
  (alongside Keywords): `app/pages/app/engine/schema.vue`, a single
  read-only reference page listing every real card-authoring schema type
  rooted at `CardDefinition`. New `functional-model/card-schema-source.ts`
  (comment/string-aware brace-depth text scanner, NOT a real TS parser —
  no `typescript` package pulled in) slices exactly 19 named top-level
  type/interface declarations straight out of `card.ts` (plus `TokenInfo`
  from `interfaces.ts`, the one referenced type declared elsewhere) with
  their real leading doc comments intact, verbatim, on every request (no
  caching, matches every other `/app/engine/*` evidence route's own
  convention) — never touched `card.ts` itself, read-only per the task's
  own constraint. New thin `server/api/card-schema.get.ts` wrapper. Page
  reuses `EngineConsoleShell` (nav slot = short intro only, no
  list/search — "single page is fine," no sub-nav) + `EngineConsoleCode-
  Section` per type (title = type name, `default-open` on all 20 so the
  reference shows everything without click-to-reveal; still collapsible
  since `CardDefinition`/`Effect` are 500-650 real lines each). Verified
  via Playwright against the already-running dev server: overflow menu
  shows Schema next to Keywords, navigates to `/app/engine/schema`, all 20
  sections render with real syntax-highlighted content (spot-checked all
  35 real `Effect` variant kinds present, e.g. `createToken`/`pumpAll`/
  `putCounterAll`/`custom`/`program`/`endTurn`). `npm run typecheck` clean
  on every file this task touched (pre-existing errors elsewhere, e.g.
  `card.ts`/`CardDetailTabs.vue`/`mana.ts`, are a concurrent agent's
  in-flight `fdn-cards`/engine work, confirmed via `git diff --stat` — not
  from this task, never edited `card.ts`).

- `EngineConsoleShell.vue`: made the nav/sidebar pane drag-resizable
  (`navWidth` ref, 180–480px clamp, default 240, `localStorage`-persisted
  under `mtg-visualizer-engine-console-nav-width`, shared across all six
  `/app/engine/*` tabs). Handle mirrors `CardPeekPanel.vue`'s existing
  pointerdown/pointermove drag-resize pattern (checked for and reused
  rather than inventing a new one, per the task's own instruction). No
  other file touched; prev/next nav, arrow-key handling, tabs placement
  all untouched. `npm run typecheck` clean (verified via `git stash`
  before/after diff — same pre-existing errors both ways, all outside
  this file). Live-verified via a throwaway (not checked-in) Playwright
  script: drag resizes on both `/app/engine/cards` and
  `/app/engine/sinks`, width persists across client-side nav between them
  and across a full reload, min/max clamp holds, prev/next+arrow-key nav
  unaffected. Hit a real concurrent-agent collision risk mid-task: ran
  `git stash`/`git stash pop` for the typecheck diff and it silently
  carried a DIFFERENT in-flight agent's uncommitted changes (
  `CardDetailTabs.vue`, `PlainOracleText.vue`, `functional-model/card.ts`,
  `scripts/validate-card-definition.mjs`, an untracked
  `_tmp_verify_plaintext.mjs`) along for the ride — popped back cleanly
  with no conflict, own diff confirmed intact/untouched after, but this
  confirms another agent WAS active despite the dispatch stating none
  was — flagged to orchestrator in the handback. Lesson: avoid
  `git stash` on a shared working tree when any concurrent agent might be
  running; a targeted `git diff`/`git show HEAD:<path>` comparison is
  safer than stashing the whole tree.

- `PlainOracleText.vue` (FDN card page's plain oracle-text renderer):
  rebuilt to render `FunctionalModelText.vue`'s own visual chrome (name
  heading, mana cost + inline oracle-text mana costs as real
  `<ManaSymbol>` icons via `parseManaSegments`, type line, P/T line) with
  zero Fact/annotation/highlight machinery — was previously a bare
  `<p>{{ oracleText }}</p>`, which had regressed `{3}{W}`-style inline
  costs back to literal curly-brace text. Widened props from
  `oracleText: string` to `name`/`manaCost`/`typeLine`/`power?`/
  `toughness?`/`oracleText`, sourced from `data.card` (plain `CardData`,
  not `AnnotatedCard`/`AnnotatedFace` — no `colorIndicator` field exists
  on that type, so that one swatch-row piece is genuinely omitted, not
  faked). Updated `CardDetailTabs.vue`'s call site to pass the new props
  and changed its `v-else-if` gate from `isFdn && oracleText` to plain
  `isFdn` so the header still renders for a vanilla (no-rules-text)
  creature. `npx vue-tsc --noEmit` clean. Live-verified via Playwright on
  the running dev server: Celestial Armor (fdn/5) — name+cost header,
  type line, "Equip {3}{W}" now shows real mana pips inline (was literal
  text before); Felidar Savior (fdn/12) — header + P/T "2/3" line;
  vanilla-creature/empty-oracle-text path has no real case in the live
  transcribed pool yet (every "Not started" pipeline card has
  `functionalModel: null` entirely, not an empty `oracleText` string), so
  verified via a `page.route()` interception forcing `oracleText: ''` on
  Celestial Armor's response — header renders, no stray paragraph/gap.
  Hit the SAME concurrent-agent `git stash` collision as the entry above,
  from the other side — this task's own uncommitted edits (this file +
  `CardDetailTabs.vue`) got silently stashed and popped by a peer `ui`
  agent's own `git stash`/`pop` around its own typecheck diff, mid-task;
  recovered with no data loss, confirmed via re-read + `git diff --stat`,
  but flagged to orchestrator in the handback per that entry's own
  "avoid `git stash` on a shared tree" lesson — this occurrence is the
  concrete case that lesson is about.
