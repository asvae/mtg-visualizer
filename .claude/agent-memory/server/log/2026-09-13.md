# 2026-09-13

- Review-panel retirement (user's explicit call): removed the build/
  config surface — `nuxt.config.ts` `runtimeConfig.public.enableReview`
  field + comment; `package.json` `review-server` script entry (target
  `scripts/review-server.mjs` already gone); `.env.example` deleted
  outright (only content was documenting `NUXT_PUBLIC_ENABLE_REVIEW`).
  `appVersion`/`buildCommit` untouched. Landed together with the
  deck-builder pivot groundwork in commit `092868d1`.
- At the time, two doc mentions (`docs/prds/02-navigation.md:38`,
  `app/pages/docs/[[slug]].vue:10`) were left untouched and flagged to the
  orchestrator to route if docs needed updating — orchestrator-side
  follow-up, not tracked further here.
