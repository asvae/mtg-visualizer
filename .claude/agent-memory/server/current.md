# server agent memory index

- [Nitro dev-bundle top-level throw → TDZ](topics/nitro-dev-bundle-top-level-throw-tdz.md) — an unguarded `import.meta.dirname` top-level throw anywhere in the server import graph breaks unrelated routes; fixed 2026-09-19, watch for recurrence in new dev-only server utils.

See `log/` for resolved history (Netlify Node-version pin for
`node:sqlite`, review-panel config retirement).
