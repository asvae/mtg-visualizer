# server agent memory index

No active topics — see `log/` for resolved history (Netlify Node-version
pin for `node:sqlite`, review-panel config retirement). Nothing currently
open that isn't already documented in the shell config itself
(`netlify.toml`'s inline comment) or in git log.
