## Sink→Matcher rename verification (card-schema.md section 14 follow-up)

Reviewed schema's mechanical `Sink`→`Matcher` rename diff on my two
forced-exception files (`card-interactions.ts`, `card-interactions.test.ts`
vs `HEAD~5`): confirmed genuinely mechanical — every changed line is a
comment/doc-prose citation or an import/type identifier rename
(`SinkCatalogEntry`→`MatcherCatalogEntry`, `matchSink`→`matchQuery`,
`SinkInstance`→`Matcher`, etc). Zero logic/conditional/control-flow lines
changed.

Fixed the 2 stale citations flagged for me:
- `functional-model/SYNERGY_DESIGN.md` line 17: `sink-model/SINK_MODEL_DESIGN.md`
  → `matcher-model/MATCHER_MODEL_DESIGN.md`.
- `functional-model/synergy.ts` (~line 2298-2301, `hasPrintedLifelink` doc
  comment): `sink-model/predicates/lifelink.ts` → `sink-derivation-
  predicates/lifelink.ts` (that predicates subsystem moved but kept its
  "sink" naming per the rename's own explicit exclusion — NOT renamed to
  matcher); `sink-model/match-sink.ts`'s `deriveOccurrences`/`matchSink` →
  `matcher-model/match-query.ts`'s `deriveOccurrences`/`matchQuery`.

Verified both citation targets exist at their new paths before editing.
No new durable topic needed — one-off verification, reported back via
SubagentHandback.
