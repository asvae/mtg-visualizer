- Fixed 9 typecheck errors in engine.ts (7) and triggers.ts (2) introduced by
  schema's TriggerOld/Trigger split (33eb5065, CardDefinition.triggers now
  `(Trigger | TriggerOld)[]`). All 7 engine.ts sites read `.on` (occasion
  dispatch: enter/upkeep-endStep/tapLandForMana/attacks/equippedAttacks) or
  `.tapLandForManaColor`; triggers.ts's fireTrigger read `.activationLimit`
  for the per-turn cap gate. Fix: switched all reads to card.ts's own
  triggerOn/triggerActivationLimit/triggerTapLandForManaColor narrowing
  helpers (schema had already added these for exactly this purpose, right
  next to the Trigger/TriggerCause defs) instead of hand-rolling a new
  `'on' in trigger` guard. No new engine dispatch logic added for the nested
  `cause` shape — helpers just read the right field per shape; Exemplar of
  Light's own 2 triggers (lifeGained/counterAdded) still don't match any
  on-value these call sites check, so still correctly un-fired.
  Verified: typecheck back to the pre-existing 5-error baseline only
  (mana.ts:275, tokens/by-key.ts:32, card-status.ts:263, card.ts:3812
  endTurn — all untouched); `npx vitest run functional-model` still 120
  files / 1329 passed / 5 skipped / 0 failed. Committed as 32dc13c, staged
  explicitly (functional-model/engine.ts, functional-model/triggers.ts
  only) since a concurrent session had unrelated unstaged changes in the
  tree (CardDetailTabs.vue, useGraphStore.ts, counters.test.ts,
  forge-script/ — none touched).
