- Migrated `.claude/agent-memory/engine/` from a single ~30,140-line
  append-only `notes.md` into the hub shape (`current.md` + `topics/*.md`
  + `log/*.md`) per SHARED.md's new memory-discipline spec. Read the old
  file in full (chunked). Found almost the entire file was either (a)
  per-card migration/bugfix session narration whose end state is already
  reflected in the current code/git history, or (b) cross-cutting
  design decisions already fully documented in
  `functional-model/SYNERGY_DESIGN.md`, `ENGINE_GAPS.md`,
  `PRD_AUTOMATED_AUTHORING.md`, and `.claude/contracts/card-schema.md` —
  spot-checked several claimed-novel decisions (Fact unification,
  scenario-count-defaults-to-1, the printed-Lifelink exception,
  `Fact.value` removal, the FDN pipeline-status/sink-catalog axis) and
  confirmed each was already written up there in more complete form.
  Kept only what wasn't captured elsewhere: 8 topic files covering
  tooling/CLI gotchas, concurrent-session hazards specific to this pool,
  a recognizer-wiring checklist, the sink-predicate-corpus mocking
  policy, the Features-tab `blue` evidence caveat, FDN/sink-model
  still-open items, and combinator.ts's still-open node kinds. Archived
  the old file's full content verbatim into 5 lump `log/archive-*.md`
  files (best-effort chronological, not exact — the original file wasn't
  strictly ordered by date throughout) rather than a per-date split, per
  the task's own "don't over-engineer the archive" instruction. Deleted
  the old `notes.md` (git history preserves it).

- Reverted the same-day sink-ATTACHMENT persistence layer per an explicit
  user correction: deleted `functional-model/sink-attachment.ts` + its
  test + the two real `sinks.json` artifacts (`ajani-s-pridemate`,
  `serra-angel`); restored `pipeline-status.ts`'s `blue` to its original
  gate-only meaning (removed the `isSinkAttachmentComplete` check from
  `effectivePipelineStatus`, doc comments record the tried-then-reverted
  history rather than silently deleting it); fixed `pipeline-status.test
  .ts` (removed 3 attachment-specific tests, folded `blue` into the
  generic pass-through `it.each`). Re-verified live: all 7 real FDN cards
  previously stored `blue` are back to effective `blue` (matches raw
  stored value 1:1, confirmed via a real script run, not assumed) — the
  3 `purple` cards unaffected either time. Updated `.claude/contracts/
  card-schema.md` to strike the attachment/blue-redefinition sections
  (kept catalog-entry/catalog-review sections, unaffected) with the same
  "tried, considered, reverted" posture.
- Implemented the "derive on the fly" replacement in
  `functional-model/card-interactions.ts`: checks `definition` against
  every `isSinkCatalogEntryUsable` `SINK_CATALOG` entry's own `query`
  FIRST via the existing `matchSink` (no new persistence, no new
  matching primitive) — a match swaps that occurrence's raw
  `describeFact` label for the catalog's real `query.category` label,
  self-inclusive pool count computed the same way. Verified live against
  all 10 real FDN cards: Day of Judgment now genuinely shows "Graveyard
  fodder" (was raw "destroy") — a real, correct win. Ajani's Pridemate
  does NOT get "Lifegain" under this design (flagged explicitly, not
  forced) — confirmed live `matchSink(lifegainQuery, ajanisPridemate)` is
  `false`, since the catalog query is producer-shaped and Ajani only
  reacts to lifegain (a consumer signal with no safe structural
  derivation, per this same file's own earlier same-day investigation).
  Updated `card-interactions.test.ts` (3 existing tests' expected
  category names changed: 'destroy'->'Graveyard fodder', 'life gain'
  ->'Lifegain' ×2; one existing test's assertion strengthened to also
  assert 'Lifegain' is absent for Ajani). Full `functional-model` suite
  green (113 files/1184+5 skipped); full-repo suite green apart from the
  5 pre-existing unrelated `scripts/relations.test.mjs` failures.
  Typecheck: 0 new errors in any file this task touched; new missing-
  module errors appeared in `card`/`server`-owned consumers of the
  deleted `sink-attachment.ts` (`CardDetailTabs.vue`, `cardResponse.ts`,
  `server/api/card/[set]/[number].ts`, `server/api/fdn-cards/[slug]/
  {review,sinks}.post.ts`) — expected fallout, explicitly the `card`
  agent's own next-step cleanup, not touched here.

- Corrected the "Ajani's Pridemate can't own Lifegain" finding above per an
  explicit user course-correction mid-task (first draft of this attempt
  wrongly tried an oracle-text regex on a Trigger.name, which the user
  vetoed outright: "sinks should not touch oracle text in any way. Only
  definitions"). Landed the real fix instead: `SinkCatalogEntry
  .consumerTriggerNames?: string[]` (`sink-model/catalog/entry.ts`) — a
  second, CONSUMER-side recognition mode checked via a new
  `matchesConsumerTriggerNames` (`sink-model/match-sink.ts`, pure
  `Trigger.name` field comparison, never oracle text). `lifegain` now
  declares `consumerTriggerNames: ['onLifeGained']`; confirmed
  `ajanisPridemate` is the ONLY real FDN card with that trigger name pool-
  wide (zero false-positive risk). Wired into `computeCardInteractions`'s
  catalog-first loop (self AND pool-wide, both checked via producer-OR-
  consumer). Rewrote `card-interactions.test.ts`'s former "Ajani does NOT
  get Lifegain" case to assert it now DOES (self-inclusive); added a
  negative case (Serra Angel, no matching trigger, still excluded).
  `lifegain.test.ts`/`.corpus.json` gained 4 new consumer-mode cases
  (8/8, still blue). Updated `card-schema.md`'s catalog section with a
  SUPERSEDED note (kept the prior finding as historical record).

- Addendum: built the full "Card > Sink (Lifegain) > Predicate (Lifelink) >
  Card" worked example end-to-end. New real FDN card `felidar-savior`
  (FDN #12, `{3}{W}` Cat Beast 2/3, printed Lifelink, an ETB "put a +1/+1
  counter on each of up to two OTHER target creatures you control" —
  widened `venat-heart-of-hydaelyn...`'s own real `selectUpTo(...
  .filter('excludeSelf'), 1, 'target', [applyToBound('target', 0, ...)])`
  Blessing-of-Light shape from max-1/index-0 to max-2/index-0-AND-1, no new
  combinator vocabulary). Passed `validate-card-definition-cli.mjs`
  (`ok:true`); wrote `pipeline-status.json` from that real result. New 5th
  sink-derivation predicate `sink-model/predicates/lifelink.ts` (+.test.ts
  +.corpus.json, mocked fixtures + a real `state.dealDamage` engine call
  per case, mirroring saga/crew's own convention) — a bare
  `keywords.includes('Lifelink')` structural read (front+back face
  independently), registered in `SINK_DERIVATION_MECHANISMS`
  (`sink-derivation-status.ts`), landed already `blue` (3/3 corpus), wired
  into `match-sink.ts`'s `deriveOccurrences` behind the same
  `isSinkDerivationMechanismUsable` gate saga/crew use.
  Found + resolved a real, confirmed disagreement this surfaced: `sink-
  model/match-sink.test.ts`'s pre-existing "sink D" case explicitly
  asserted Aerith Gainsborough's own printed Lifelink does NOT satisfy the
  lifegain sink, citing `synergy.ts`'s PARKED `LIFELINK_SYNTHETIC_FACT_
  ENABLED = false` (2026-09-14) by name. Confirmed this new predicate does
  NOT touch that flag or FIN's real served pipeline at all (`
  computeCardInteractions`'s only production call site is FDN-scoped,
  `loadFdnFunctionalModel`) — this is a deliberate, freshly-commissioned
  divergence in the separate sink-only prototype matcher only, not a silent
  revival of the parked FIN pattern. Flipped that test's expectation with a
  full explanatory rewrite; added a cross-reference doc comment on
  `LIFELINK_SYNTHETIC_FACT_ENABLED` itself pointing at the new predicate so
  a future reader isn't confused by the apparent contradiction. Updated
  `sink-derivation-status.test.ts` (4->5 seeded mechanisms) and
  `card-schema.md` (new "Full chain worked example" section). Verified
  live: `computeCardInteractions(ajanisPridemate, [ajanisPridemate,
  felidarSavior, serraAngel, healersHawk])`'s own "Lifegain" category now
  includes Felidar Savior AND Healer's Hawk, excludes Serra Angel. Full
  `functional-model` suite green (114 files, 1197 passed/5 skipped);
  `npm run typecheck` shows the identical pre-existing error set as a
  clean `main` baseline (confirmed via a scoped git-stash before/after).

- Mechanical prep for scaling the FDN authoring pool from 11 to 100 real
  cards (orchestrator handles the actual cheap-tier authoring dispatches
  itself). Determined the real in-scope pool (`data/cards.db`,
  `set_code='fdn' AND booster:true`, Basic Lands excluded — 271 distinct
  names, 0 collisions, all `layout:'normal'`), picked the next 89 by
  collector-number order (`sire-of-seven-deaths` cn1 through
  `kellan-planar-trailblazer` cn91), and ran `prep-card-context.mjs` for
  all 89 — every single one got real Forge AND real XMage source (89/89,
  zero misses). Built `functional-model/scripts/gate-and-write-status.mjs`
  (batchable `<slug>...`/`--all` wrapper around `validateCardDefinition` +
  `pipelineStatusFromGateResult` — no such reusable script existed before;
  the original 11 cards' own `pipeline-status.json` files had no prior
  writer to point to), live-verified against all 11 real cards (byte-
  identical modulo `computedAt`) then reverted the test diff. Left the new
  script uncommitted for review, per the dispatch's own instruction. Full
  slug list + gate invocation reported back to the orchestrator directly
  (not written to a file). See `topics/fdn-sink-model-open-items.md`'s new
  "Scale-up to 100 cards" entry.

- New sink catalog entry: `etb` (Felidar Savior, FDN #12, had a real
  structural ETB trigger but showed nothing in Interactions since no
  catalog entry covered "ETB" yet). Resolved the design question by
  checking the real engine first, not guessing: `resolveTop`/
  `fireOnPhaseEnterTriggers` (engine.ts) only ever fire a `Trigger.on ===
  'enter'` entry off the SAME permanent that just entered — no
  Soul-Warden-style "another creature enters" sweep exists anywhere in
  this engine today — so unlike `lifegain`, ETB genuinely needs NO
  producer/consumer split: "has a real `on:'enter'` trigger" is itself the
  whole, self-inclusive structural fact. Added a NEW, dedicated `event:
  'etb'` producer occurrence to `match-sink.ts`'s `collectForFace`
  (deliberately separate from the pre-existing `entersBattlefield`
  occurrence, which fires for virtually every normal permanent regardless
  of whether it has any trigger — reusing it would over-match almost the
  whole pool as "ETB"). New `sink-model/catalog/etb.ts` (+`.test.ts` 5/5
  +`.corpus.json`, mocked fixtures per policy) — single-role, `query:
  {category:'ETB', event:'etb', controller:'you'}`, no
  `consumerTriggerNames`. Registered in `SINK_CATALOG`
  (`catalog/index.ts`) — now 3 real entries. Updated
  `sink-catalog-status.test.ts` (2 assertions needed the new slug/category)
  and `card-interactions.test.ts` (Helpful Hunter's own ETB now genuinely
  surfaces "ETB" instead of `[]`; added a Felidar-Savior-specific case).
  Live-verified against the real, now-100-card FDN pool via a scratch
  vite-node script: 20 real cards match "ETB" (Felidar Savior, Helpful
  Hunter, Angel of Finality, Arahbo the First Fang, and others from the
  89-card authoring batch); Felidar Savior's own `computeCardInteractions`
  now genuinely returns BOTH "ETB" (count 20) and "Lifegain" (count 11) —
  previously showed neither. Full `functional-model` suite green
  (115 files, 1203 passed/5 skipped). `npm run typecheck`: identical
  pre-existing baseline error set confirmed via a scoped git-stash
  before/after (zero new errors). Updated `card-schema.md`'s catalog
  section (new subsection 6) and `topics/fdn-sink-model-open-items.md`.
  Left everything uncommitted for review per the dispatch's own
  instruction.

- Two real bug fixes, dispatched together (self-ownership + ETB redesign):
  (1) `ProducerOccurrence.predicateDerived`/`SinkMatchResult.predicateDerived`
  added (`match-sink.ts`), set only at the 3 predicate call sites inside
  `deriveOccurrences`; `card-interactions.ts`'s self-ownership gate now
  requires a NON-predicate-derived producer match (or a consumer signal) —
  fixes Healer's Hawk/Felidar Savior (and 3 more real Lifelink-only FDN
  cards) self-showing "Lifegain" off the `lifelink` predicate alone;
  reverse direction (Ajani's Pridemate seeing them as real producers)
  confirmed unchanged. (2) `etb` catalog entry reworked from a wrong
  single-role design into a genuine producer/consumer split, same shape as
  `lifegain`: new `event:'bounce'` producer occurrence (`move` effect,
  Battlefield->Hand) + new `SinkCatalogEntry.consumerTriggerOn`/
  `matchesConsumerTriggerOn` (Trigger.on-based consumer signal) — fixes
  Felidar Savior self-showing "ETB: 20" (now correctly 1, Bigfin Bouncer
  only). Rewrote `etb.ts`/`etb.test.ts`/`etb.corpus.json` (8 cases),
  updated `card-interactions.test.ts`, `match-sink.test.ts`, `card-schema
  .md` (new sections 7+8, section 6 marked superseded). Full
  `functional-model` suite green (115 files, 1212 passed/5 skipped),
  `npm run typecheck` clean (identical pre-existing baseline, confirmed
  zero new errors from any touched file). Live-verified against the real,
  now-100-card FDN pool both before and after. Left everything
  uncommitted per the dispatch's own instruction.

- Fixed all 37 real-TS-error FDN cards flagged by a gate-and-write-status.mjs
  --all run after the 89-card Haiku authoring batch (100 fdn-cards total):
  34 fixed to blue with real vocabulary (Keyword casing, Trigger.on values,
  TokenInfo fields, real Actions/Player/Card members, AlternateCost->modal
  for Kicker, flashback() factory for Flashback), 3 reclassified purple as
  genuine capacity gaps (kaito-cunning-infiltrator: no emblem mechanic;
  tragic-banshee: no Morbid/"died this turn" tracking; zul-ashur-lich-lord:
  no standing MayPlay-style graveyard-cast permission). The 3 reported
  "validator crash" cases (dragon-trainer, kaito-cunning-infiltrator,
  mischievous-mystic) were all the SAME real, reproducible bug: a token
  literal missing the required `TokenInfo.name` field crashes
  `synergyTags()`'s `effect.token.name.toLowerCase()` at card.ts~2848
  during the vocab-walk phase (runs before tsc, on untyped transpiled JS)
  — not a bug in validate-card-definition.mjs itself; fixed by supplying
  real token shapes. Final gate re-run: 85 blue / 15 purple / 0 other / 0
  missing across all 100. Full `npx vitest run functional-model`: 115
  files/1212 passed/5 skipped. `tsc --noEmit -p .nuxt/tsconfig.server.json`:
  unchanged pre-existing baseline (card-status.ts:263, card.ts:2970,
  mana.ts:275, server/api/tokens/by-key.ts), zero new errors from any of
  the 37 touched files. Did not touch the pre-existing 12 correctly-
  flagged purple cards. Left everything uncommitted per the dispatch's
  own instruction. Wrote up recurring cheap-tier vocabulary mistakes to
  topics/fdn-cheap-tier-vocabulary-mistakes.md for future batches.

- New sink catalog PAIR: `battlefield-presence-cats`/`battlefield-presence
  -creatures` (Claws Out, FDN #6 — "Affinity for Cats" costReduction +
  bare "Creatures you control get +2/+2" pumpAll, both framed by the user
  as the same "cares about board-count of a filtered set" concept, two
  catalog rows sharing one matcher). Producer side needed ZERO new
  occurrence code — reused the existing baseline `entersBattlefield`/
  `createToken` occurrences (`types:{has:['Cat'/'Creature']}}` already
  resolves via `typeWordsFromTypeLine`'s real subtype-splitting). New
  consumer-recognition mode `SinkCatalogEntry.consumerBattlefieldPresence
  ?: {subtype?:string}` + `matchesBattlefieldPresenceConsumer`
  (`match-sink.ts`) — reads `CostReduction.perControlled`/`pumpAll`/
  `putCounterAll` directly (front+back face, incl. nested `modal` modes),
  genuinely different in kind from the trigger-keyed
  `consumerTriggerNames`/`consumerTriggerOn`. Real bug found LIVE by the
  user mid-task (before reporting done): Helpful Hunter (a genuine,
  printed Cat with no Affinity/anthem effect at all) self-displayed
  "Cats" purely for BEING a Cat — bare type/subtype membership, not a
  deliberate ability, same shape of over-match the original `etb` design
  already hit once. Fixed with a new, general escape hatch — `entry
  .requireConsumerForSelfOwnership?: boolean` — gating self-ownership down
  to `selfConsumerMatch` ALONE for these 2 entries (every other entry's
  `selfDirectProducerMatch || selfConsumerMatch` rule is unchanged); the
  REVERSE direction (who counts as a match for whoever DOES own the
  category) was never gated on self-ownership and is completely
  unaffected — Helpful Hunter still correctly appears in Claws Out's own
  "Cats" matchingCardNames. Reverted ~5 pre-existing card-interactions
  .test.ts assertions back to their original "does NOT self-display"
  form once the fix made the earlier (wrong) ripple-update moot. Verified
  live against the real, running dev server (not just offline scripts) —
  also incidentally surfaced and resolved a real Nitro-dev HMR staleness
  artifact (a brand-new 5th `SINK_CATALOG` entry read as unregistered for
  ~1 request after file creation, self-resolved, not a code bug — see
  topics/fdn-sink-model-open-items.md). Full `functional-model` suite:
  117 files, 1239 passed/5 skipped. `npm run typecheck`: identical
  pre-existing baseline, zero new errors. Flagged (not fixed, out of this
  task's file scope): `server/api/sink-catalog/index.get.ts`'s
  `computeRealMatches` doesn't know about `consumerBattlefieldPresence`
  yet, so the review-tool page's own `consumerMatches` list stays
  `undefined` for both new entries (producerMatches still correct).
  Updated `card-schema.md` (new section 9). Left everything uncommitted
  per standing practice.

- Real gate-correctness bug found+fixed: `inspiring-paladin` had a whole
  real ability left completely unmodeled (free-text `staticAbilities` +
  code comment admitting the gap, no structural marker at all) yet
  `validate-card-definition.mjs` returned `ok:true`/`blue` — confirmed
  root cause in the actual gate script (only checks vocabulary of what's
  USED, blind to a `staticAbilities` entry with no functional
  counterpart at all; `card-status.ts`'s own `isUnsupportedNoOp` doc
  comment already named this exact blind spot). Per an explicit user
  ruling simplifying the original fuzzier brief: added a blunt,
  deterministic `findStaticAbilityGapReasons` to
  `validate-card-definition.mjs` (exported, unit-tested in new
  `validate-card-definition.test.ts`) — ANY non-empty `staticAbilities`
  entry, on either face, now automatically caps the gate at
  `failureKind:'capacity-gap'`/`purple`, no per-entry judgment call, even
  when another part of the same card models that identical clause
  correctly elsewhere. Reran `gate-and-write-status.mjs --all` across all
  100 fdn-cards: 16 flipped `blue`→`purple` (`inspiring-paladin` +15
  others — see `topics/fdn-static-abilities-gate-rule.md` for the full
  list), 0 other/missing, new totals 69 blue/31 purple (was 85/15). Full
  `functional-model` suite green (118 files, 1244 passed/5 skipped).
  `npx nuxt typecheck`: identical pre-existing baseline
  (`card-status.ts:263`, `card.ts:2970`, `mana.ts:275`,
  `server/api/tokens/by-key.ts`) plus pre-existing, not-mine
  `CardDetailTabs.vue` errors from concurrent in-flight work — zero new
  errors from any file this task touched. Did not touch
  `sink-model/`/`match-sink.ts`/`card-interactions.ts`/`catalog/*` (a
  concurrent agent's scope) — confirmed via `git diff --stat` before
  finishing. Left everything uncommitted per standing practice.

- Two independent catalog-side fixes, dispatched together (deliberately
  zero overlap with the concurrent gate-correctness agent's `fdn-cards/*`/
  validator-script scope, confirmed via `git status` before finishing):
  (1) Dazzling Angel's (FDN #9) ETB-consumer gap: its real "whenever
  ANOTHER creature you control enters" trigger is name-only
  (`name:'onOtherCreatureEnter'`, no `on` value — CR 603.6b, a real,
  still-unbuilt engine gap `consumerTriggerOn:['enter']` can never close for
  this shape) — added a sibling `consumerTriggerNames:
  ['onOtherCreatureEnter']` to `etb.ts` (grepped every real FDN
  `definition.ts` first: zero name collisions; Skyknight Squire's own
  "another creature enters" convention is modeled differently, via
  `on:'enter'`, already covered). (2) New sink catalog entry
  `counters-plus1plus1` (Exemplar of Light, FDN #11 — a genuine
  self-referential Lifegain-consumer/Counters-producer + Counters-consumer
  loop) — needed ZERO new matching code on either side: producer reuses the
  EXISTING generic `putCounter`/`putCounterTarget`/`putCounterAll`
  occurrence + its pre-existing `counterType` equality check
  (`query: {category:'Counters (+1/+1)', event:'putCounter',
  counterType:'+1/+1', controller:'you'}`); consumer reuses the EXISTING
  `consumerTriggerNames` mechanism (`['onCounterAdded']`, same real-gap
  reasoning as Dazzling Angel — no `Trigger.on` member for "counter added"
  either). Reasoned through and deliberately did NOT set
  `requireConsumerForSelfOwnership` for this entry (unlike Battlefield
  presence) — putting a counter via a real `putCounter`-family effect is a
  genuine authored ability, not bare type/subtype membership, so a direct
  producer match IS legitimate self-ownership evidence here. Live
  verification surfaced a real, separate THIRD gap not in the original
  brief: Exemplar of Light's own "gain life" REACTOR trigger is named
  `'onLifeGain'` (missing the `d`) — a real, checked-in spelling variant of
  Ajani's Pridemate's `'onLifeGained'`, not a typo to collapse — so
  `lifegain.ts`'s `consumerTriggerNames` list was missing it entirely
  (confirmed Exemplar of Light showed NO "Lifegain" row before this fix,
  contrary to the task's own initial assumption it already did); widened
  to `['onLifeGained', 'onLifeGain']` (zero collision, grepped). Also
  surfaced (not a bug, a real newly-additive consequence of adding a new
  catalog entry): Ajani's Pridemate and Felidar Savior both ALSO now
  self-own+self-match "Counters (+1/+1)" (real, direct `putCounter`-family
  effects of their own, unconstrained-target query matches broadly by
  design, same convention every other unconstrained query already uses) —
  updated their pre-existing `card-interactions.test.ts` assertions to
  match rather than treating it as a regression to suppress. All 3 new/
  changed catalog entries' own `.test.ts`/`.corpus.json` pairs updated in
  lockstep (`etb`: 8->11 cases; `lifegain`: 8->9 cases; new
  `counters-plus1plus1`: 9 cases, `blue` on first landing).
  `sink-catalog-status.test.ts` updated for the 6th registered slug/
  category. Full `functional-model` suite green (119 files, 1261 passed/5
  skipped, was 117/1239/5). `npm run typecheck`: identical pre-existing
  baseline error set (`CardDetailTabs.vue`, `card-status.ts:263`,
  `card.ts:2970`, `mana.ts:275`, `server/api/tokens/by-key.ts`), zero new
  errors. Live-verified against the real, running dev server's
  `/api/card/fdn/9`, `/api/card/fdn/11`, and `/api/sink-catalog` (before/
  after) — Dazzling Angel now shows both "Lifegain"/"ETB"; Exemplar of
  Light now shows both "Counters (+1/+1)"/"Lifegain"; the new entry appears
  distinctly labeled, `blue`, with real producer/consumer counts on the
  catalog route. Left everything uncommitted per standing practice.

## Fourth silent-gap class: name-only Trigger gate rule

Added `findNameOnlyTriggerGapReasons` to `validate-card-definition.mjs`
(FDN-only gate) — any `Trigger` with no `on` value caps a card at
`purple`. Confirmed legitimate/unflaggable in FIN (205 real instances, all
backed by real `scenarios.ts` manual invocation) but a genuine dead end in
FDN (no `scenarios.ts` exists there at all). Checked all 28 real FDN
name-only-trigger instances individually, no exception found. Unit tests
added to `validate-card-definition.test.ts` (12/12 passing). Ran
`gate-and-write-status.mjs` against 99/100 FDN slugs (excluded
`hare-apparent`, mid-edit by a concurrent session) — 21 more cards flipped
`blue`->`purple`. Full `functional-model` vitest suite (120 files) green;
scoped `tsc --noEmit` clean on the touched files.

## Hare Apparent — third "Battlefield presence" filter variant (same-name copies)

Task: extend the battlefield-presence sink with a "same name as self"
filter variant for Hare Apparent (FDN #15), whose ETB token-count was a
raw `(ctx) => number` closure opaque to `matchesBattlefieldPresenceConsumer`.
Chose Option 1 (new combinator primitive: `FilterPredicate:
{field:'sameNameAsSelf'}`, mirrors `'excludeSelf'`) over Option 2 (narrow
additive marker) — cheap because `Query.source:'creaturesInPlay'`/
`Aggregate{op:'count'}` already existed, only one new predicate was
needed. Migrated `hare-apparent/definition.ts`'s `createToken.amount` onto
`you.creaturesInPlay().filter('sameNameAsSelf').count()`; widened
`card.ts`'s `createToken.amount` to `Computed<number> | ValueRef` (new
`resolveCreateTokenAmount`, scoped narrowly, not a `resolve<T>` widening);
exported `combinator.ts`'s `resolveValue`. New catalog entry
`battlefield-presence-hare-apparent` (7th SINK_CATALOG entry) — producer
query is a literal `name:{eq:'Hare Apparent'}` (deliberately bespoke/
one-card, sanctioned by `entry.ts`'s own doc comment — "same name as self"
has no honest generic producer query the way Cat/Creature subtype does);
consumer side (`consumerBattlefieldPresence:{sameNameAsSelf:true}`) walks
`createToken.amount`'s own AST via new `match-sink.ts` helpers
(`effectsCareAboutSameNameCount`/`isSameNameCountValueRef`/
`queryChainHasSameNameFilter`); `requireConsumerForSelfOwnership:true`
(same reasoning as Cats/Creatures — bare producer self-match here is just
as trivial/non-deliberate). Two incidental typecheck fixes needed for
exhaustiveness: `combinator.ts`'s `describePredicate` and
`recognizers/program-ast-walker.ts`'s `readPool` (declines the new
predicate — no real `program` effect chains it). Re-ran
`gate-and-write-status.mjs hare-apparent` (still blue). Live-verified via
the already-running dev server: `GET /api/card/fdn/15` now shows
`{category:'Same-name copies', count:1, matches:[{card:'Hare Apparent',
self:true}]}`; `GET /api/sink-catalog` lists the new entry distinctly
(`blue`, 7/7 corpus, `realMatches.producerMatches:['Hare Apparent']` only).
Full `functional-model` suite: 120 files, 1276 passed/5 skipped (was 120
files/1269 before the 2 incidental fixes added a few more parametrized
cases). `npm run typecheck`: identical pre-existing baseline error set,
zero new errors. Did not commit — left for orchestrator review. Full
writeup in `topics/fdn-sink-model-open-items.md`.
