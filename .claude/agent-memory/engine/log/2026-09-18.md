- Migrated `.claude/agent-memory/engine/` from a single ~30,140-line
  append-only `notes.md` into the hub shape (`current.md` + `topics/*.md`
  + `log/*.md`) per SHARED.md's new memory-discipline spec. Read the old
  file in full (chunked). Found almost the entire file was either (a)
  per-card migration/bugfix session narration whose end state is already
  reflected in the current code/git history, or (b) cross-cutting
  design decisions already fully documented in
  `functional-model/SYNERGY_DESIGN.md`, `ENGINE_GAPS.md`,
  `PRD_AUTOMATED_AUTHORING.md`, and `.claude/contracts/card-schema.md` —
  spot-checked several claimed-novel decisions (Fact unification,
  scenario-count-defaults-to-1, the printed-Lifelink exception,
  `Fact.value` removal, the FDN pipeline-status/sink-catalog axis) and
  confirmed each was already written up there in more complete form.
  Kept only what wasn't captured elsewhere: 8 topic files covering
  tooling/CLI gotchas, concurrent-session hazards specific to this pool,
  a recognizer-wiring checklist, the sink-predicate-corpus mocking
  policy, the Features-tab `blue` evidence caveat, FDN/sink-model
  still-open items, and combinator.ts's still-open node kinds. Archived
  the old file's full content verbatim into 5 lump `log/archive-*.md`
  files (best-effort chronological, not exact — the original file wasn't
  strictly ordered by date throughout) rather than a per-date split, per
  the task's own "don't over-engineer the archive" instruction. Deleted
  the old `notes.md` (git history preserves it).

- Reverted the same-day sink-ATTACHMENT persistence layer per an explicit
  user correction: deleted `functional-model/sink-attachment.ts` + its
  test + the two real `sinks.json` artifacts (`ajani-s-pridemate`,
  `serra-angel`); restored `pipeline-status.ts`'s `blue` to its original
  gate-only meaning (removed the `isSinkAttachmentComplete` check from
  `effectivePipelineStatus`, doc comments record the tried-then-reverted
  history rather than silently deleting it); fixed `pipeline-status.test
  .ts` (removed 3 attachment-specific tests, folded `blue` into the
  generic pass-through `it.each`). Re-verified live: all 7 real FDN cards
  previously stored `blue` are back to effective `blue` (matches raw
  stored value 1:1, confirmed via a real script run, not assumed) — the
  3 `purple` cards unaffected either time. Updated `.claude/contracts/
  card-schema.md` to strike the attachment/blue-redefinition sections
  (kept catalog-entry/catalog-review sections, unaffected) with the same
  "tried, considered, reverted" posture.
- Implemented the "derive on the fly" replacement in
  `functional-model/card-interactions.ts`: checks `definition` against
  every `isSinkCatalogEntryUsable` `SINK_CATALOG` entry's own `query`
  FIRST via the existing `matchSink` (no new persistence, no new
  matching primitive) — a match swaps that occurrence's raw
  `describeFact` label for the catalog's real `query.category` label,
  self-inclusive pool count computed the same way. Verified live against
  all 10 real FDN cards: Day of Judgment now genuinely shows "Graveyard
  fodder" (was raw "destroy") — a real, correct win. Ajani's Pridemate
  does NOT get "Lifegain" under this design (flagged explicitly, not
  forced) — confirmed live `matchSink(lifegainQuery, ajanisPridemate)` is
  `false`, since the catalog query is producer-shaped and Ajani only
  reacts to lifegain (a consumer signal with no safe structural
  derivation, per this same file's own earlier same-day investigation).
  Updated `card-interactions.test.ts` (3 existing tests' expected
  category names changed: 'destroy'->'Graveyard fodder', 'life gain'
  ->'Lifegain' ×2; one existing test's assertion strengthened to also
  assert 'Lifegain' is absent for Ajani). Full `functional-model` suite
  green (113 files/1184+5 skipped); full-repo suite green apart from the
  5 pre-existing unrelated `scripts/relations.test.mjs` failures.
  Typecheck: 0 new errors in any file this task touched; new missing-
  module errors appeared in `card`/`server`-owned consumers of the
  deleted `sink-attachment.ts` (`CardDetailTabs.vue`, `cardResponse.ts`,
  `server/api/card/[set]/[number].ts`, `server/api/fdn-cards/[slug]/
  {review,sinks}.post.ts`) — expected fallout, explicitly the `card`
  agent's own next-step cleanup, not touched here.
