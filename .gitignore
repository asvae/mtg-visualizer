node_modules/
dist/
*.log
.env
.env.*
!.env.example
.claude/

# Live review-relay scratch queue — actively mutated by the running relay
# process, not meaningful project history (data/fin/fin_relations.json is the
# real source of truth once a card's confirmed).
scripts/review-drafts.json
scripts/review-responses.json
