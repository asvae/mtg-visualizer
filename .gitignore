node_modules/
dist/
.nuxt/
.output/
storybook-static/
*.log
.env
.env.*
!.env.example
.claude/*
!.claude/agents/
!.claude/agents/**
!.claude/contracts/
!.claude/contracts/**
!.claude/agent-memory/
!.claude/agent-memory/**

# Live review-relay scratch queue — actively mutated by the running relay
# process, not meaningful project history (data/fin/fin_relations.json is the
# real source of truth once a card's confirmed).
scripts/review-drafts.json
scripts/review-responses.json

# Local Netlify folder
.netlify
deno.lock

# Scratch/regenerable downloads (e.g. Scryfall bulk-data dumps), not source.
tmp/

# forge-json-mapper (experiment) output, consumed by forge-json-compiler
# (a real FDN authoring-pipeline tool as of 2026-09-19, promoted out of
# scripts/experiments/): local-only per explicit user decision (2026-09-19)
# — Forge-corpus JSON output stays off-repo for now. counters.test.ts/
# fdn-1-50.test.ts's loadCompiled() reads these locally-generated files; a
# fresh clone/CI run has none of them and those tests will ENOENT there
# until this is revisited.
functional-model/scripts/experiments/forge-json-mapper/output/

# Local card database + its Scryfall bulk-data cache — regenerated via
# scripts/sync-card-db.mjs, not meaningful project history (multi-hundred-MB).
data/cards.db
data/.bulk-default-cards.jsonl.gz

# Commander Spellbook bulk-data cache — regenerated via
# functional-model/scripts/sync-combos.mjs, not meaningful project history
# (data/combos.json, its filtered output, IS committed).
data/.bulk-commander-spellbook-variants.json.gz

# Per-card scratch context for the FDN sink-only-synergy-model experiment's
# authoring pipeline — regenerated via
# functional-model/scripts/prep-card-context.mjs, not meaningful project
# history (Scryfall/Forge/XMage source dumps + a generated vocabulary
# reference, all derivable on demand).
functional-model/.fdn-scratch/
